# Sampora Issue Ledger

This ledger prevents historical issues from being rediscovered and repaired again without current evidence.

Use it before implementing any GPT, human, Codex, downloaded-report, or agent-generated issue list. The ledger is not proof by itself; it is a routing layer for status, owner, and required evidence.

## How To Use

1. If a new issue resembles an entry below, start from the ledger status instead of treating the issue as new.
2. Verify against current files, scripts, browser state, or zip entries.
3. Reclassify only with fresh evidence:
   - `[REGRESSION]` becomes `[CONFIRMED]` only when its current guard fails.
   - `[REMOVED]` stays no-repair/no-restore unless current evidence proves the issue has returned.
   - `[STRATEGY]` stays limited to docs, QA, packaging, or workflow policy.
4. If a repair changes source, reports, QA scripts, or zips, update the row's current state and last evidence.
5. Add new recurring issues here with a stable ID before they appear in multiple handoff entries.

## Current Ledger

| ID | Issue / historical failure | Status | Owner | Acceptance | Current state | Last evidence | Allowed action |
|---|---|---|---|---|---|---|---|
| REDIRECT-001 | Physical Chinese legacy redirect HTML files were present in public root or public zip. | `[REMOVED]` | Package E / Delivery | Docs/redirects, Packaging | Current policy is server/CDN rewrite only; do not restore physical Chinese redirect HTML files. | `page-layer-static-check.mjs` PASS for 8 physical HTML files and 7 absent Chinese legacy redirects; current handoff records public zip with 0 Chinese physical redirect entries. | Verify absence only. If any Chinese legacy HTML file returns, reclassify as `[CONFIRMED]` for Package E/Delivery. |
| REDIRECT-002 | QA static script expected removed Chinese legacy files and failed after redirect policy changed. | `[REGRESSION]` | Package E QA | Docs/redirects | Script now treats only `products.html` and `pricing.html` as physical redirect files and guards absence of Chinese legacy files. | `page-layer-static-check.mjs` PASS. | Run guard after redirect/script changes. Repair only if current script or source contradicts policy. |
| STICKY-001 | Homepage sticky nav source conflict: `.mast` top values diverged across base/condensed/override CSS. | `[REGRESSION]` | Package A Homepage | Navigation/sticky | 2026-05-18 scoped homepage regression repaired again: when `.status` condenses away, all homepage desktop condensed `.mast` overrides now pin to viewport top instead of leaving the 34px status-bar gap. Runtime sticky coverage exists. | 2026-05-18 18:58 CST Package A browser red check before repair showed `body.nav-condensed`, `.status` top `-35`, bottom `0`, opacity `0`, transform `translateY(-35)`, `.mast.top=34`, and hero/body content visible at y10/y20/y33. Post-repair 1440x900 Chromium samples at scroll `57/80/100/120/160/200/240` showed `body.nav-condensed`, `.status` hidden/up, and `.mast.top=0`; detailed completion samples at scroll `240` and `612` showed `gapViewportTopToMast=0`, nav visible with height `67`, and y10/y20/y33 inside `.mast`. Top-of-page sample kept status visible and `.mast.top=35` directly below it. `final-audit-static-check.mjs` PASS. | Verify homepage sticky source/runtime only when touching `index.html`, shared nav CSS, or browser sticky script. |
| STICKY-002 | Resources sticky nav source conflict: later override changed `.mast` top away from status height. | `[REGRESSION]` | Package B Solutions/Resources | Navigation/sticky | Resources source conflict was repaired; runtime sticky coverage exists. | Handoff records `.mast` top only at 34px and browser sticky samples. | Verify Resources sticky source/runtime only when touching `resources.html`, shared nav CSS, or browser sticky script. |
| CTA-001 | Bare or stale CTA links such as `contact.html#contact-form` appeared without required intent routing. | `[REGRESSION]` | Package A/B/C/D by touched page, controller for policy | CTA policy | Conversion CTAs must route to `contact.html` with required `intent`; old bare-anchor issue is historical. | Handoff/log record narrow CTA follow-ups and current static checks. | Run scoped CTA grep/intent checks for touched page. Do not sweep all CTAs from memory. |
| CONTACT-001 | Contact form backend readiness and placeholder behavior were repeatedly revisited. | `[REGRESSION]` | Package C Plans/Contact | Contact backend readiness | Frontend keeps `[BACKEND_CONTACT_ENDPOINT]` placeholder and must not claim backend receipt until real endpoint exists. | Handoff records contact browser payload/hidden-field checks and backend placeholder risk as external. | Verify contact/form behavior after `contact.html`, CTA intent, or backend endpoint changes. Do not replace placeholder without real endpoint. |
| I18N-001 | Footer/legal/i18n sweep risk: old legal names, ICP text, empty translations, or mojibake-like strings. | `[REGRESSION]` | Shared i18n / Package A/B/C/D + Package E policy | Legal/footer, Language/i18n, Forbidden copy | 2026-05-18 Package A `index.html` scoped homepage language repair completed the remaining Hindi desktop nav overwrite in the later chrome/footer helper. Homepage EN/ZH/HI switching now keeps hero, CTAs, workflow title/first step, footer description/legal, active language state, CTA hrefs, and Hindi nav labels language-correct. Earlier Package C `plans.html` and `contact.html` language switcher/footer legal/icon repairs remain recorded; broader non-homepage HI content standards stay deferred unless reassigned. | 2026-05-18 19:11 CST scoped homepage browser sweep PASS in Chrome at 1440px for EN -> ZH -> HI -> EN: HI nav rendered `उत्पाद / समाधान / संसाधन / योजनाएँ / संपर्क`; hero/CTA/workflow/footer/legal switched with no empty strings, no `????`, no replacement characters, no console errors, one active language state, and all sampled conversion CTA hrefs retained `intent=`. Inline `index.html` script syntax extraction -> OK for 4 scripts. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. | Use full regression for affected contract when touching i18n sweep, footer/legal, or shared copy. Use scoped homepage i18n/CTA checks when touching only `index.html` language chrome. |
| TOPOLOGY-001 | Workflow/topology motion was validated too weakly by screenshots or lost runtime animation state. | `[REGRESSION]` | Package A/B, QA browser guard | Workflow motion, Topology motion, Topology/modal details | Browser checks sample workflow steps, animation names, dash offsets, scan/packet visibility; 2026-05-18 v3 working-source homepage repair aligned the topology motion with `动效/index.html`; 2026-05-18 Resources modal repair keeps the close control inside the modal window bar and removes the internal console-window scrollbar; 2026-05-18 Solutions repair aligned flow primitives with `动效/solutions.html`, added panel -> network -> enterprise cycling, animateMotion packets, mark spacing, and homepage-style modal containment; 2026-05-18 Resources topology brand/motion repair aligned motion primitives with `动效/resources.html` while preserving acceptance-required `topoDash` naming. | Resources scoped checks PASS: hub label is `Sampora`, old inner `S` mark and `SMP` hub code removed, `topoScan` duration 5.8s, setup active flow uses `topoDash` 2.65s plus breathe, supplier amber uses `topoDash` 2.95s plus amber breathe, 9 animateMotion packets, 0 packet cx/cy attributes, changing dash offsets and packet coordinates over time, hub pulse active, modal close text `X` inside `.window-bar`, and panel overflow hidden. | Run motion/browser sampling after topology/workflow changes. Check close placement, modal scrollbars, route cycling, packet animateMotion, and product-mark shape/text after topology/modal edits. Do not repair from screenshot-only evidence. |
| MANUAL-001 | Resource manuals language/fallback/deep-link behavior regressed or used generic copy. | `[CONFIRMED]` | Package D Resource Manuals / controller reassignment | Language/i18n, Forbidden copy | New user standard says HI must not be all or largely English. Current `resource-manuals.html` HI runtime fails that broader content standard outside this footer icon/legal worker scope: nav, manual cards/app/sidebar, footer main, and footer contact are still mostly English. Per the user's latest priority, this worker recorded the red check only and did not perform broad HI copy repair. `assets/resource-manuals/data.js` and `assets/resource-manuals/app.js` were intentionally untouched. | 1440px HI runtime red check: `resource-manuals.html` nav is `Product / Solutions / Resources / Plans / Contact`; cards/manual app/sidebar/footer main/contact sections have `devanagari=0`; visible sample has Devanagari `12` vs Latin `2153`; aggregate has Devanagari `30` vs Latin `6447`. `resources.html` does not show the same broad failure: aggregate Devanagari `2221` vs Latin `1943`. | Controller should reassign before repair. Do not edit Resource Manuals assets unless page-local repair is proven insufficient and the controller explicitly expands scope. Rerun manual smoke/browser checks after any Package D language repair. |
| PATH-001 | Active handoff paths referenced old machine-specific absolute paths. | `[REMOVED]` | Package E handoff policy | Docs/redirects, Scope | Active entrypoint paths and public report source paths are repo-relative; historical entries remain legacy by policy. | Active-path scan passed; public text artifacts scan found no old absolute path under `sampora-website-public/`. | Use repo-relative paths. Do not create compatibility folders or symlinks for legacy paths. |
| PACKAGING-001 | Source was repaired but public/handoff zips still contained stale files or docs. | `[REGRESSION]` | Delivery / Package E | Packaging | Per the user's latest instruction, current working-source fixes after v3 packaging are intentionally not zipped until the user explicitly asks to package. Existing v3 zips may predate ongoing source changes. | Last public v3 zip inspection before later working-source fixes: 233 entries, 0 backslash entries, 8 root HTML files, 0 non-ASCII HTML files, 0 parent prefixes; SHA-256 `CABC9FCC53804442D4388AD79A73D9CCBB8F55072EDF695790346624028A9E75`. | Do not rebuild after point fixes. Do not create v4 or rebuild public/handoff zips until the user explicitly requests packaging/final delivery. When requested, inspect entries, compare source-vs-zip, and report SHA-256 hashes. |
| PACKAGING-002 | Same-name public folders or zips let reviewers inspect one artifact while the user uploads another. | `[STRATEGY]` | Delivery / controller | Packaging | Working source remains `sampora-website-public-v3/` during continued repairs. Final packaged deliverables should be produced only after explicit user packaging request. | User instruction on 2026-05-18 17:19 CST: continue modifying based on v3; do not package until asked. | Keep editing `sampora-website-public-v3/`. Do not treat previous v3 zip as latest source proof after source changes. Package and hash only on explicit user request. |
| QA-EVIDENCE-001 | Browser/Lighthouse verification refreshed many screenshots or JSON evidence files, making unrelated dirty diffs look like repair work. | `[STRATEGY]` | Controller / QA evidence | Scope, Accessibility/performance | QA evidence changes are verification-output batches, not page-source failures by themselves. | Current working tree and handoff record refreshed QA evidence from a parallel verification agent. | Do not revert or repair evidence files blindly. Attribute them to the verification round and judge source changes separately. |
| AUDIT-STALE-001 | Downloaded or historical five-layer reports listed issues that current source/package had already removed. | `[STRATEGY]` | Controller intake | Scope | Downloaded reports enter pending verification pool first and cannot directly create repairs. | Handoff records downloaded checklist reclassified as `[REMOVED]` or `[REGRESSION]` after current verification. | Verify current files before action. Use report only to choose checks or trace history. |

## Latest Ledger Evidence Notes

- 2026-05-18 20:35 CST / `I18N-001`: Controller total review confirmed and repaired the cross-page footer legal/icon blocker. Red browser evidence showed `index.html`, `resources.html`, `resource-manuals.html`, `plans.html`, and `contact.html` ZH footer legal still contained both English and Chinese company names, while TEL/LOC footer icon font sizes differed across pages (`10px`, `10.5px`, `11px`). After Package A/B-D/C repairs and controller verification, all six canonical pages pass: ZH footer legal shows `© 2026` plus Chinese company only; EN/HI show English company only; TEL/LOC compute to `10.5px`, `@` to `17px`, no icon overflow in 34px circles, and `overflowX=0`. `final-audit-static-check.mjs`, `page-layer-static-check.mjs`, `resource-manuals-language-smoke.mjs`, `resource-manuals-browser-language-check.mjs`, `page-layer-browser-check.mjs`, and `lighthouse-axe-check.mjs` all passed after the repair. Status remains `[REGRESSION]`; allowed action remains verify after footer/legal/i18n edits.

- 2026-05-18 20:35 CST / `TOPOLOGY-001`: Controller total review confirmed and repaired a homepage workflow startup blocker. Red browser evidence showed `index.html` workflow could stay stuck at Step 01 `relay-running` plus Step 02 `relay-receive` when `IntersectionObserver` did not deliver a start callback. The homepage workflow now has a visibility fallback and controller browser sampling passed: Step 01/02 at 0-1s, Step 02/03 at 3s, Step 04 at 6s, and loop back to Step 01/02 by 8s. `page-layer-browser-check.mjs` also passed with `failures: []`. Status remains `[REGRESSION]`; allowed action remains verify after workflow/topology edits.

- 2026-05-18 19:50 CST / `TOPOLOGY-001`: Controller sync after user clarification. The user confirmed the homepage/solutions topology highlight scan/sheen disable is intentional, not a regression. Treat `index.html` and `solutions.html` `.topo-scan` / highlight-only sheen `animationName: none` as expected where the current handoff records a highlight-disable task. Required topology business motion remains active flow/dash changes, packet or route motion, ticker motion where present, close placement, modal containment, and product mark stability. Do not re-enable highlight-only scan/sheen unless the user explicitly asks; future browser guards should distinguish this intentional disable from real topology motion loss. Status remains `[REGRESSION]`; allowed action remains scoped verification after topology/workflow edits.

- 2026-05-18 19:30 CST / `STICKY-001`: Package A `sampora-website-public-v3/index.html` mobile header overflow follow-up confirmed the prior 390px homepage document overflow. Red browser evidence before repair showed `docScroll=419`, `bodyScroll=419`, `overflow=29`; the actual right-edge offender was `.mast .header-actions` ending at `418.75px`, with `.mobile-nav-toggle` also ending at `418.75px`. The homepage now has a mobile-only header layout guard below 760px and clips the same mobile ribbon's settled 2px chip overhang so the document-level done gate passes. Post-repair browser checks passed at 390px EN/HI/back-EN with `docScroll=390`, `bodyScroll=390`, `overflow=0`, `.mast .header-actions.right=374`, `.mobile-nav-toggle.right=374`, and no right-edge offenders; 1440px EN overflow remained 0. Sticky desktop samples still kept `.mast` visible and pinned to viewport top after status condensed. `final-audit-static-check.mjs` PASS. Status remains `[REGRESSION]`; allowed action remains verify homepage header/sticky/runtime layout when touching `index.html` or shared nav CSS.

- 2026-05-18 18:50 CST / `TOPOLOGY-001`: Package A `sampora-website-public-v3/index.html` workflow connector refinement repaired the user-confirmed workflow visual issue without changing the four cards or the existing slower relay timing. Pre-repair browser evidence showed receiver border/shadow still used amber `rgba(255, 189, 90, ...)`, the active trace pseudo-element computed `display: none`, and the grid line background included amber, while source timing already contained `isLast ? 2500 : 1980`. After repair, `.workflow-flow-lines`, `workflowConnectorFlow`, and `data-flow-step` drive restrained cyan/blue desktop connector lanes. Browser samples progressed Step 01 -> 02 -> 03 -> 04 -> Step 01, active card transform stayed `matrix(..., -4)`, connector overlay displayed with `workflowConnectorFlow`, receiver border was `rgba(123, 227, 255, 0.42)`, active/receiver/last yellow detection was false, desktop overflow was 0, mobile workflow shell/grid overflow was 0 with connector overlay hidden, and `final-audit-static-check.mjs` PASS. Status remains `[REGRESSION]`; allowed action remains verify after workflow/topology edits.

- 2026-05-18 18:37 CST / `I18N-001`: Package A `sampora-website-public-v3/index.html` footer `@` icon follow-up converged the homepage email marker to the already repaired cross-page visual standard. The homepage `.sampora-footer-icon--email` rule now uses 17px instead of the prior 15px. 1440px browser geometry check passed: `@` box `34x34`, glyph `17.63x20`, side margins `8.19px/8.19px`, center offset `(0, -0.5)`, no overflow; `TEL` and `LOC` remain 10px with glyph `18x13`, side margins `8px/8px`, centered, and no overflow. Page horizontal overflow is 0; footer legal remains valid. `final-audit-static-check.mjs` PASS; `page-layer-static-check.mjs` PASS. Status remains `[REGRESSION]`; allowed action remains verify after footer/legal/i18n edits.

- 2026-05-18 18:33 CST / `I18N-001`: Package A `sampora-website-public-v3/index.html` footer contact icon balance was rechecked after the user's homepage screenshot feedback. Red browser evidence at 1440px showed the prior global `.sampora-footer-icon` 9px font kept `TEL`/`LOC` inside the 34px circles but made the single-character `@` too small (`@` fill ratio `0.353` versus `TEL`/`LOC` `0.477`). The homepage footer now uses explicit icon modifiers: `TEL` and `LOC` use compact 10px mono labels, while `@` uses a separate 15px single-character label. Post-repair browser check passed: `TEL` fill ratio `0.529`, `@` `0.559`, `LOC` `0.529`, all boxes `34x34`, centered, no overflow, page horizontal overflow `0`, and footer legal i18n still passed for EN/ZH/HI with no ICP/bad markers. `final-audit-static-check.mjs` PASS; `page-layer-static-check.mjs` PASS. Status remains `[REGRESSION]`; allowed action remains verify after footer/legal/i18n edits.

- 2026-05-18 18:29 CST / `TOPOLOGY-001`: Package A scoped homepage topology flow smoothing covered both normal hero and enlarged modal topology. Current-vs-reference diagnosis used `动效/index.html`: reference hero/modal has dash/pulse motion but 0 `animateMotion` packets and 0 packet filters; current v3 had 12 packets, packet `drop-shadow` filters on all packets, `topoDash` 2.4s, packet `dur=2.4s`, `topoScan` 4.2s, pulse animation, and prior live-node modal ownership already fixed. `sampora-website-public-v3/index.html` now keeps `topoDash` at 2.4s and active packets visible, removes packet drop-shadow filters, slows packet `animateMotion` to 3.6s, and softens/slows `topoScan` to 5.8s linear. Post-repair browser samples passed for hero and modal: packet filters `none`, dash offsets changed over time, packet coordinates changed, scan animation active at 5.8s, modal uses one live `#network` instance in `.topology-modal__body`, and 0 missing `mpath` targets. `final-audit-static-check.mjs` PASS; `page-layer-browser-check.mjs` PASS with `failures: []`. Status remains `[REGRESSION]`; allowed action remains verify after topology/modal edits.

- 2026-05-18 18:27 CST / `TOPOLOGY-001`: Package A `sampora-website-public-v3/index.html` workflow motion comment was verified with browser-time sampling, not screenshots. The issue was reclassified from `[VERIFY-FIRST]` to `[CONFIRMED]` for homepage workflow pacing only: red sample showed Step 01 -> Step 02 -> Step 03 -> Step 04 in about 4s, active-card transform stayed identity, section signal duration was 1s, and local reference `动效/index.html` used calmer `setTimeout(runStep, isLast ? 2500 : 1980)`. After repair, scoped source/browser checks passed: timer is `2500 : 1980`, section signal is `workflowSignal 9s ease-in-out infinite`, active trace is `workflowActiveTrace 2.2s`, browser sequence over 12s reached Step 04 at about 5.5s and looped to Step 01 at about 8.0s, active card lifted to `matrix(..., -4)`, active shadow/border were stronger, `overflowX=0`, and `final-audit-static-check.mjs` PASS. Status remains `[REGRESSION]`; allowed action remains verify after topology/workflow edits.
- 2026-05-18 18:19 CST / `TOPOLOGY-001`: Package A scoped homepage topology modal repair used `动效/index.html` as the reference file. Current v3 red check confirmed the enlarged modal cloned `#network` (`bodyChildClone="true"`) while the original remained in `.container.hero-grid`, leaving two topology animation instances alive and restarting the modal SVG animation state. `sampora-website-public-v3/index.html` now moves the live `#network` node into `.topology-modal__body` with a placeholder and restores it on close. Post-repair browser sample passed with `bodyChildId="network"`, `bodyChildClone=null`, `sourceCount=1`, close text `X` inside the panel, active `topoDash`/`topoScan`, changing dash offsets, moving packet coordinates, 12 `animateMotion` packets, 0 missing `mpath` targets, and restore-on-close back to `.container.hero-grid`. `final-audit-static-check.mjs` PASS; `page-layer-browser-check.mjs` PASS with `failures: []`. Status remains `[REGRESSION]`; allowed action remains verify after topology/modal edits.
- 2026-05-18 17:53 CST / `TOPOLOGY-001`: Package B scoped rerun for `sampora-website-public-v3/solutions.html` found the previously reported active-flow binding failure no longer reproduces. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-browser-check.mjs"` -> PASS with `failures: []`; static selector check confirmed `.topo-svg .flow.on` and `.topo-svg .flow-group.on .flow` both contain `animation: topoDash 2.4s linear infinite`, while `@keyframes topoDash`, `.topo-scan` animation, and `.packet.on` opacity remain present. Status remains `[REGRESSION]`; allowed action remains verify after topology edits.
- 2026-05-18 18:04 CST / `TOPOLOGY-001`: Package B scoped repro for `sampora-website-public-v3/resources.html` found the reported `#topologyClose` viewport top-right placement does not reproduce after forced reload. Static check showed no `consoleEl.appendChild(closeBtn)` path, `windowBar.appendChild(closeBtn)` in open/close modal paths, close text `X`, `.resource-console.modal-open .console-window` uses `overflow: hidden`, and `topoScan`/`topoDash` coverage remains present. Browser check at 1769x1270 showed `modalOpen=true`, `closeParentClass=window-bar`, `closeDirectAsideChild=false`, `closeInsidePanel=true`, `closeNearPanelTopRight=true`, `panelHasInternalScrollbar=false`, `scanAnimation=topoScan`, and changing flow dash offsets. Status remains `[REGRESSION]`; current assigned item is `[VERIFY-FIRST -> not reproduced]`, so no source repair was made.
- 2026-05-18 18:06 CST / `TOPOLOGY-001`: Package B `sampora-website-public-v3/resources.html` repair used `动效/resources.html` as the reference file. Confirmed current left hub had old `S` mark plus `SMP`; current motion differed from reference (`topoScan 4.2s`, fast `topoDash 1.15s`, static packet cx/cy points, no hub pulse animation). After repair, static and browser checks passed with full `Sampora` hub label, no `brand-core-mark`, no `SMP` hub code, `topoScan 5.8s`, active setup `topoDash 2.65s`, active supplier amber `topoDash 2.95s`, 9 `animateMotion` packets, 0 packet cx/cy attributes, changing dash/packet positions, hubPulse active, and modal close still inside `.window-bar`. Status remains `[REGRESSION]`; allowed action remains verify after topology edits.
- 2026-05-18 18:05 CST / `I18N-001` and `MANUAL-001`: Resources/Resource Manuals footer icon/legal scoped worker fixed and verified assigned footer legal/icon issues. A separate HI broad-content red check found `resource-manuals.html` currently fails the new "HI cannot be all or largely English" standard, while `resources.html` does not show the same broad failure. Per the user's latest priority, broad HI content repair is deferred for controller reassignment; no Resource Manuals assets or packages were changed.
- 2026-05-18 18:12 CST / `TOPOLOGY-001`, `CTA-001`, and `I18N-001`: Package A `sampora-website-public-v3/index.html` repair fixed homepage topology modal replacement/motion loss, workflow relay instability, primary CTA hover shine, status pulse, language switcher structure, footer structure/icons/legal i18n. Scoped browser check PASS: modal open for 10.4s kept `replaceChildren` count at 1, inline and modal active flow/packet counts stayed nonzero after project ticks, `topoDash`/`topoScan` stayed active, dash offsets and packet positions changed, and modal/source run titles stayed synchronized. Workflow showed Step 01/02/03/04 with 0 injected relay spans and stable layout. Four primary CTAs kept hrefs and used `translateY(-1px)` plus shadow with no visible `::after`. Header language buttons passed 1440/1366 EN/ZH/HI geometry with no `lang-pill` and no overflow. Footer legal/icon checks passed: EN/HI legal use English fallback, ZH legal shows `安徽省嘉禹企业服务有限公司`, no bad markers/ICP, and TEL/@/LOC 34px icons do not overflow. `final-audit-static-check.mjs` PASS; `page-layer-static-check.mjs` PASS. A separate homepage HI broad-content red check found HI still mostly English (`devanagariKeyCount=0`, 178/180 visible key nodes mostly ASCII); per latest user priority, broad HI content repair was deferred for controller reassignment and not implemented. Statuses remain `[REGRESSION]`; allowed action remains verify after touched-page topology/CTA/i18n edits.

- 2026-05-18 18:20 CST / `TOPOLOGY-001` and `CTA-001`: Package B `sampora-website-public-v3/solutions.html` repair completed the confirmed Solutions topology scope. Browser samples at 1440px showed route cycle `panel -> network -> enterprise -> panel` at `t=0/3.2/6.5/9.7`, active flow animation `topoDash` at `2.4s`, dash offset changed `-12.6693px -> -28.3353px`, packet center moved `(961.34,625.12) -> (988.55,600.49)`, `animateMotionCount=13`, `packetCxCount=0`, and no console errors. Normal hub S/Sampora overlap is gone (`gap=5.24px`); modal clone overlap is gone (`gap=10px`), clone has 13 `animateMotion` nodes, 3 active flows, 3 active packets, close text `X`, close inside header/panel, panel overflow hidden, body overflow auto, and no panel scrollbar. Two Solutions primary Start trial CTAs kept `contact.html?intent=start_trial#contact-form`, hovered with about `translateY(-1px)` plus shadow, and had no `.btn.primary::after` shine. `final-audit-static-check.mjs` PASS; `page-layer-static-check.mjs` PASS; inline script syntax check OK. Status remains `[REGRESSION]`; allowed action remains verify after topology/CTA edits.
- 2026-05-18 18:20 CST / `I18N-001`: Package B `sampora-website-public-v3/solutions.html` footer icon/legal scoped repair passed. Footer icons are `TEL`, `@`, and `LOC`, each fixed 34px with `flex-basis:34px`, centered text, and no overflow. Runtime legal check passed for EN/ZH/HI: EN legal is `© 2026 Anhui Jiayu Enterprise Service Co., Ltd.` with no Chinese company; ZH legal is `© 2026 安徽省嘉禹企业服务有限公司`; HI legal uses the English company fallback and is non-empty with no question marks, replacement character, ICP, or placeholder markers. Separate Solutions HI broad-content red check remains recorded but unrepaired per the user's latest priority: HI hero/topology/footer still contain English/Hinglish fallback, topology includes visible `undefined` mode-prefix text, and footer retains English labels such as `Sample operations`, `Supplier coordination`, `Multi-party settlement`, and `Contact sales`. Controller should reassign broad HI content only after Package C language-switcher priority work.

- 2026-05-18 18:29 CST / `I18N-001`: Package C `sampora-website-public-v3/plans.html` and `contact.html` footer contact icon follow-up repaired the too-small `@` icon without reopening the prior LOC overflow. Red check showed the shared 11px icon rule made `@` only `6.83px` wide in Plans and `6.28px` wide in Contact. After repair, `@` uses `.sampora-footer-icon--at` at 16px while TEL/LOC keep the safer base sizing. 1440px browser check passed across EN/ZH/HI/back-EN: `@` measured 16px with centered text and safe margins, LOC remained 11px with at least 6.76px side margin, TEL stayed centered, footer legal remained language-correct, and `overflowX=0`. `final-audit-static-check.mjs` PASS; `page-layer-static-check.mjs` PASS. Status remains `[REGRESSION]`; allowed action remains verify after footer/legal/i18n edits.

- 2026-05-18 18:31 CST / `I18N-001`: Package B `sampora-website-public-v3/solutions.html` footer contact icon follow-up repaired the user-reported undersized `@` symbol without shrinking TEL/LOC further. Red check showed `@` text width `6.31px` versus TEL/LOC `18.91px` inside the same 34px circle. Post-repair 1440px browser check passed: `@` uses `.sampora-footer-icon--at`, 17px Inter, text width `17.63px`, margins `8.19px/8.19px`, center offset `0/-0.5`, and no overflow; TEL and LOC remain 10.5px monospace with text width `18.91px`, margins `7.55px/7.55px`, center offset `0/-0.25`, and no overflow. Page horizontal overflow is 0. Topology guard also passed in the same run: route cycle `panel -> network -> enterprise -> panel`, active `topoDash 2.4s`, dash offset changed, packet center moved, `animateMotionCount=13`, and `packetCxCount=0`. `final-audit-static-check.mjs` PASS; `page-layer-static-check.mjs` PASS. Status remains `[REGRESSION]`; allowed action remains verify after footer/legal/i18n edits.

## Add New Rows With This Template

| ID | Issue / historical failure | Status | Owner | Acceptance | Current state | Last evidence | Allowed action |
|---|---|---|---|---|---|---|---|
| AREA-### | Short recurring issue name. | `[VERIFY-FIRST]` | Package / controller owner | Acceptance category | Current state from current files. | Command/browser/zip evidence. | Verification or repair rule. |
