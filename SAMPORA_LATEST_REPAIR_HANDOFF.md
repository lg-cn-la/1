# Sampora Latest Repair Handoff

> Short entrypoint for new Codex windows and controller/subagent handoffs. Detailed historical logs are archived in `SAMPORA_LATEST_REPAIR_LOG.md`.

## READ FIRST / UPDATE REQUIRED

- Any new window, worker subagent, reviewer subagent, or controller must read this file before modifying Sampora files.
- Also read `ACCEPTANCE_TESTS.md`; it defines what must be checked before delivery claims.
- Also check `ISSUE_LEDGER.md`; it records stable IDs, current statuses, and allowed actions for recurring issues.
- Default entry is this short handoff. Read SAMPORA_LATEST_REPAIR_LOG.md only for history or regression tracing.
- Default Sampora repair role is controller-only orchestration: issue intake, status decisions, package/file ownership, subagent dispatch, conflict handling, evidence synthesis, and handoff records. Do not directly implement page/source repairs, own first-pass verification, or own final review when suitable subagents are available. Send first-pass verification to read-only verification subagents, implementation to implementation subagents with disjoint file ownership, and visual/final review to read-only review subagents. Controller docs/process updates are allowed when the task is handoff, acceptance, ledger, or intake policy.
- Before editing, summarize active scope, file ownership, known pitfalls, and planned verification.
- During execution, keep changes inside the assigned package boundary.
- Use short write locks for implementation work: lock only while actually writing files, release immediately after the write completes, and do not hold a write lock during read-only verification, browser checks, or review.
- Before final response, append concise status to this file or, for detailed package history, to SAMPORA_LATEST_REPAIR_LOG.md.
- If a new pitfall is discovered, record symptom, root cause, prevention rule, verification, and owner package.

## Path Policy

- Resolve active paths relative to the repository root, the directory that contains `AGENTS.md`.
- Treat historical absolute paths in old handoff/log entries as legacy paths from a previous machine. Map them to the same repo-relative file when possible.
- Do not create compatibility folders or symlinks for legacy absolute paths.
- New handoff entries should use repo-relative paths unless quoting a command output or historical artifact exactly.

## Cross-Machine Git Push Policy

When the user expects GitHub to be pulled on another computer for continued work, each push must include the files needed to reconstruct the working context, not just the latest visible page edit.

- **Class 1 / must push:** current page/source files that changed, active handoff docs, acceptance/ledger/glossary/policy docs touched in the round, repair scripts used by the workflow, and any small code/config file needed for the site or QA commands to run on another machine.
- **Class 2 / should push:** QA scripts and compact QA evidence files such as `.json` outputs that describe current checks, browser geometry, language evidence, Lighthouse/axe results, or page-layer results. These are lightweight working context and should travel with the branch when updated.
- **Class 3 / push only when needed:** screenshot/image/video QA evidence. Screenshots are useful for visual handoff and final delivery proof, but they are large and change often. Do not include new screenshot churn in ordinary small repair pushes unless the user asks for visual proof, a final handoff needs it, or the screenshot itself is the accepted evidence artifact.
- Before committing, inspect the staged file list and separate unrelated delivery-package deletions, old public package deletions, temp browser folders, and untracked historical/reference folders from the source/QA/context push unless the user explicitly assigns packaging or cleanup.
- If a relevant Class 1 or Class 2 file already exists on the remote and did not change in the current round, it does not need a new commit, but the final report should say it is already present when the user asks.

## Required Intake File Index

Use this index before classifying, dispatching, repairing, or reviewing Sampora work. It prevents controllers and subagents from reading only the main three files and missing supporting handoff material.

Always read or check:

- `AGENTS.md` for workspace-level Sampora process rules and the intake file index.
- `SAMPORA_LATEST_REPAIR_HANDOFF.md` for active scope, source of truth, package boundaries, pitfalls, and latest notes.
- `ACCEPTANCE_TESTS.md` for acceptance categories, issue-status workflow, validation tiers, and required proof.
- `ISSUE_LEDGER.md` for stable recurring IDs, status, evidence, and allowed action.
- `SAMPORA_TERMINOLOGY_GLOSSARY.md` before any visible-copy, i18n, topology-label, workflow-copy, CTA/footer/FAQ/pricing, dynamic data, or resource-manual language change.

Read only when relevant:

- `SAMPORA_LATEST_REPAIR_LOG.md` for detailed history or regression tracing. It is not current proof.
- `redirect-map.md` for redirect, deployment, package, or legacy-route work.
- `sampora-website-public-v3/README.md`, `DEPLOYMENT_NOTES.md`, `QA_REPORT.md`, `verification-report.md`, `backend-form-handoff.md`, `content-audit-fixes.md`, `visual-animation-fixes.md`, and `MODIFICATION_SUMMARY.md` for active-source package docs and prior QA notes. Verify current files before relying on them.
- `README.md`, `PROJECT_HANDOFF.md`, `SurveySaaS_官网终审交接文档.md`, `docs/surveysaas/product-positioning-summary.md`, `docs/surveysaas/sample-supplier-taxonomy.md`, and `docs/surveysaas/backend-function-map.md` for product positioning, target-customer taxonomy, backend capability, and claim-authenticity references. Read when positioning, visible claims, role taxonomy, product capability, backend evidence, or sales/demo narrative is in scope; verify current page/source files before relying on them.
- `补充资料归档/README.md`, `补充资料归档/operations-console/README.md`, `补充资料归档/operations-console/DESIGN_STRATEGY_v4.7.md`, and `补充资料归档/operations-console/V48_R8_REVIEWED_DESIGN_NOTES.md` for local design/product reference material. Read when visual direction, operations-console design strategy, or old design rationale is relevant; do not treat it as current proof.
- `SAMPORA_CONFIRMED_FIX_RESULT.md`, `SAMPORA_COPY_FIX_RESULT.md`, `SAMPORA_RUNTIME_VERIFY_RESULT.md`, `SAMPORA_TOPOLOGY_MARK_FIX_RESULT.md`, `content-audit-fixes.md`, `verification-report.md`, and `visual-dynamic-fixes.md` for historical repair/result reports. Read only when tracing prior repair decisions, package identity, or regression history; rerun current checks before action.
- `archives/`, `动态/`, and other local reference/demo folders, when present, for historical/reference context only. Do not treat archived reports, reference HTML, screenshots, or old QA JSON as current proof.

When dispatching subagents, paste the subset of this index that matches their assignment. Do not assume a subagent will discover supporting handoff files without being told.

## Current Status Summary

**Current closeout as of 2026-05-18 20:35 CST:** all controller-thread subagent tasks have completed and were controller-reviewed. The total review blockers found during this round (cross-page footer legal/icon consistency and homepage workflow motion fallback) are repaired and verified in the active working source `sampora-website-public-v3/`. No active controller-thread task queue remains, and no new task is queued by this handoff. Do not run compression, zip, packaging, v4 creation, or zip rebuild steps unless the user explicitly asks for packaging. The older timestamped summary below is historical context and must not be used as permission to package.

**Status as of 2026-05-18 16:48 CST:** homepage topology motion has been adjusted in the active v3 working source using the local topology reference documented in the detailed log. Per the user's latest instruction, packaging and full final delivery validation are deferred until the current batch of point-by-point fixes is complete.

**Source of truth:** `sampora-website-public-v3/`

**Final package outputs:**
- `sampora-website-public-v3.zip`
- `sampora-website-handoff-v3.zip`

These zips are the last packaged outputs before the latest working-source motion fix. Rebuild and hash the next versioned deliverables only after the user confirms the batch is complete.

**Detailed history:** `SAMPORA_LATEST_REPAIR_LOG.md`

**Acceptance contract:** `ACCEPTANCE_TESTS.md`

**Recurring issue ledger:** `ISSUE_LEDGER.md`

## Historical Mojibake / Archive Boundary Rule

- Historical mojibake-looking strings may appear only in `SAMPORA_LATEST_REPAIR_LOG.md` or in clearly labeled historical evidence blocks.
- This active handoff, current acceptance rules, current ledger routing rows, and current page/source files must not contain unlabeled mojibake-looking strings such as repeated question marks, replacement characters, or legacy garbled samples.
- Agents must verify current files, scripts, or browser state before repairing. Do not treat archived mojibake samples, old screenshots, old reports, or old QA JSON as current proof.
- Do not copy historical mojibake markers into source files, translation objects, QA scripts, README/deployment docs, or active handoff notes.
- If a future task needs to cite a historical marker, put the exact sample in the detailed log under a dated archive heading and summarize it in active docs with ASCII-safe wording.

## Package Status

| Package | Files | Current Status | Remaining Risk |
|---|---|---|---|
| A Homepage | index.html | Homepage sticky-nav source conflict repaired; workflow/topology static and browser QA included in final verification | None found in final verification |
| B Solutions/Resources | solutions.html, resources.html | Resources sticky-nav source conflict repaired; topology/static/browser checks included in final verification | None found in final verification |
| C Plans/Contact | plans.html, contact.html | Backend-ready placeholder form, browser follow-up, review complete | Real backend endpoint still external: [BACKEND_CONTACT_ENDPOINT] |
| D Resource Manuals | resource-manuals.html, assets/resource-manuals/data.js, assets/resource-manuals/app.js | Fallback hardening, language smoke/browser checks, review complete | None found in final verification |
| E Docs | README/deployment/QA/verification/redirect docs | Chinese legacy route policy aligned to server/CDN rewrites; static/browser QA coverage updated; review complete | None found in final verification |
| Delivery | sampora-website-delivery, public/handoff zips | Synced and rebuilt from current source | Axe has 8 non-blocking violations, 0 blocking |

## Fresh Verification Evidence

This window reran the critical final checks after the other agent reported completion:

- node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs" -> PASS final audit static checks
- node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs" -> PASS static checks for 8 physical HTML files and 7 absent Chinese legacy redirects
- node --check "sampora-website-public-v3\assets\i18n-sweep.js" -> exit 0
- node --check "sampora-website-public-v3\assets\resource-manuals\data.js" -> exit 0
- node --check "sampora-website-public-v3\assets\resource-manuals\app.js" -> exit 0
- node "sampora-website-public-v3\qa-evidence\resource-manuals-language-smoke.mjs" -> resource manuals language smoke: OK
- node "sampora-website-public-v3\qa-evidence\resource-manuals-browser-language-check.mjs" -> resource manuals browser language check: OK
- node "sampora-website-public-v3\qa-evidence\page-layer-browser-check.mjs" -> status: PASS, failures: [], screenshots: 42
- node "sampora-website-public-v3\qa-evidence\lighthouse-axe-check.mjs" -> status: PASS, axeViolationCount: 8, axeBlockingViolationCount: 0
- dedicated topology mark browser check -> PASS for EN/ZH/HI and modal clone: nav/text `S`, matching cyan text, monospace font, rounded-rect mark, 0 inner mark circles.
- dedicated topology motion browser check -> PASS: `topoDash` duration 2.4s, changing stroke dash offsets, moving packet coordinates, `pulse-ring`, `topoScan`, and modal clone motion all active.
- page-layer browser evidence includes sticky samples for index, solutions, resources, resource-manuals, plans, and contact.
- Previous public v3 source/zip comparison before the latest motion fix -> 233 source files, 0 missing, 0 mismatches, 0 extra.
- Previous zip inspection before the latest motion fix -> public v3 zip has 233 entries, handoff v3 zip has 18 entries, both have 0 backslash entries; public zip has 0 non-ASCII HTML entries and 0 parent-prefix entries.
- Current packaging note -> source changed after those v3 zips; do not treat existing v3 zip hashes as proof of the latest working source. Rebuild once after the batch is complete.

## Non-Negotiable Scope For Future Changes

- New agents/controllers must operate as controller-only orchestrators for Sampora repairs: issue intake, issue-status decision, package/file ownership, subagent dispatch, conflict handling, evidence synthesis, and handoff logging. Read-only verification subagents own first-pass verification; implementation subagents own page/source repairs; read-only review subagents own visual acceptance review and final review. The controller performs docs/process governance edits and only runs minimal direct sanity checks when subagent evidence is missing, contradictory, blocked, or explicitly challenged, unless the user explicitly approves a no-subagent/controller-only exception.
- Implementation agents must use short write locks. Before any write, check the latest handoff and current diff for active/recent ownership of the same file, selector, section, or function; announce the exact file and scoped region to be written; then write only if no overlap exists. Unlock immediately after the file write is complete, before verification. If overlap exists, report `[BLOCKED: overlapping write ownership]` and do not edit.
- Fix only issues named in the latest audit or the user's newest message.
- Anything not explicitly named is out of scope and must not be changed.
- Every new visual, business-path, language, motion, legal, or packaging regression must be mapped to `ACCEPTANCE_TESTS.md` before repair.
- If no acceptance item covers the failure, add or update one before or alongside the fix.
- Treat `sampora-website-public-v3/` as the current working source of truth. Per the user's latest batching instruction, keep point-by-point repairs in the active working source and defer the next versioned zip rebuild/full final validation until all current fixes are complete.
- Do not rely on old reports, old screenshots, or previous QA JSON as proof.
- Do not paste mojibake or broken multilingual text into source files or active handoff notes.
- Visible copy changes are multilingual by default. When any page-visible text, i18n key, card body, CTA copy, FAQ, workflow/topology label, pricing copy, footer copy, hero proof card, or dynamic project/sample data is changed in one language, the same semantic update must be made in EN, ZH, and HI primary translations/source data in the same task. Do not rely on `i18n-sweep.js` alone as a substitute. A single-language edit is allowed only when the newest user message explicitly limits scope to one language; the worker prompt and handoff entry must record that exception and name the other languages as intentionally deferred.
- Page behavior and structure changes are multilingual by default too. Layout, DOM structure, links, data order, rendered module additions/removals, and visible workflow/card/module relationships must be synchronized and verified across EN, ZH, and HI, even when the changed source is not obviously "copy".
- Terminology source: read `SAMPORA_TERMINOLOGY_GLOSSARY.md` before changing any EN/ZH/HI visible copy, i18n key, topology label, workflow copy, pricing/FAQ/footer/CTA copy, dynamic sample/project data, or resource-manual language. Follow its canonical EN/ZH/HI glossary, topology policy, and Hindi style rules.
- Visual acceptance is mandatory for every visual, layout, component, interaction, style, spacing, sizing, hierarchy, card-structure, alignment, responsive, or motion change. EN/ZH/HI text sync is still required for copy changes, but it never replaces visual acceptance when the task affects rendered UI.
- Visual acceptance cannot be replaced by static checks, CSS-property checks, no-overflow checks, screenshot existence, or EN/ZH/HI copy checks. It must reach the real visual perception / 真实观感 layer: the reviewer must state whether a normal user actually sees the intended improvement, or whether the surface still looks unchanged, cramped, hard to read, unbalanced, misaligned, or unlike the user's target. It must map each user visual goal to rendered evidence across EN/ZH/HI and desktop/mobile, including before/after or current visual-difference notes, actual geometry thresholds such as height, spacing, boundaries, overlap, line count, and alignment deltas, plus explicit PASS/FAIL against any user-specified number or look-and-feel target.
- If the user names a visual target such as "around 150px", "align the border with content above/below", "title must not be squeezed", or "should not look basically unchanged", final acceptance must use rendered values and visual-difference judgment. If the measured result misses the threshold or the visible difference is not meaningful, record a remaining failure instead of claiming done.
- Confirmed legal names only: verify the Chinese legal name from current source/browser when legal copy is in scope; the English legal name is `Anhui Jiayu Enterprise Service Co., Ltd.`.
- Overseas-server deployment stage: do not display ICP numbers or ICP placeholders.

## Default Repair Intake Gate

When the user asks in Chinese or English to follow `AGENTS.md` and this handoff before taking over, that short instruction is enough to trigger the repair workflow. The agent must not ask the user to restate the handoff rules.

Before any file edit, complete a short internal intake gate. Do not print the full gate by default unless the user explicitly asks to see the plan. The gate must determine:

1. Current explicit issues to fix.
2. Explicit out-of-scope areas that must not be touched.
3. Package split and writable files for each package.
4. Acceptance categories affected in `ACCEPTANCE_TESTS.md`.
5. Issue status for each item: `[CONFIRMED]`, `[VERIFY-FIRST]`, `[STRATEGY]`, `[REGRESSION]`, `[REMOVED]`, `[OUT-OF-SCOPE]`, or `[BLOCKED]`.
6. Red-check commands and completion verification for each package.
7. Planned read-only verification subagents, implementation subagents, read-only review subagent, and final controller evidence synthesis.

If the gate finds unclear scope, cross-package write overlap, or any need to touch an unmentioned area, stop and ask the user before editing. If the gate is clear and the user's newest message asks to proceed or includes repair content, continue after the gate using the subagent workflow. By default, give only a brief status update instead of printing the full gate.

When the user provides a repair checklist, GPT/web-audit summary, or issue list, treat it as repair input and internally normalize every item before dispatch:

- acceptance category in `ACCEPTANCE_TESTS.md`
- existing test/script coverage
- coverage gap, if any
- owner package and writable files
- red check and completion check
- issue status and allowed action

Print a concise issue status table before edits when an issue list is supplied. Keep it short; it is not the full intake gate. Required columns: item, status, evidence/command, owner package, and allowed action. If an item cannot be mapped to an acceptance category and owner package, mark it `[BLOCKED]` and stop to ask only about that item before editing.

## Standard Repair Flow

Use this flow for every GPT, human, Codex, downloaded-report, or agent-generated issue list:

1. **Issue intake:** put every proposed issue into a pending verification pool. The list itself is not proof.
2. **Ledger check:** if an item resembles `ISSUE_LEDGER.md`, carry forward its stable ID, current status, last evidence, and allowed action before deciding whether fresh evidence changes that status.
3. **Status table:** verify each item against current files, scripts, or browser state, then label it `[CONFIRMED]`, `[VERIFY-FIRST]`, `[STRATEGY]`, `[REGRESSION]`, `[REMOVED]`, `[OUT-OF-SCOPE]`, or `[BLOCKED]`.
4. **Action:** repair only `[CONFIRMED]`; run verification only for `[VERIFY-FIRST]`; edit only docs/QA/packaging for `[STRATEGY]`; run guards only for `[REGRESSION]` until failure reclassifies it as `[CONFIRMED]`; do not repair `[REMOVED]` or `[OUT-OF-SCOPE]`; ask for `[BLOCKED]`.
5. **Scope:** assign each actionable item to one Package A/B/C/D/E owner and keep edits inside that writable file boundary.
6. **Verification:** small changes use scoped verification; global/shared changes use full regression for the affected contract; final delivery claims use full five-layer validation.

## Issue Status Workflow

- `[CONFIRMED]`: Current files, script output, or browser evidence prove the issue exists. It maps to an acceptance category and owner package. Only these items may receive page/source repairs.
- `[VERIFY-FIRST]`: The issue is plausible but not proven. Allowed action is limited to adding/running a scoped test, grep, static check, or browser verification. Reclassify before repairing.
- `[STRATEGY]`: The item concerns docs, redirect policy, packaging policy, deployment guidance, acceptance coverage, or handoff strategy. Allowed action is limited to those files and delivery policy; do not change page behavior.
- `[REGRESSION]`: The item is a standing regression guard. Rerun the mapped check every round and record PASS/FAIL. Repair only if current evidence shows a failure and the item is reclassified as `[CONFIRMED]`.
- `[REMOVED]`: The item is a historical issue that no longer exists in the current package. Do not repair it, do not restore removed behavior/files, and keep only evidence if needed.
- `[OUT-OF-SCOPE]`: The item is not in the latest audit/newest user message, or it would require an unassigned file. Do not repair.
- `[BLOCKED]`: The item is ambiguous, lacks owner/acceptance mapping, or cannot be verified from the current workspace. Ask for clarification before repair.

After repairs, final handoff must include scoped diff summary and fresh evidence: commands/browser checks run, PASS/FAIL results, remaining failures, and files intentionally left untouched.

## Repair Sequence And Done-When Gate

When multiple issue types are present, prefer this dependency order unless the controller has fresh evidence that a different order is safer:

1. Packaging, redirect policy, and public/handoff boundary.
2. Global i18n, footer/legal, and CTA policy.
3. Contact form backend readiness and intent handling.
4. Topology and workflow motion.
5. SEO, Lighthouse, axe, delivery sync, zip inspection, and final acceptance.

Do not run implementation work in parallel across dependency layers that can overwrite each other. Each implementation task must include a `Done when` checklist with concrete checks, such as exact static grep results, browser-time samples, visible text assertions, route/intent assertions, or named QA commands.

## Validation Scope Tiers

Verification depth must match the change scope:

- **Scoped check:** use for light, single-package, or docs-only changes. Run the mapped red check and completion check for the touched acceptance category only. Examples: README/redirect policy does not require topology motion checks; `contact.html` edits focus on contact form, CTA/intent, hidden fields, and relevant i18n checks.
- **Full regression for affected contract:** use when shared contracts change, such as `i18n-sweep.js`, footer/legal, CTA policy, shared header/nav, cross-page CSS, or QA scripts. Run the affected all-page language/footer/CTA/static/browser guard, but do not automatically add unrelated Lighthouse or resource-manual deep checks unless the changed contract touches them.
- **Full five-layer validation:** use before final delivery claims, public/handoff zip rebuilds, broad cross-package repairs, or after changes that affect multiple acceptance categories at once. This includes static checks, syntax checks, language/manual checks, browser checks, Lighthouse/axe, delivery sync, and zip inspection.

Do not use old reports, old screenshots, or old QA JSON as current evidence. They can help choose a scoped check, but the acceptance claim must come from current files, current scripts, or current browser evidence.

## Read-Only Verification Subagents

For long issue lists or multi-package audits, the controller should split first-pass verification across read-only verification subagents before creating the issue status table.

- Split by package or topic, such as Homepage, Solutions/Resources, Plans/Contact, Resource Manuals, Docs/redirects/packaging, or cross-page policy.
- Verification subagents must not edit files, update docs, rebuild packages, or repair anything.
- Each verification subagent checks only its assigned issue subset against current files, scripts, grep output, or browser state.
- Each verification subagent reports a compact table: item, exists now, evidence/command, recommended status, recommended owner package, and notes.
- The controller/main agent merges those reports, resolves conflicts, and makes the final issue status decision before dispatching implementation workers.
- Implementation workers may receive only items the controller has classified as `[CONFIRMED]`, except `[STRATEGY]` workers, which remain limited to docs/QA/packaging policy.

## Controller Scheduling Rule

After dispatching subagents, the controller must not wait idly if independent work remains. Continue splitting and dispatching non-blocking read-only verification, implementation, or review tasks whose file ownership and dependency layer are clear. Wait only when the next critical-path action is blocked by a needed subagent result, an unclear scope decision, a cross-package write overlap, or a dependency-layer ordering risk.

Implementation write locks are short-lived. They begin immediately before an agent performs an actual file write and end immediately after that write finishes. Read-only verification, browser runtime checks, visual review, grep/static checks, and handoff evidence synthesis do not hold write locks. If a second implementation agent sees an active or recent overlapping write owner for the same file/selector/function, the second agent must stop as `[BLOCKED: overlapping write ownership]` instead of trying to merge or overwrite.

After a subagent is dispatched, do not keep the same controller turn open only to poll or continuously wait for that subagent. Let the subagent return its report asynchronously, continue with already-visible non-blocking work, or return control so the client can deliver the next queued user comment. Check or wait on a subagent only when the next controller action truly needs that result, when a long silence suggests the subagent may be stuck or dead, or when closure/reassignment is needed. If checking after a long silence, request a concise status once and then either proceed from the returned report, close/reassign the task, or continue with other visible work; do not repeatedly busy-poll.

When one or more subagent completion reports arrive in the current controller context, the controller must prioritize closeout immediately. Read each report, close the finished agent, classify the result as PASS, FAIL, or BLOCKED, record or prepare the required handoff update, decide whether read-only review is needed, and continue dispatching the next non-conflicting confirmed task. Do not leave completed agents hanging until the user asks for status. This does not conflict with the no-busy-wait rule: the controller does not need to continuously wait for subagents, but once a completion notice is visible in the current context it must be processed before ordinary new work. If the report shows the same file still has a follow-up write conflict, record the blocking reason and do not dispatch overlapping writes; otherwise keep the workflow moving. Controller status updates and final responses must list active/running agents, completed-and-processed agents, and any blocked/completed-but-not-actionable reports so the user can see the real queue state.

## Controller Final Review Delegation Rule

Final review must be delegated to a read-only review subagent whenever subagents are available. Visual acceptance review must also be delegated to a read-only review subagent for UI-affecting work. The controller's review role is evidence synthesis: inspect the review report for obvious evidence gaps, incorrect scope, status mistakes, contradictions, owner-package errors, or missing real visual perception / multilingual checks. The controller should rerun verification directly only when the report is missing required proof, is internally inconsistent, conflicts with current evidence, is blocked, or the user explicitly asks.

## Multilingual Copy Sync Rule

All future visible-copy repair tasks must treat EN, ZH, and HI as one content surface unless the user's newest instruction explicitly says the task is single-language only.

- If a worker changes English copy, it must also update the corresponding Chinese and Hindi copy with the same business meaning.
- If a worker changes Chinese or Hindi copy, it must also check and update the matching English and remaining third-language copy where the same key/surface exists.
- This applies to static fallback HTML, `translations.en`, `translations.zh`, `translations.hi`, dynamic project/sample arrays, topology labels, workflow cards, hero proof cards, pricing/FAQ/footer/CTA copy, and resource/manual data where the same content appears in language-specific objects.
- Do not satisfy this rule by only patching `i18n-sweep.js`; the primary translation/source objects must carry complete EN/ZH/HI content.
- If the user intentionally assigns only one language for a narrow polish round, the prompt must state the exception, the handoff entry must say which languages were intentionally not changed, and review must not treat those deferred languages as silently complete.
- Read-only review must compare the changed keys/surfaces across EN/ZH/HI and report any stale, English-only, copied-through, empty, or mojibake-looking counterpart as a remaining `I18N-001` failure or explicitly deferred exception.
- Terminology-sensitive copy must also match `SAMPORA_TERMINOLOGY_GLOSSARY.md`. If a term is missing or conflicts with the newest user instruction, treat it as a terminology strategy gap before page repair.

## Failure-Prevention Checklist

**Before editing:**
- Confirm the change is in the latest audit or user's newest message.
- Map each change to one package and one file owner.
- Run a scoped red check before editing.
- Do not touch out-of-scope files.

**During implementation:**
- Do not trust subagent summaries alone; inspect changed files or scoped diffs.
- Do not mix write ownership between workers.
- For visible-copy edits, update the corresponding EN/ZH/HI primary translation/source entries together unless the newest user message explicitly grants a single-language exception.
- For any rendered UI change, plan and run visual acceptance separately from static and runtime geometry checks. Do not collapse visual PASS into "CSS matches", "no overflow", "screenshot exists", or "translations passed".
- Do not validate workflow/topology motion with screenshots only; use browser-time sampling and computed style checks.
- Do not update CTA links from memory; verify the intent URL policy with search.
- Do not change docs based on intended package contents; check actual files.

**Before handoff:**
- Rerun fresh scoped checks for touched files.
- Use a read-only review subagent for latest-audit review when subagents are available.
- Treat the controller's final-review work as a sanity-check of the read-only review report by default; rerun checks directly only for gaps, contradictions, current-evidence conflicts, or explicit user request.
- Run any needed controller-level final verification only after implementation and read-only review pass.
- Rebuild packages only after source verification.
- Inspect zip entry paths and repaired-file inclusion.

## Subagent Prompt Contract

Do not assume subagents automatically read global instructions. The controller must paste the relevant package, file ownership, failure-prevention rules, red checks, verification commands, and handoff-log requirement directly into every worker/reviewer prompt.

Implementation subagent prompts must require:
- Read this handoff and `ACCEPTANCE_TESTS.md` first.
- Restate assigned files and out-of-scope files.
- Map the work to acceptance categories.
- Restate the assigned issue statuses and allowed actions.
- State the validation tier: scoped, full regression for affected contract, or full five-layer.
- State a `Done when` checklist with concrete verification conditions before editing.
- Use a short write lock only for the actual file write: before writing, check latest handoff/current diff for overlapping active or recent ownership of the same file/selector/function; announce the exact file and scoped region; if overlap exists, stop as `[BLOCKED: overlapping write ownership]`; after the file write completes, release the lock and continue verification read-only.
- For visible-copy tasks, include a `Done when` item proving the matching EN/ZH/HI fallback, translation, and dynamic data entries were updated together, or state the explicit single-language exception from the user's newest message.
- For visual/UI tasks, include separate `Done when` items for `technical/static PASS`, `runtime geometry PASS`, and `visual acceptance PASS`. The visual acceptance item must name the user visual goals, target languages, desktop/mobile viewports, actual rendered values, before/after or current visual-difference notes, and PASS/FAIL against any specified threshold or look-and-feel target. If any one of the three PASS types is missing, report partial/incomplete instead of done.
- Run scoped red check before editing.
- Run scoped verification after editing.
- Append result to the handoff/log before final response.
- Record new pitfalls using symptom/root-cause/prevention/verification/owner format.

Read-only verification subagent prompts must require:
- Read this handoff and `ACCEPTANCE_TESTS.md` first.
- Restate the assigned issue subset and out-of-scope files.
- Make no edits and do not rebuild packages.
- Verify each item against current files, scripts, grep output, or browser state.
- Report item, exists now, evidence/command, recommended status, recommended owner package, and notes.
- Mark ambiguous or unmapped items `[BLOCKED]` instead of guessing.
- Mention matching `ISSUE_LEDGER.md` IDs when an assigned item resembles a recurring issue.

Review subagent prompts must require:
- Read-only mode.
- Latest-audit checklist as source of truth.
- Acceptance checklist coverage review.
- Direct public-package review only.
- Exact PASS/FAIL output with file references.
- Recommended owner package for any remaining failure.
- Ledger status changes required for recurring failures.
- For visible-copy changes, PASS/FAIL coverage for EN/ZH/HI counterparts, including static fallback, translation objects, and dynamic data objects touched by the same surface.
- For visual/UI changes, PASS/FAIL coverage must explicitly separate `technical/static PASS`, `runtime geometry PASS`, and `visual acceptance PASS`. Missing visual acceptance is a review failure even when CSS, overflow, screenshots, or multilingual copy checks pass.
- A report complete enough for controller sanity-check: commands/evidence run, PASS/FAIL for each assigned item, remaining gaps, out-of-scope items, and whether any controller rerun is actually needed.

## If Another Repair Round Starts

Use this package ownership map:

| Package | Writable Files |
|---|---|
| A Homepage | sampora-website-public-v3\index.html |
| B Solutions/Resources | sampora-website-public-v3\solutions.html, sampora-website-public-v3\resources.html |
| C Plans/Contact | sampora-website-public-v3\plans.html, sampora-website-public-v3\contact.html |
| D Resource Manuals | sampora-website-public-v3\resource-manuals.html, sampora-website-public-v3\assets\resource-manuals\data.js, sampora-website-public-v3\assets\resource-manuals\app.js |
| E Docs | README/deployment/QA/verification/redirect docs only |

## Latest Handoff Note / Handoff Update Log

Historical detailed update-log entries that previously made this active handoff noisy were archived to `SAMPORA_LATEST_REPAIR_LOG.md` under `2026-05-19 CST Archive - Active handoff historical update log before mojibake boundary cleanup`. Historical mojibake-looking samples in that log are preserved only as labeled history, not current evidence.

### Current Active Notes

- Source of truth remains `sampora-website-public-v3/` unless the user explicitly assigns packaging or a new versioned source.
- Do not package, rebuild zips, sync delivery folders, stage, commit, push, or create a new delivery version unless explicitly asked.
- Keep using the subagent workflow and read-only final-review delegation when subagents are available; controllers should not busy-poll asynchronous subagents, but must immediately close out any completion reports that have arrived in the current context.
- Visible-copy changes must stay synchronized across EN/ZH/HI primary source data unless the newest user message explicitly grants a single-language exception.
- Old handoff entries, old screenshots, old reports, and old QA JSON are never current proof. Rerun the mapped scoped check before claiming a repair.
- Package ownership remains the map above; page source, QA evidence outputs, delivery folders, and zips are out of scope for Package E docs-only strategy tasks unless explicitly assigned.
- GitHub pushes for cross-machine continuation must follow the Cross-Machine Git Push Policy above: include changed Class 1 must-push files and changed Class 2 JSON/script context, keep screenshot evidence separate unless needed, and avoid staging unrelated old package/zip/temp-folder churn.
- Terminology governance now lives in `SAMPORA_TERMINOLOGY_GLOSSARY.md`; future language workers must use it as the source of truth before changing role names, workflow terms, topology labels, Hindi SaaS terms, or Chinese equivalents.
- The required intake file index now lives in both `AGENTS.md` and this handoff. Controllers must use it during intake and paste the relevant subset into subagent prompts. The index includes current v3 package docs, product/backend positioning references, local design references, and historical repair/result reports with strict current-proof boundaries.
- `VISUAL-QA-001` records the strategy risk that visual acceptance was previously treated too loosely. Future visual/layout/component/interaction/style tasks must separate technical/static PASS, runtime geometry PASS, and visual acceptance PASS before final completion claims.
- Latest archived active entries before this cleanup included Package E async subagent wait rule sync, Package A FAQ layout balance, Package B Solutions card/fit-cycle repair, multilingual copy sync policy, and topology ledger evidence. Read the detailed log only when tracing those decisions.

### 2026-05-21 JST - Package B Solutions solution-card badge overflow

**Files changed:** `sampora-website-public-v3/solutions.html` scoped to `#solutionCards` `.plan-top` / `.badge` CSS, and `SAMPORA_LATEST_REPAIR_HANDOFF.md` for this closeout. No `#compare` table, topology, CTA, header/footer, other pages, QA scripts, delivery folders, zips, staging, commit, push, or packaging files were intentionally changed. `ISSUE_LEDGER.md` was intentionally left untouched per assignment.

**Verification run:** red check before edit in system Chrome/Playwright showed EN 1280x900 badge text overflowed the pill by `47.1px` and entered the third card by `4.1px`; EN 1200x900 overflowed the pill by `73.8px` and entered the third card by `30.8px`. Post-edit browser geometry covered EN/ZH/HI at `1907x1278`, `1440x900`, `1280x900`, `1200x900`, and `390x900`: every sample had badge border box inside the network card, text range inside the badge and `.plan-top`, no title/short overlap, document/body overflow `0`, and desktop third-card clearance at least `225.4px` for EN 1200 and `252.1px` for EN 1280. Static checks passed: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"`, `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"`, and `git diff --check -- sampora-website-public-v3/solutions.html SAMPORA_LATEST_REPAIR_HANDOFF.md`.

**Remaining failures:** none found in this scoped badge-overflow task. Visual acceptance: PASS; the badge now reads as a normal short pill in the top area of each solution card, no longer visibly exceeds its border or crosses into the third card.

**Next-agent notes:** keep this as a scoped Package B visual/layout repair. Do not treat it as permission to rework compare-table layout, topology behavior, CTA routing, packaging, or cross-page header/footer changes.

### 2026-05-20 CST - Package E controller scope narrowing

**Files changed:** `AGENTS.md`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, `ACCEPTANCE_TESTS.md`, and `ISSUE_LEDGER.md`. No HTML page source, JS/CSS assets, QA evidence outputs, delivery folders, zips, staging, commit, push, or packaging files were intentionally changed.

**Audit item addressed:** `[STRATEGY]` controller scope narrowing after the user clarified that even verification and review should be delegated when subagents are available. The must-obey wording now defines the controller as an orchestrator rather than a primary verifier/reviewer: the controller owns intake, status decisions, ownership boundaries, dispatch, conflict handling, evidence synthesis, and handoff records. First-pass verification goes to read-only verification subagents, page/source repair goes to implementation subagents, and visual/final review goes to read-only review subagents. The controller only runs minimal direct sanity checks when subagent evidence is missing, contradictory, blocked, or explicitly challenged.

**Verification run:** searched `AGENTS.md`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, `ACCEPTANCE_TESTS.md`, and `ISSUE_LEDGER.md` for controller-only, first-pass verification, read-only verification subagents, final review, visual acceptance review, evidence synthesis, and no-subagent exceptions. `git diff --check -- AGENTS.md SAMPORA_LATEST_REPAIR_HANDOFF.md ACCEPTANCE_TESTS.md ISSUE_LEDGER.md` passed.

**Remaining failures:** none for this docs/process governance update. This round did not evaluate page visuals or source behavior.

**Notes for next handoff:** new controllers should delegate verification and review by default, not just implementation. The controller should merge reports, decide statuses, catch contradictions, and maintain the handoff; direct controller checks are the exception path for missing/contradictory/blocked evidence or explicit user instruction.

### 2026-05-20 CST - Package E must-obey checklist audit

**Files changed:** `AGENTS.md`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, `ACCEPTANCE_TESTS.md`, and `ISSUE_LEDGER.md`. No HTML page source, JS/CSS assets, QA evidence outputs, delivery folders, zips, staging, commit, push, or packaging files were intentionally changed.

**Audit item addressed:** `[STRATEGY]` must-obey intake checklist hardening. A controller audit confirmed that the major rules were already present: controller-only operation, implementation by subagents, issue-status workflow, current-proof requirement, latest-audit scope, EN/ZH/HI synchronization, terminology glossary, strict visual acceptance, handoff logging, ledger updates, packaging deferral, and old-evidence rejection. Three items were not explicit enough in the entry rules: `AGENTS.md` did not state the real visual perception / 真实观感 requirement as directly as `ACCEPTANCE_TESTS.md`; implementation subagent prompts did not explicitly require `AGENTS.md`, `ISSUE_LEDGER.md`, and relevant on-demand indexed files; review subagent prompts did not explicitly name multilingual sync and real visual perception review. These are now promoted into the must-obey sections.

**Verification run:** searched `AGENTS.md`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, `ACCEPTANCE_TESTS.md`, and `ISSUE_LEDGER.md` for controller-only, implementation subagents, issue statuses, current proof, latest audit/out of scope, EN/ZH/HI, terminology glossary, visual acceptance / real visual perception, handoff log, ledger update, and packaging/zip rules. `git diff --check -- AGENTS.md SAMPORA_LATEST_REPAIR_HANDOFF.md ACCEPTANCE_TESTS.md ISSUE_LEDGER.md` passed.

**Remaining failures:** none for this docs/process governance update. This audit did not evaluate current page visuals or source behavior.

**Notes for next handoff:** new controllers should treat these as red lines in the first intake pass. Subagent prompts must carry the exact relevant files and rules, not merely say "follow the handoff"; review must reject missing EN/ZH/HI sync or missing real visual perception acceptance as incomplete.

### 2026-05-20 CST - Package E controller-only and visual governance surfacing

**Files changed:** `AGENTS.md`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, `ACCEPTANCE_TESTS.md`, and `ISSUE_LEDGER.md`. No HTML page source, JS/CSS assets, QA evidence outputs, delivery folders, zips, staging, commit, push, or packaging files were intentionally changed.

**Audit item addressed:** `[STRATEGY]` controller intake governance after the user asked whether new-agent handoff docs make the essential rules unavoidable: controller-only operation, implementation by subagents, strict real visual perception acceptance, and EN/ZH/HI synchronization. Existing rules already covered these areas, but the controller-only/subagent implementation rule was not prominent enough in the top intake sections. The rule is now surfaced in the `READ FIRST` / non-negotiable sections and in `AGENTS.md`; `ACCEPTANCE_TESTS.md` now states controller-only operation at the top of the use instructions.

**Verification run:** searched `AGENTS.md`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, and `ACCEPTANCE_TESTS.md` for controller/main-agent, subagent, visual acceptance / real visual perception, and EN/ZH/HI synchronization rules. Confirmed existing visual and multilingual requirements were already present, then added the missing prominence for controller-only implementation routing. `git diff --check -- AGENTS.md SAMPORA_LATEST_REPAIR_HANDOFF.md ACCEPTANCE_TESTS.md ISSUE_LEDGER.md` passed.

**Remaining failures:** none for this docs/process governance update. This round did not evaluate page visuals or source behavior because no page repair was requested.

**Notes for next handoff:** a new agent should now see in the first intake pass that it is the controller by default. Page/source implementation goes to implementation subagents with disjoint ownership; visual/UI tasks require real rendered visual judgment; copy/structure changes require EN/ZH/HI synchronization unless explicitly scoped otherwise.

### 2026-05-20 CST - Package E intake navigation completeness

**Files changed:** `AGENTS.md`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, and `ISSUE_LEDGER.md`. No HTML page source, JS/CSS assets, QA evidence outputs, delivery folders, zips, staging, commit, push, or packaging files were intentionally changed.

**Audit item addressed:** `[STRATEGY]` controller intake/navigation completeness after the user asked whether the handoff files had been read fully and whether supporting documents were missing from the handoff navigation. The review found `sampora-website-public-v3/MODIFICATION_SUMMARY.md` was not listed with the active v3 package docs, and the root product/backend/design references were only implicitly covered by broad reference-folder wording. The intake index now explicitly names the current v3 package docs, product positioning/taxonomy/backend references, local operations-console design references, and historical repair/result reports, while keeping all of them behind current-proof verification rules.

**Verification run:** read/checked `AGENTS.md`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, `ACCEPTANCE_TESTS.md`, `ISSUE_LEDGER.md`, root Markdown document names, `sampora-website-public-v3` Markdown document names, `sampora-website-public-v3/MODIFICATION_SUMMARY.md`, root historical result report headers, product positioning/taxonomy/backend reference files under `docs/surveysaas/`, and Markdown files under `补充资料归档/`. `git diff --check -- AGENTS.md SAMPORA_LATEST_REPAIR_HANDOFF.md ISSUE_LEDGER.md` passed.

**Remaining failures:** none for this docs/process strategy update. Current browser/page behavior was not evaluated in this round because the task was intake navigation completeness only. Future visual acceptance remains mandatory for UI-affecting work and must use real rendered evidence, not these reference documents as proof.

**Notes for next handoff:** controllers should not stop at the main three files when a task touches product positioning, backend capability claims, role taxonomy, design direction, package docs, or old repair results. Read the relevant indexed references, then verify the current v3 source/browser state before classifying or repairing.

### 2026-05-20 CST - Package E visual acceptance strategy closeout

**Files changed:** `SAMPORA_LATEST_REPAIR_HANDOFF.md`, `ACCEPTANCE_TESTS.md`, and `ISSUE_LEDGER.md`. `AGENTS.md` was checked and already contained the visual acceptance and subagent-report separation rules, so it was intentionally not modified. No HTML page source, QA evidence outputs, delivery folders, zips, glossary, staging, commit, push, or packaging files were intentionally changed.

**Audit item addressed:** `[STRATEGY]` `VISUAL-QA-001` visual acceptance governance. Future visual/layout/component/interaction/style/spacing/sizing/hierarchy/card-structure/alignment/responsive/motion work must not claim final completion from static CSS, no-overflow, screenshot existence, or language-sync checks alone. Workers and reviewers must separate `technical/static PASS`, `runtime geometry PASS`, and `visual acceptance PASS`; any missing PASS is partial/incomplete.

**Verification run:** `git diff --check -- AGENTS.md SAMPORA_LATEST_REPAIR_HANDOFF.md ACCEPTANCE_TESTS.md ISSUE_LEDGER.md` passed. Search confirmed the four documents expose either `VISUAL-QA-001`, `visual acceptance`, or `视觉验收`, and `ISSUE_LEDGER.md` contains `VISUAL-QA-001`.

**Remaining failures:** none for this docs-only strategy update. Page behavior was intentionally untouched.

**New pitfall:**
- **Pitfall / symptom:** A Resources `.resource-entry` visual repair was reported PASS even though the user still saw little meaningful visual difference and user targets such as card feel, around-150px sizing, and unsqueezed titles were not strictly proven.
- **Root cause:** The worker treated static CSS properties, no-overflow checks, and language/source checks as enough proof, without separating technical/static PASS, runtime geometry PASS, and rendered visual acceptance PASS.
- **Prevention rule:** Every visual/UI task must map each user visual goal to rendered EN/ZH/HI desktop/mobile evidence and report technical/static, runtime geometry, and visual acceptance results separately. Missing or failed visual acceptance means partial/incomplete, not done.
- **Verification:** Review the handoff, acceptance contract, and ledger for `VISUAL-QA-001` / visual acceptance rules; for future UI work, inspect the worker report for actual rendered heights, spacing, boundaries, overlap, line count, alignment deltas, and before/after or current visual-difference notes.
- **Owner package:** Controller and the package worker/reviewer for the touched UI surface.

### 2026-05-20 CST - Package E pricing/version terminology update

**Files changed:** `SAMPORA_TERMINOLOGY_GLOSSARY.md`, `ISSUE_LEDGER.md`, and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No page source HTML, CSS/JS assets, QA evidence outputs, delivery folders, zips, staging, commit, push, or packaging files were intentionally changed.

**Audit item addressed:** `[STRATEGY]` user-confirmed homepage/pricing version-name terminology. The glossary now records the Chinese plan/version names: `Panel` -> `样本库运营版`, `Supplier Network` -> `供应商网络运营版`, and `Enterprise` -> `企业版`. `ISSUE_LEDGER.md` keeps this under `TERMINOLOGY-001` as a docs/i18n strategy guard for later Package A page-copy repair.

**Verification run:** scoped docs checks confirmed the three new Chinese version names appear in `SAMPORA_TERMINOLOGY_GLOSSARY.md` and `ISSUE_LEDGER.md`; `node "sampora-website-public-v3\qa-evidence\active-handoff-mojibake-guard.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS.

**Remaining failures:** the homepage `index.html` pricing/version card runtime copy and other homepage language issues remain unrepaired in this controller/docs-only strategy step. They require a separate Package A implementation worker using the glossary mappings and fresh scoped EN/ZH/HI browser/static verification.

**Notes for next handoff:** do not use generic role descriptions as pricing/version card names. When repairing homepage pricing cards, Chinese visible plan names must be `样本库运营版`, `供应商网络运营版`, and `企业版`; keep English plan names as `Panel`, `Supplier Network`, and `Enterprise` unless the user explicitly changes English product naming.

### 2026-05-20 JST - Package B Solutions compare table column balance

**Files changed:** `sampora-website-public-v3/solutions.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No `resources.html`, `index.html`, other pages, visible copy/i18n, CTA/footer/legal, topology behavior, QA evidence outputs, delivery folders, zips, README files, staging, commit, push, or packaging files were intentionally changed. `ISSUE_LEDGER.md` was intentionally left unchanged because this is not a recurring ledger issue.

**Audit item addressed:** `[CONFIRMED]` Package B `solutions.html` visual layout issue from the newest user message: the `#compare` decision matrix outer `.compare-wrap` was centered, but the three business columns inherited left alignment from global table cells, leaving text tight to the left and empty on the right. The repair adds a narrow `#compareTable th:not(:first-child), #compareTable td:not(:first-child) { text-align: center }` override while preserving the first decision-point column as left aligned.

**Verification run:** red Playwright geometry before editing showed `.compare-wrap` centered with no document overflow, but non-first first-row cells computed `text-align:left`; at 1280px business-column imbalances were `138.91px`, `53.03px`, and `106.02px`, and at 1440px they were `171.33px`, `85.45px`, and `138.45px`. Post-repair Playwright geometry passed at 1280px and 1440px: business columns computed `text-align:center` with left/right text-gap imbalance `0-0.02px`, header aligns were `left/center/center/center`, first column remained `left`, `.compare-wrap` remained centered, and document overflow stayed `0`. Mobile 390px kept document overflow `0`, table width `960px`, and `.compare-wrap` horizontal scroll overflow `604px`. Static guards passed: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `git diff --check` -> exit 0 with CRLF warnings only.

**Remaining failures:** none found in this scoped Package B decision-matrix layout task. Existing dirty workspace changes outside this assigned scope were not reviewed, reverted, staged, committed, packaged, or synced.

**Notes for next handoff:** future `solutions.html` compare-table visual checks should measure internal text gaps and computed `text-align`, not only outer container centering. Keep the first column left aligned for row-label scanability unless a future audit explicitly assigns that behavior.

**Follow-up note:** verifier follow-up confirmed the user's "leave space on both sides" request also applied to the desktop/tablet `.compare-wrap` width, not only internal cell alignment. Files changed in the follow-up: `sampora-website-public-v3/solutions.html` and this handoff only; `ISSUE_LEDGER.md` stayed unchanged because this remains a one-off visual layout issue. Red Playwright check before the follow-up showed 1280px and 1440px `.compare-wrap` container inner margins were `0/0` while mobile 390px had document overflow `0`, table width `960px`, and wrap scroll overflow `604px`. The follow-up added `#compare .compare-wrap { max-width: 1120px; margin-inline: auto }`, preserving `#compareTable` min-width `960px` and non-first-column center alignment. Green Playwright check passed: 1280px viewport margins `80/80`, container inner margins `56/56`; 1440px viewport margins `160/160`, container inner margins `120/120`; 390px document overflow `0`, wrap `356` client / `960` scroll / `604` overflow; first column remained `left`, business columns and headers remained `center`. Static checks passed: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `git diff --check` -> exit 0 with CRLF warnings only. Remaining failures: none in this scoped follow-up.

### 2026-05-20 JST - Package B Solutions hero seam visual fix

**Files changed:** `sampora-website-public-v3/solutions.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No copy/i18n labels, CTA/footer/legal/routes, topology behavior, assets, QA evidence outputs, delivery folders, zips, screenshots, or other pages were intentionally changed. `ISSUE_LEDGER.md` was intentionally left untouched because this is not a recurring ledger issue.

**Audit item addressed:** `[CONFIRMED]` Package B `solutions.html` visual issue from the newest user message: the global `.section { border-top: 1px solid var(--line) }` created a horizontal separator between the hero and the first `01 / Solution layers` section. Added a narrow `.hero + .section { border-top: 0 }` override only for the hero-adjacent first section, leaving the global section border and other separators intact.

**Verification run:** required red check failed before editing with `FAIL: first solutions section inherits global section border-top after hero`. Post-repair red check passed with `PASS: first solutions section border override present or no inherited border`. Source grep confirmed the global `.section` border remains and the `.hero + .section` override exists. Static guards passed: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS and `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS.

**Remaining failures:** none found in this scoped visual fix. Browser screenshot verification, Lighthouse/axe, QA evidence refresh, delivery sync, zip rebuild, and packaging were intentionally not run.

**Notes for next handoff:** if future hero-to-section seam work is requested, keep it scoped to the affected page and first section unless a fresh audit explicitly assigns broader section-border behavior.

### 2026-05-20 JST - Package B topology performance smoothing

**Files changed:** `sampora-website-public-v3/solutions.html`, `sampora-website-public-v3/resources.html`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, and `ISSUE_LEDGER.md` for scoped `TOPOLOGY-001` evidence. No `index.html`, `plans.html`, `contact.html`, `resource-manuals.html`, assets, QA evidence outputs, delivery folders, zips, redirect docs, visible copy/i18n labels, CTA/footer/legal behavior, or package artifacts were intentionally changed.

**Audit items addressed:** `[CONFIRMED]` Package B topology performance smoothing for `solutions.html` and `resources.html`. The repair followed the previous homepage smoothing pattern: inactive base topology flows no longer run `topoDash`; active `.flow.on` and `.flow-group.on .flow` still animate; moving packet `animateMotion` primitives remain; moving flow/packet drop-shadow filters were removed; and Resources active flows no longer run the extra `flowBreathe` / `flowAmberBreathe` animations. Solutions highlight-only `.topo-scan` remains intentionally disabled; Resources required topology scan/business motion was not disabled.

**Verification run:** red browser sampling at 1440x900 confirmed current cost patterns while business motion worked. Before repair, `solutions.html` had 13 animated flows, 10 animated inactive flows, 31 topology computed filters, 13 `animateMotion` packets, active `topoDash`, dash offset change, packet movement, route cycle `network -> enterprise -> panel -> network`, close text `X`, and modal body children `1 -> 0 -> 1` across open/close/reopen. Before repair, `resources.html` had 9 animated flows, 6 animated inactive flows, 20 topology computed filters, 9 `animateMotion` packets, `flowBreathe`/`flowAmberBreathe` on all flows, active dash offset change, packet movement, setup/supplier/finance modes working, close text `X`, and close inside `.window-bar`. Local rAF in this worker did not reproduce the controller's stable 33ms median, measuring about 17-18ms p50/p95 before repair; the static cost pattern and controller conflict evidence drove the scoped repair. Post-repair browser sampling passed: Solutions now has 3 animated flows, 0 animated inactive flows, 5 topology computed filters, 13 `animateMotion` packets, active `topoDash`, dash offset `-26.2453px -> -43.5393px`, packet center movement, route cycle preserved, close `X`, and controlled modal clone count. Resources now has 3 animated flows, 0 animated inactive flows, 2 topology computed filters, 9 `animateMotion` packets, 0 `flowBreathe` animations, active `topoDash`, dash offset `-19.2147px -> -35.8873px`, packet center movement, setup/supplier/finance modes preserved, and close remains in `.window-bar`. Post-repair local rAF remained about 17-18ms p50/p95. Static guards passed: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS and `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS.

**Remaining failures:** none found in this scoped topology smoothing pass. Page-level `bgDrift` / background animations remain `[VERIFY-FIRST]` and out of scope for this worker. Full screenshot browser suite, Lighthouse/axe, QA evidence refresh, delivery sync, zip rebuild, and packaging were intentionally not run.

**Notes for next handoff:** keep Package B topology smoothing scoped to active business motion. Future topology edits should verify animated inactive flow count, computed filter count, active dash offset changes, packet movement, route/mode behavior, and modal close/placement before and after changes. Do not re-enable Solutions highlight-only scan/sheens without explicit user request, and do not pursue page-level background animation changes without a new scoped authorization.

### 2026-05-20 JST - Package B Solutions topology layout stability

**Files changed:** `sampora-website-public-v3/solutions.html`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, and `ISSUE_LEDGER.md` for scoped `TOPOLOGY-001` evidence. No visible copy, translations, other pages, QA evidence outputs, delivery folders, zips, README files, staging, commit, push, or packaging files were intentionally changed.

**Audit item addressed:** `[CONFIRMED]` Package B `solutions.html` topology/solution-layer mode layout stability. Current browser red check at 1280x932 showed EN and HI mode switching changed `#topoText` height by `22.11px` and moved hero/grid/console/next section by `22.11px`; SVG stage and modebar heights stayed stable. The repair reserves stable height for topology summary text, tags, and narrow-screen header/ticker wrapping, while preserving mode-specific active flows and packet motion.

**Verification run:** post-repair Chromium geometry checks passed for EN/ZH/HI across `network`, `panel`, and `enterprise`: 1280x932 and 1440x900 desktop had `0px` delta for hero/grid/window/main/summary/next-section/text/tags/header/ticker/modebar; 390x900 mobile smoke stayed within `0-1.34px` next-section tolerance with stable console/window heights. Active mode paths still changed to `path-network`, `path-panel`, and `path-enterprise`; active flow/packet counts remained `3/3/7`. Motion sampling passed with enterprise `topoDash` offset changing `0px -> -12.3347px` and packet center moving. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `git diff --check` -> exit 0 with CRLF warnings only.

**Remaining failures:** none found in this scoped Package B topology layout stability task. Existing dirty workspace changes outside this assigned scope were not reviewed, reverted, staged, committed, packaged, or synced.

**Notes for next handoff:** future `solutions.html` topology edits should measure geometry by language and mode, including `#topoText`, `.console-tags`, `.window-bar`, `.console-ticker`, `.console-window`, `.hero-grid`, and `.hero + .section`, and should continue verifying active path classes plus dash/packet motion.

### 2026-05-20 JST - Package E required intake file index

**Files changed:** `AGENTS.md`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, `ACCEPTANCE_TESTS.md`, and `ISSUE_LEDGER.md`. No HTML page source, JS/CSS assets, QA evidence outputs, delivery folders, zips, staging, commit, push, or packaging files were intentionally changed.

**Audit item addressed:** `[STRATEGY]` controller handoff governance. The mandatory entry files now include a required intake file index so controllers and subagents can see supporting handoff material beyond the main handoff/acceptance/ledger trio. The index covers always-read files, relevant-on-demand files, public-package docs, detailed history, `archives/`, `动态/`, and local reference/demo folders, while preserving the rule that reference/history files are not current proof.

**Verification run:** scoped docs/process checks passed after this edit: `Select-String` in `AGENTS.md` and this handoff found `Sampora intake file index` / `Required Intake File Index`, `SAMPORA_TERMINOLOGY_GLOSSARY.md`, `redirect-map.md`, `archives/`, `动态/`, and the subagent prompt instruction; `Select-String` in `ACCEPTANCE_TESTS.md` found `required file index`; `Select-String` in `ISSUE_LEDGER.md` found `HANDOFF-003`.

**Remaining failures:** none known for this docs/process strategy task. Existing page source and QA artifacts were intentionally left untouched.

**Notes for next handoff:** during intake, do not stop at the main three docs. Use the index to decide which supporting docs are relevant, then paste those exact file requirements into any subagent prompt. Archive/reference files may guide checks but cannot prove current page behavior.

- **Pitfall / symptom:** a controller read only the main handoff/acceptance/ledger files and missed supporting handoff files that were already present locally.
- **Root cause:** the mandatory entry files did not expose a clear, compact index of all supporting intake files and relevant-on-demand references.
- **Prevention rule:** use the required intake file index in `AGENTS.md` and `SAMPORA_LATEST_REPAIR_HANDOFF.md` before classifying work, and include the relevant indexed files in every subagent prompt.
- **Verification:** `Select-String` checks find the index headings and supporting file names in `AGENTS.md`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, `ACCEPTANCE_TESTS.md`, and `ISSUE_LEDGER.md`.
- **Owner package:** Package E handoff policy / controller intake.

### 2026-05-19 CST - Package E controller subagent completion closeout rule

**Files changed:** `SAMPORA_LATEST_REPAIR_HANDOFF.md`, `ACCEPTANCE_TESTS.md`, and `ISSUE_LEDGER.md`. No HTML, JS, CSS, QA evidence outputs, delivery folders, zips, README, staging, commit, push, packaging, or Package A `index.html` files were intentionally changed.

**Audit item addressed:** `[STRATEGY]` controller workflow governance. The controller scheduling rule now explicitly says that when one or more subagent completion reports arrive in the current context, the controller must immediately read reports, close finished agents, classify PASS/FAIL/BLOCKED, record or prepare handoff updates, decide whether review is needed, and keep dispatching the next non-conflicting confirmed work. Completed agents must not be left hanging until the user asks for status. The rule also clarifies that this does not conflict with no busy-wait/no polling: no continuous wait is required, but visible completion notices take priority once they arrive.

**Verification run:** scoped docs/strategy checks passed. `Select-String` in this handoff found `completion reports arrive`, `completed agents hanging`, `completed-and-processed agents`, `same file still has a follow-up write conflict`, and the new section heading. `Select-String` in `ACCEPTANCE_TESTS.md` found `completion reports arrive`, `PASS/FAIL/BLOCKED`, `completed-processed agent state`, and `completion-closeout rule`. `Select-String` in `ISSUE_LEDGER.md` found `HANDOFF-002`, `completion-closeout`, `completed subagent reports hanging`, `PASS/FAIL/BLOCKED`, and `no-busy-wait`. `node "sampora-website-public-v3\qa-evidence\active-handoff-mojibake-guard.mjs"` -> PASS.

**Remaining failures:** none known for this docs/process rule. Page source and Package A worker-owned `index.html` were intentionally left untouched.

**Notes for next handoff:** future controllers should report active/running/completed-processed agents in status updates and final responses. If a completed report reveals a same-file follow-up write conflict, record the blocking reason; otherwise continue with the next non-conflicting confirmed task without waiting for a user nudge.

### 2026-05-19 CST - Package E terminology glossary and handoff index

**Files changed:** `SAMPORA_TERMINOLOGY_GLOSSARY.md`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, `ACCEPTANCE_TESTS.md`, and `ISSUE_LEDGER.md`. No HTML, JS, CSS, QA evidence, delivery folders, zips, README, staging, commit, or packaging files were intentionally changed.

**Audit item addressed:** `[STRATEGY]` terminology governance for future visible-copy and i18n work. A standalone glossary now records canonical EN/ZH/HI terms, including user-confirmed `Panel Provider` -> `自有样本运营方`, `Workspace` -> `工作台`, topology-node policy, and Devanagari-first Hindi style with limited retained SaaS/industry terms.

**Verification run:** scoped docs/strategy checks confirmed the glossary exists and contains the required core terms, the active handoff indexes the glossary, `ACCEPTANCE_TESTS.md` maps glossary compliance under `Language/i18n`, and `ISSUE_LEDGER.md` records `TERMINOLOGY-001`. UTF-8 required-term coverage check found all 51 tracked terms, including `Panel Provider`, `自有样本运营方`, `Workspace`, `工作台`, `SAMPLE SUPPLY POOL`, and Hindi canonical role/workspace strings. Glossary bad-marker check found no repeated-question-mark placeholder marker or replacement character. `node "sampora-website-public-v3\qa-evidence\active-handoff-mojibake-guard.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS.

**Remaining failures:** none found in this scoped docs/strategy task. This worker did not implement page translation repairs or reclassify any page-source item.

**Notes for next handoff:** future language agents must read `SAMPORA_TERMINOLOGY_GLOSSARY.md` before page copy changes. If a visible term is absent from the glossary, add or confirm the terminology strategy before modifying HTML/source copy.

### 2026-05-19 CST - Package B Solutions decision matrix column balance

**Files changed:** `sampora-website-public-v3/solutions.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No compare text/data, CTA, footer, hero, topology, mini-metrics, workflow-tab logic, resources/plans/contact files, QA evidence outputs, delivery folders, zips, packaging, staging, commit, or push files were intentionally changed. `ISSUE_LEDGER.md` was intentionally left untouched because no recurring issue status changed.

**Audit item addressed:** `[CONFIRMED]` Package B `section#compare` decision matrix layout. Source red check found `#compareTable` inherited global `table { width:100%; min-width:860px }` and later global `@media(max-width:1100px) table { min-width:940px }`, with no scoped `table-layout` or column allocation. Browser red check at 1681px ZH showed auto-layout columns about `290 / 317 / 360 / 390px`, so the solution columns widened unevenly toward the right. The table now has scoped `#compareTable` fixed layout, `min-width:960px`, first column at `24%`, and the three solution columns equally split at `25.333%`; cell wrapping is scoped to `#compareTable`.

**Verification run:** static CSS check confirmed `#compareTable` has fixed layout, scoped min-width, explicit first/remaining column widths, scoped wrapping rules, the existing mobile `.compare-wrap` overflow guard remains, and the previous mini-metrics/workflow strings remain present. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. Inline script syntax check for 3 scripts -> OK. `node "sampora-website-public-v3\qa-evidence\active-handoff-mojibake-guard.mjs"` -> PASS. Browser DOM check with Chrome -> ZH 1681px, ZH 1440px, and EN 1440px all had `tableLayout=fixed`, `minWidth=960px`, no document horizontal overflow, table using the compare wrap width, right blank about `1px`, first column `24%`, solution width spread about `0.05px`, and column widths about `326 / 344 / 344 / 344px`. ZH 390px had document overflow `0` with internal `.compare-wrap` scroll overflow `604px`, preserving mobile table scrolling.

**Remaining failures:** none found in this scoped Package B `#compare` layout task. Existing dirty `ISSUE_LEDGER.md` state was not touched by this worker.

**Notes for next handoff:** keep compare table layout scoped to `#compareTable`; do not change global `table` rules for this issue. If future decision-matrix work changes copy or data, apply visible-copy EN/ZH/HI sync rules and recheck desktop column balance plus mobile internal scrolling.

### 2026-05-19 CST - Package B Solutions mini-metric phrase wrap返工

**Files changed:** `sampora-website-public-v3/solutions.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No CTA, footer, topology, workflow-tab timer/lock logic, compare table, ticker, other pages, QA evidence outputs, delivery folders, zips, packaging, staging, commit, or push files were intentionally changed. `ISSUE_LEDGER.md` was intentionally left untouched because no recurring issue status changed.

**Audit item addressed:** `[CONFIRMED]` Package B `section#fit` Panel recommendation `#r2` phrase-wrap failure where `survey participation` could split across visual lines. Desktop `.mini-metrics strong` no longer uses `overflow-wrap:anywhere`; each rendered metric `span` uses `white-space:nowrap` so short intended phrases stay intact. The mobile overflow guard was extended to `.mini-metrics strong span` and resets span `white-space:normal` below 680px so narrow screens still have a safe wrap path.

**Verification run:** red source check before repair confirmed Panel EN r2 was `→ Member operations\n→ survey participation`, desktop `.mini-metrics strong` used `overflow-wrap:anywhere`, and spans had no nowrap guard. Post-repair static CSS check confirmed desktop strong uses `overflow-wrap:normal`, spans use `white-space:nowrap`, mobile guard includes metric spans and restores `white-space:normal`, the previous no-`margin-top:auto` middle-gap fix remains, and workflow auto-cycle/click-lock strings remain present. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. Inline script syntax check for 3 scripts -> OK. Browser DOM check with Chrome at 1440px -> EN Panel `#r2` spans `→ Member operations` and `→ survey participation` each stayed `white-space:nowrap`, one visual line with 17.39px line height, and `overflow=0`; EN Panel page `overflowX=0`, label top spread `0`, description-to-metrics gap `26px`. EN Enterprise, ZH Network, and HI Enterprise samples also had stacked spans, label top spread `0`, value areas inside cards, `strongOverflow=0`, span overflow `0`, page `overflowX=0`, and preserved no-middle-blank gap.

**Remaining failures:** none found in this scoped返工. Existing workflow-tab auto-cycle/click-lock and EN/ZH/HI rec data formatting were preserved.

**Notes for next handoff:** desktop mini-metric lines should preserve intended phrases as atomic spans; do not reintroduce `overflow-wrap:anywhere` on `.mini-metrics strong` without a desktop phrase-wrap browser check. Keep the mobile span override so narrow screens can still wrap safely.

### 2026-05-19 CST - Package B Solutions recommendation middle-gap返工

**Files changed:** `sampora-website-public-v3/solutions.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No CTA, footer, topology, workflow-tab timer/lock logic, compare table, ticker, other pages, QA evidence outputs, delivery folders, zips, packaging, staging, commit, or push files were intentionally changed. `ISSUE_LEDGER.md` was intentionally left untouched because no recurring issue status changed.

**Audit item addressed:** `[CONFIRMED]` Package B `section#fit` recommendation large-card rhythm返工 after user reported a large empty middle band in Hindi Enterprise. `.mini-metrics` no longer uses `margin-top:auto`; it now uses a controlled `clamp(18px, 2.4vw, 26px)` gap after the description with no padding-top push. `.recommend` keeps a coherent centered stack rhythm so title/description/metrics read as one vertical group instead of top text plus an empty middle plus bottom-anchored metrics. The mini cards still preserve the previous label-top/value-area grid and line-span value rendering.

**Verification run:** red source check before repair confirmed `.mini-metrics` still had `margin-top:auto; padding-top:20px`, while the internal mini cards already used the label/value grid. Post-repair static CSS check confirmed `.mini-metrics` has no `margin-top:auto`, has the controlled clamp margin and `padding-top:0`, the mini-card `grid-template-rows: auto minmax(66px, 1fr)` remains, and workflow auto-cycle/click-lock strings remain present. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. Inline script syntax check for 3 scripts -> OK. Browser DOM check with Chrome at 1440px -> Hindi Enterprise `descriptionToMetricsGap=26px`, `metricsBottomGap=60.64px`, `labelTopSpread=0`, `overflowX=0`, all values inside cards, mini-card bottom gaps about `7px`, and r2 first line `→ प्राप्ति`; ZH Enterprise and ZH Network also had `descriptionToMetricsGap=26px`, `labelTopSpread=0`, `overflowX=0`, all values inside cards, and r2 first lines `→ 接收` / `→ 项目接收`.

**Remaining failures:** none found in this scoped返工. Existing workflow-tab auto-cycle/click-lock and EN/ZH/HI rec data formatting were preserved.

**Notes for next handoff:** do not reintroduce `margin-top:auto` on `.mini-metrics`; it recreates the middle void in stretched recommendation cards. Future checks should sample Hindi Enterprise plus one ZH state for description-to-metrics gap, label top alignment, value containment, and page overflow.

### 2026-05-19 CST - Package A contact final CTA follow-up rhythm fix

**Files changed:** `sampora-website-public-v3/index.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No visible copy, translations, dynamic projects, CTA hrefs, other pages, QA evidence outputs, delivery folders, zips, README, staging, commit, or packaging files were intentionally changed.

**Audit item addressed:** `[CONFIRMED]` Package A `#contact` final CTA layout regression from the previous attempt. The previous `#contact .final-steps { margin-top: auto; padding-top: 30px; }` pushed the three steps and buttons toward the bottom of the left `.final-cta` card, creating the user-reported large middle void. This follow-up removes that auto push and replaces it with a continuous rhythm: body-to-steps gap, step row gap, and steps-to-buttons gap now stay close and natural. The right FAQ panel keeps the existing equal-row distribution and long-text wrapping approach.

**Verification run:** scoped source red check before repair identified `#contact .final-steps` using `margin-top: auto`. Post-repair source check confirmed `#contact .final-steps` now uses `gap: 12px`, `margin-top: 26px`, and `padding-top: 0`; step rows use `min-height: 34px`; mobile keeps `margin-top: 24px`, `gap: 8px`, and `padding-top: 0`. Chromium geometry check passed at 1680x1270 for EN/ZH/HI: left `.final-cta` height `454px`, body-to-steps `26px`, steps-to-buttons `26px`, step heights `34/34/34`, step gaps `12/12`, FAQ heights `113/113/113`, FAQ gaps `12/12`, FAQ bottom gap `0`, horizontal overflow `0`, and contact CTA hrefs stayed `contact.html?intent=start_trial#contact-form` and `contact.html?intent=book_demo#contact-form`. Chromium geometry check passed at 390x900 for EN/ZH/HI: body-to-steps `24px`, steps-to-buttons `22px`, step heights `34/34/34`, step gaps `8/8`, FAQ items stayed equal height per language (`147/147/147` EN, `125.3/125.3/125.3` ZH, `152.8/152.8/152.8` HI), FAQ gaps `12/12`, FAQ bottom gap `0`, horizontal overflow `0`, and the same CTA hrefs were preserved. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS.

**Remaining failures:** none found in this scoped Package A `#contact` final CTA / FAQ layout follow-up. `I18N-001`, `CTA-001`, and `STICKY-001` remain regression guards only; no ledger status changed, so `ISSUE_LEDGER.md` was intentionally left untouched.

**Notes for next handoff:** do not reintroduce `margin-top: auto` or `justify-content: space-between` on the left final CTA content stack. If future work changes this section, verify final CTA group gaps and FAQ equal-row behavior separately after reveal animations have settled.

### 2026-05-19 CST - Package B Solutions mini-metrics rhythm rework

**Files changed:** `sampora-website-public-v3/solutions.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No CTA, footer, topology, compare table, ticker, resource/plans/contact/index/resource-manuals files, QA evidence outputs, delivery folders, zips, packaging, staging, commit, or push files were intentionally changed. `ISSUE_LEDGER.md` was intentionally left untouched because no recurring issue status changed.

**Audit item addressed:** `[CONFIRMED]` Package B `section#fit` recommendation mini-metrics rhythm返工. The mini metric cards no longer center the entire card content as one flex column. Each mini card now uses a two-row grid so the label (`Primary asset` / `Main workflow` / `Finance view`, and ZH/HI equivalents) stays aligned at the top while the value area uses the remaining space. Metric values are rendered as safe `span.textContent` lines inside the `strong` element, allowing compact even distribution without unsafe HTML. The `Main workflow` metric format is now consistent across EN/ZH/HI and across panel/network/enterprise rec entries: every workflow line begins with `→`, including `→ Project intake`, `→ 项目接收`, `→ Intake`, and `→ 接收`.

**Verification run:** red source check before repair confirmed `.mini-metrics div` used whole-card `justify-content:center`, Enterprise r2 first lines lacked leading arrows, and Network r2 remained one-line in EN/ZH/HI. Post-repair static source checks confirmed the old one-line Network r2 and old no-leading-arrow Enterprise r2 strings are absent, new leading-arrow r2 strings are present for panel/network/enterprise EN/ZH/HI, `.mini-metrics div` uses `grid-template-rows: auto minmax(66px, 1fr)`, the scoped mini card block no longer has `justify-content:center`, and `setMetricValue()` renders line spans with `span.textContent`. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. Inline script syntax check for 3 scripts -> OK. Browser DOM check with Chrome at 1440px -> EN Enterprise, ZH Enterprise, and ZH Network all had label top spread `0px`, page `overflowX=0`, all value areas inside their cards, strong/card horizontal overflow `0`, card bottom gap about `7px`, recommendation bottom gap `29px`, mini cards using CSS grid, and r2 visual lines beginning with `→`; a character-code check confirmed ZH Network r2 line starts are U+2192.

**Remaining failures:** none found in this scoped Package B返工. Previously completed workflow-tab auto-cycle/click-lock remained untouched.

**Notes for next handoff:** keep `sampora-website-public-v3/` as active source. Future mini-metric work should preserve the label-top/value-area grid structure and verify label-top spread, value containment, and r2 leading-arrow format in at least Enterprise plus any edited state.

### 2026-05-19 CST - Package B Solutions recommendation metrics and workflow tabs

**Files changed:** `sampora-website-public-v3/solutions.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No resources/plans/contact/index/resource-manuals files, QA evidence outputs, delivery folders, zips, packaging, staging, commit, or push files were intentionally changed. `ISSUE_LEDGER.md` was not updated because `I18N-001` and `TOPOLOGY-001` remain regression guards with no material status change.

**Audit items addressed:** `[CONFIRMED]` Package B `section#fit` Enterprise recommendation `Primary Asset` and `Main workflow` mini metrics. EN/ZH/HI source data now render `Primary Asset` as stacked Panel / client / supplier roles with no plus signs in that rec metric, and `Main workflow` as stacked steps with arrows at the start of each step after the first. `.mini-metrics strong` uses safe `textContent` with `white-space: pre-line`; metric cards have tighter line height, padding, and centered vertical distribution so stacked r1/r2 and one-line r3 do not leave large bottom whitespace or horizontal overflow. `[CONFIRMED]` Package B `#workflow` Operating Path tabs now auto-cycle through `panel -> network -> enterprise` before user interaction and stop on the clicked tab after any workflow-tab click, using separate workflow timer/lock names from the existing topology fit carousel.

**Verification run:** red source checks before editing confirmed the old EN/ZH/HI Enterprise rec metric strings and the old direct workflow click handler were present, with no workflow timer/lock helpers. Post-repair: static source checks confirmed the old one-line Enterprise rec metric strings are absent from the rec values, stacked newline values are present, `white-space: pre-line` / compact centered metric CSS exists, the old direct workflow click handler is absent, `workflowRouteTimer`, `workflowUserLocked`, `startWorkflowRouteCycle()`, and initialization call are present, and topology route cycle helpers remain intact. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. Inline script syntax check for 3 scripts -> OK. Browser DOM check with Chrome at 1440px -> workflow active tab changed `network -> enterprise` after one interval, then clicking `panel` stayed on `panel` beyond another interval; EN/ZH Enterprise metric samples had `overflowX=0`, r1/r2 stacked line counts 3/4, `white-space: pre-line`, line-height about 18.3px, metric-card horizontal overflow 0, recommendation-card bottom gap 29px, and balanced metric-card content bottom gaps (EN r1/r2/r3 about 22px/13px/22px; ZH about 22px/13px/31px with matching top gaps). HI browser smoke also showed r1/r2 stacked line counts 3/4 with no r1/r2 horizontal overflow and page `overflowX=0`.

**Remaining failures:** none found in this scoped Package B task. Existing plus and arrow strings outside the assigned Enterprise recommendation mini metrics, such as compare rows, tickers, and other copy, were intentionally left untouched.

**Notes for next handoff:** keep `sampora-website-public-v3/` as the active working source and do not package or rebuild zips until explicitly requested. Future `#fit` mini-metric copy changes should compare EN/ZH/HI source data together and include a browser/DOM spacing sample when stacked text is introduced.

### 2026-05-19 CST - Package A contact final CTA and FAQ panel balance

**Files changed:** `sampora-website-public-v3/index.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No FAQ/final CTA visible copy, translation objects, dynamic data, button hrefs, other pages, QA evidence outputs, delivery folders, zips, README, staging, commit, or packaging files were intentionally changed.

**Audit item addressed:** `[CONFIRMED]` Package A `#contact` scoped layout polish. The right `Quick answers` FAQ panel keeps the three FAQ items evenly distributed through the available card height. The left `.final-cta` now uses a quiet vertical flex rhythm so the steps and CTA buttons extend into the lower part of the desktop card instead of leaving a large unused lower area. Long EN/ZH/HI FAQ strings keep wrapping inside their cards.

**Verification run:** pre-edit browser red/layout check inspected EN/ZH/HI at 1440x900, 1680x1270, and 390x900. Post-edit Chromium geometry check passed with no failures: desktop EN/ZH/HI left and right cards matched heights (`446.39px` at 1440, `454px` at 1680), final CTA bottom gap was about `1px`, buttons remained `contact.html?intent=start_trial#contact-form` and `contact.html?intent=book_demo#contact-form`, FAQ list bottom gap was about `1px`, FAQ item height deltas were `0-0.02px`, FAQ item gaps were `12px`, text overflow was `0`, and document/body horizontal overflow was `0`. Mobile 390px EN/ZH/HI passed with `overflowX=0`, button hrefs preserved, and no FAQ text overflow. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS.

**Remaining failures:** none found in this scoped Package A `#contact` final CTA / FAQ layout task. `I18N-001`, `CTA-001`, and `STICKY-001` remain regression guards only; no ledger status changed, so `ISSUE_LEDGER.md` was intentionally left untouched.

**Notes for next handoff:** keep `sampora-website-public-v3/` as the active working source and do not package or rebuild zips until explicitly requested. Future `#contact` layout work should keep visible copy and EN/ZH/HI translations unchanged unless the task is explicitly a copy/i18n task, and should verify both final CTA vertical rhythm and FAQ equal distribution in EN/ZH/HI.

### 2026-05-19 CST - Package B Solutions fit recommendation card spacing

**Files changed:** `sampora-website-public-v3/solutions.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No text, CTA, topology/workflow motion, resource page, QA evidence output, delivery folder, zip, packaging, staging, commit, push, `ACCEPTANCE_TESTS.md`, or `ISSUE_LEDGER.md` files were intentionally changed.

**Audit item addressed:** `[CONFIRMED]` Package B `section#fit` right-side `.recommend` carousel/recommendation card content was visually crowded toward the upper half because `.fit-panel` stretched the right card while `.recommend` had no vertical distribution mechanism and `.mini-metrics` relied on a fixed `margin-top: 24px`. The card now uses vertical flex layout, and `.mini-metrics` uses `margin-top: auto` plus internal top padding so the metrics settle into the lower portion without hardcoding language-specific heights.

**Verification run:** scoped red source check before repair confirmed `.recommend` did not use `display:flex` or `display:grid`, `.mini-metrics` used fixed `margin-top: 24px`, and `.fit-panel` used `align-items: stretch`. Post-repair static check confirmed `.recommend` has `display:flex` and `flex-direction:column`, `.mini-metrics` no longer has fixed `24px` margin and uses `margin-top:auto; padding-top:28px`, and the existing mobile `.mini-metrics{grid-template-columns:1fr}` guard remains present. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. Browser geometry check with local Playwright/Chrome at 1440px for EN and ZH across `panel`, `network`, and `enterprise` states showed `.recommend` computed as `flex`/`column`, metrics top ratios about `0.50-0.65`, bottom gap `29px`, `overflowX=0`, and metrics left/right within the card.

**Remaining failures:** none found in this scoped Package B `#fit` recommendation-card layout task. `TOPOLOGY-001` and `I18N-001` remain regression guards only; no ledger status changed, so `ISSUE_LEDGER.md` was intentionally left untouched.

**Notes for next handoff:** keep `sampora-website-public-v3/` as the active working source and do not package or rebuild zips until explicitly requested. Future `#fit` card layout changes should preserve the EN/ZH/HI copy unchanged unless the task is explicitly a multilingual copy task, and should verify the recommendation card in at least one long-copy language for overflow.

### 2026-05-19 CST - Package E active handoff mojibake/archive boundary cleanup

**Files changed:** `SAMPORA_LATEST_REPAIR_HANDOFF.md`, `SAMPORA_LATEST_REPAIR_LOG.md`, `ACCEPTANCE_TESTS.md`, `ISSUE_LEDGER.md`, and `sampora-website-public-v3/qa-evidence/active-handoff-mojibake-guard.mjs`. No page source HTML, Resource Manuals app/data, QA evidence JSON/PNGs, delivery folders, zips, packaging outputs, staging, commit, or push files were intentionally changed by this Package E task.

**Audit item addressed:** `[STRATEGY]` governance fix for active-handoff mojibake/history noise. The bulky historical update-log block was moved from the active handoff into `SAMPORA_LATEST_REPAIR_LOG.md` under the dated archive heading `2026-05-19 CST Archive - Active handoff historical update log before mojibake boundary cleanup`. The active handoff now keeps a concise current notes section and a Historical Mojibake / Archive Boundary Rule. `ACCEPTANCE_TESTS.md` maps the rule under Docs/redirects, and `ISSUE_LEDGER.md` records stable `HANDOFF-001`.

**Verification run:** red check before edit found `SAMPORA_LATEST_REPAIR_HANDOFF.md` had 1463 lines and its `Latest Handoff Note / Handoff Update Log` began at line 266 with many historical entries. Post-edit scoped checks passed: `node "sampora-website-public-v3\qa-evidence\active-handoff-mojibake-guard.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS.

**Remaining failures:** none found in this scoped docs/QA strategy task. Existing unrelated dirty working-tree changes in page source, QA images/JSON, old delivery folders, and zips were not reviewed or modified by this task.

**Notes for next handoff:** active handoff should remain short. Put bulky historical evidence in `SAMPORA_LATEST_REPAIR_LOG.md` with a dated archive heading and clear historical-only label. Do not treat archived mojibake-looking samples as current page failures; verify current files/browser/scripts first.

- **Pitfall / symptom:** a no-write mojibake guard can flag an existing bad-text detector regex in source as if it were page-visible mojibake.
- **Root cause:** the same marker characters may appear as test patterns inside a sanitizer/validator, not as user-facing content.
- **Prevention rule:** guard scripts should distinguish page-visible/source-copy failures from clearly marked detector regexes, and must not repair out-of-scope page/source files during a docs-only strategy task.
- **Verification:** `node "sampora-website-public-v3\qa-evidence\active-handoff-mojibake-guard.mjs"` passes while still scanning active handoff, archive heading, acceptance coverage, ledger row, and public source files.
- **Owner package:** Package E QA guard / controller scope classification.

### 2026-05-19 CST - Package A hero proof cards i18n and layout

**Files changed:** `sampora-website-public-v3/index.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No other pages, `#contact` layout, hero console/topology, CTA hrefs, QA evidence outputs, delivery folders, zips, README, staging, commit, or packaging files were intentionally changed. `ISSUE_LEDGER.md` was intentionally left untouched because the related `I18N-001`, `CTA-001`, and `STICKY-001` rows remain regression guards with no status change.

**Audit item addressed:** `[CONFIRMED]` Package A homepage hero proof cards i18n/layout. Fallback HTML and `translations.en` keep the user-provided canonical EN copy for `Project intake`, `Supplier allocation`, and `Review & settlement`. `translations.zh` now uses professional Chinese counterparts: `项目接收`, `供应商分配`, `审核与结算`, with copy covering client/country/CPI/IR/LOI/quota/launch rules, unsuitable supply blocking, supplier pricing, review evidence, callback logs, supplier bills, and client invoices. `translations.hi` now uses Devanagari-first Hindi with retained industry terms where useful, including `CPI`, `IR/LOI`, `supply`, `supplier pricing`, `callback logs`, `supplier bills`, and `client invoices`. The proof-card CSS now uses a compact grid rhythm with centered desktop content, lower min-height, tighter title/body sizing, and mobile auto-height cards so content does not sit at the top with a large blank lower area.

**Verification run:** red check before repair confirmed zh/hi `heroProof*` keys still rendered stale English values including `Supplier allocation updated` and `Traceability`; Chromium geometry at 1680x1270 showed the first EN proof card had `bottomBlank=53.94px` while cards were stretched as plain blocks. Post-repair static scoped check -> PASS for fallback HTML, `translations.en/zh/hi` hero proof content, old zh/hi hero proof values absent, and Start trial / Book demo hrefs unchanged. Chromium geometry check -> PASS at 1680x1270, 1440x900, and 390x900 for EN/ZH/HI: desktop card heights stayed equal (`108.44px` EN, `106px` ZH/HI), top/bottom content gaps were balanced (`13-26.27px`), card/page horizontal overflow was `0`, mobile cards auto-sized without overlap or overflow, and CTA hrefs stayed `contact.html?intent=start_trial#contact-form` and `contact.html?intent=book_demo#contact-form`. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS.

**Remaining failures:** none found in this scoped Package A hero proof cards task. Existing unrelated dirty changes in the workspace were not reviewed, modified, reverted, staged, committed, packaged, or synced.

**Notes for next handoff:** keep visible-copy sync mandatory for any future hero proof card edits: fallback HTML plus `translations.en`, `translations.zh`, and `translations.hi` must move together. Do not reintroduce stretched block proof cards that leave short-language content pinned to the top with a large empty lower band.

### 2026-05-19 CST - Package A hero left-column rhythm follow-up

**Files changed:** `sampora-website-public-v3/index.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No visible copy semantics, translation values, CTA hrefs, hero console/topology, `#contact`, role scenarios, other pages, QA evidence outputs, delivery folders, zips, README, staging, commit, or packaging files were intentionally changed. `ISSUE_LEDGER.md` was intentionally left untouched because no recurring issue status changed.

**Audit item addressed:** `[CONFIRMED]` Package A homepage hero left-column layout follow-up. The user clarified that the issue covered the full left hero rhythm, not only the three proof cards. The repair scopes `.hero-copy`, `.hero h1`, `.hero-sub`, `.cta-row`, `.hero-proof-grid`, and `.proof-mini`: the left column now uses explicit direct-child vertical spacing from eyebrow to H1 to subtitle to buttons to proof grid, and the proof cards use a smaller responsive min-height, clearer title/body gap, and more readable body line-height so card content no longer bunches together or leaves large top/bottom voids.

**Verification run:** pre-repair Chromium geometry at 1680x1270 and 1440x900 showed hero vertical gaps `18/16/22/24px` for kicker-to-H1, H1-to-subtitle, subtitle-to-buttons, and buttons-to-proof-grid. The previous proof cards used `align-content:center`, `min-height:106px`, `gap:7px`, and `10.5px/1.38` body text; ZH desktop cards measured `106px` high with top/body-bottom gaps about `26.27px`. Post-repair Chromium geometry passed for EN/ZH/HI at 1680x1270, 1440x900, and 390x900: main hero gaps are consistently `22/20/26/24px`; desktop proof card measurements are EN `122.5px` high with top/body-bottom gaps `23.25/23.25px` on the shorter card and `15/15px` on longer cards, ZH `92px` high with `16.25/16.25px`, and HI `106px` high with `23.25/23.25px` or `15/15px`; title-body gap is `9px` in all sampled cards. Mobile proof cards auto-size with top/body-bottom gaps `14/14px`; all sampled cards have text overflow `0`, document horizontal overflow `0`, and CTA hrefs remain `contact.html?intent=start_trial#contact-form` and `contact.html?intent=book_demo#contact-form`. Scoped source search confirmed fallback heroProof text and EN/ZH/HI `heroProof*` translation entries are still present. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS.

**Remaining failures:** none found in this scoped Package A hero-left rhythm follow-up. Existing unrelated dirty workspace changes were not reviewed, modified, reverted, staged, committed, packaged, or synced.

**Notes for next handoff:** do not restore margin-only desktop overrides that collapse the hero rhythm back to `18/16/22/24px`, and do not increase proof-card fixed height without rechecking EN/ZH/HI card inner top/title/body/bottom gaps. Future hero-left layout work should preserve current text semantics and CTA intent URLs unless explicitly assigned.

### 2026-05-19 CST - Package B solution-layer card title fit

**Files changed:** `sampora-website-public-v3/solutions.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No other pages, visible copy, translations, CTA hrefs, topology/workflow carousel logic, compare table data, QA evidence outputs, delivery folders, zips, staging, commit, push, or packaging files were intentionally changed.

**Audit item addressed:** `[CONFIRMED]` Package B solution-layer card title fit. The English `Supplier Network` title in the first solution card row was splitting into two lines at 1681px. The solution card top area now uses a two-column grid with a resilient title column, card titles stay on one line on desktop, the later `text-wrap: balance` polish no longer applies to `.plan-card h3`, badges can wrap inside their own column, and the mobile overflow guard restores normal title wrapping below 680px.

**Verification run:** controller red check before repair showed `article.plan-card.network h3` at 1681x1270 EN had height `74.88px`, line-height `37.44px`, and `lineCount=2`. Post-repair no-screenshot Chrome DOM checks passed for EN/ZH/HI at 1681x1270, 1440x900, and 390x900: all sampled solution-card titles had `titleOverflow=0`, card/page overflow was `0`, choose buttons were visible, and EN `Supplier Network` measured `lineCount=1` at 1681px and 1440px. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. Inline script syntax check for `solutions.html` -> OK for 2 scripts.

**Remaining failures:** none found for this scoped solution-layer card-title fit task. Earlier Package A/B dirty source changes remain outside this audit item and were not reverted or repackaged.

**Notes for next handoff:** keep `sampora-website-public-v3/` as the active working source. Do not reapply `text-wrap: balance` to `.plan-card h3` on desktop without rechecking English `Supplier Network` and the ZH/HI card titles at desktop widths. Mobile wrapping below 680px is intentional.

### 2026-05-19 CST - Package B fit recommendation vertical distribution

**Files changed:** `sampora-website-public-v3/solutions.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No visible copy, translations, CTA hrefs, topology/workflow carousel logic, compare table data, other pages, QA evidence outputs, delivery folders, zips, staging, commit, push, or packaging files were intentionally changed.

**Audit item addressed:** `[CONFIRMED]` Package B `section#fit .recommend` vertical rhythm. The right recommendation card had its content group centered as one block, leaving large empty bands above and below the actual content. The card now uses a grid row structure so the label, title, and description start near the top, while the mini-metrics area consumes the remaining lower space. This keeps the three metric cards aligned and distributes the available height instead of bunching the content in the middle.

**Verification run:** controller red check before repair showed EN Network at 1681x1270 had `cardHeight=521.44px`, `topGap=130.5px`, `metricsBottomGap=130.5px`, `descToMetrics=26px`, and `.recommend` computed as `display:flex` / `justify-content:center`. Post-repair no-screenshot Chrome DOM checks passed for EN/ZH/HI at 1681x1270 and 1440x900 across `panel`, `network`, and `enterprise`: top gap was `29px`, metrics bottom gap was `29px`, desc-to-metrics gap was `44px`, metric card height spread was `0`, label top spread was `0`, strong overflow max was `0`, metrics stayed inside the card, and page overflow was `0`. Mobile 390x900 EN/ZH/HI Network also passed with page overflow `0` and metrics inside the card. Worker checks and controller rerun passed: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; inline script syntax check for `solutions.html` -> OK for 2 scripts.

**Remaining failures:** none found for this scoped `#fit` recommendation-card vertical-distribution task. Existing dirty changes outside this audit item were not reviewed, reverted, packaged, staged, committed, or pushed.

**Notes for next handoff:** do not restore `.recommend { justify-content:center; }` for the `#fit` recommendation card. Future `#fit` layout work should measure top gap, desc-to-metrics gap, metrics bottom gap, metric label alignment, strong overflow, and page overflow across EN/ZH/HI before claiming completion.

### 2026-05-19 CST - Controller scoped total validation after Package A/B point fixes

**Files changed:** `SAMPORA_LATEST_REPAIR_HANDOFF.md` only for this controller validation note. No page source, QA evidence outputs, delivery folders, zips, staging, commit, push, or packaging files were intentionally changed by the controller validation step.

**Audit items covered:** current batch closeout for Package A homepage hero proof cards / hero-left rhythm and Package B Solutions `#fit` recommendation metrics, fit carousel click-lock, `#workflow` tab carousel click-lock, and `#compare` decision-matrix column balance. Rawls read-only review passed EN/ZH/HI synchronization for the latest Solutions metric changes. Boole read-only review passed homepage hero proof card fallback, EN/ZH/HI translations, CSS/card geometry, mobile overflow, and CTA href preservation.

**Verification run:** controller fresh checks passed: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\active-handoff-mojibake-guard.mjs"` -> PASS; `node --check` for `assets/i18n-sweep.js`, `assets/resource-manuals/data.js`, and `assets/resource-manuals/app.js` -> exit 0; `node "sampora-website-public-v3\qa-evidence\resource-manuals-language-smoke.mjs"` -> OK; `node "sampora-website-public-v3\qa-evidence\resource-manuals-browser-language-check.mjs"` -> OK. Inline script syntax checks passed for `index.html` (4 scripts) and `solutions.html` (2 scripts). Controller no-screenshot Chrome DOM checks passed for `index.html` EN/ZH/HI at 1440px and 390px: three proof cards present, desktop cards equal-height/equal-top, mobile no horizontal overflow, and Start trial / Book demo intent URLs preserved. Controller no-screenshot Chrome DOM checks passed for `solutions.html` EN/ZH/HI at 1440px and ZH 390px: fit selectors auto-cycle without user click and stay locked after clicking Enterprise; workflow tabs auto-cycle without user click and stay locked after clicking Panel; Enterprise primary asset renders as vertical lines with no plus signs; workflow metric lines start with `→`; Panel workflow spans stay `nowrap` with overflow 0, including `→ survey participation`; mini metric card height spread and label-top spread are 0; compare header widths are about `325.91 / 344.02 / 344.02 / 344.06px`; page overflow is 0; mobile compare scroll stays inside `.compare-wrap`.

**Remaining failures:** none found in this scoped controller total validation. Full screenshot-based `page-layer-browser-check.mjs`, Lighthouse/axe, delivery sync, zip inspection, and packaging were intentionally not run because the user has not asked for packaging/final delivery artifacts and the current batch was scoped to working-source point fixes.

**Notes for next handoff:** keep `sampora-website-public-v3/` as the active source. Do not rebuild zips or create a new delivery version until explicitly requested. Future visible-copy changes must continue to update EN/ZH/HI primary source data together and should use read-only review plus no-screenshot browser DOM checks for quick point-fix closeout when packaging is out of scope.

### 2026-05-19 CST - Package A comprehensive homepage continuation

**Files changed:** `sampora-website-public-v3/index.html`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, and `ISSUE_LEDGER.md` for scoped evidence notes. No other HTML pages, resource-manual assets, QA evidence outputs, delivery folders, zips, README files, package artifacts, staging, commit, push, or packaging files were intentionally changed.

**Audit items addressed:** `[CONFIRMED]` Package A continuation items A-F. Role scenarios now have synced fallback/EN/ZH/HI role copy, Chinese terminology uses `自有样本运营方` where in scope, Hindi role content is Devanagari-first, and `.quote .impact` no longer uses `margin-top:auto`; desktop p-to-impact gaps verify at `14px`. Hero console Operation Feed uses stable grid rows with aligned time/body/status columns. Dynamic project console/feed data now uses separate EN/ZH/HI project arrays instead of `projects.zh = projects.en` / `projects.hi = projects.en`. Topology stage labels are brighter, slightly larger, weight `700`, and baseline-aligned in normal and modal views while core node labels remain English. Paint-heavy `body::after signalSweep`, `body::before ambientDrift`, and offscreen `sectionBreath` references were removed while active topology flow/packet motion remains. Homepage capability 09, product surfaces, pricing roles/lists, workflow, final CTA/FAQ, and the assigned ZH hero/footer terminology were synchronized for EN/ZH/HI.

**Verification run:** scoped red checks before editing confirmed copied-through role/project data, `projects.zh = projects.en`, `projects.hi = projects.en`, `.quote .impact { margin-top:auto }`, ambient animation names, and stale role focus strings. Post-repair checks passed: inline `index.html` script syntax extraction -> OK for 5 scripts; static source assertions -> `projects` aliases removed, quote impact auto margin absent, `signalSweep|ambientDrift|sectionBreath|pulse-lime` absent, old role focus prefixes absent; `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\active-handoff-mojibake-guard.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. Chrome/Playwright no-screenshot DOM checks passed for EN/ZH/HI at 1440px and 390px: document overflow `0`, role p-to-impact gaps `14px`, feed row overflow `0`, desktop feed body-to-status gaps positive, hero proof overflow `0`, ZH/HI major samples localized, Start trial and Book demo hrefs preserved. A broader browser pass also covered 1680px. Topology modal check passed with close text `X`, ZH aria `关闭拓扑`, stage labels at `9px` / weight `700` / fill `rgba(202, 226, 246, 0.72)`. Motion sampling passed with active `topoDash` dash offset changing and packet coordinates changing. rAF sampling after ambient removal measured p50 about `16.7ms` and p95 about `16.9ms`.

**Remaining failures:** none found in this scoped Package A continuation. Full screenshot browser suite, Lighthouse/axe, delivery sync, zip rebuild, and packaging were intentionally not run because this was a working-source point repair and the user explicitly forbade package/delivery changes.

**Notes for next handoff:** keep `sampora-website-public-v3/` as the active working source. Do not reintroduce project data aliases for ZH/HI, role impact auto-push, or full-page ambient sweep animations. Future homepage visible-copy changes must update fallback/EN/ZH/HI together and rerun scoped browser checks for role scenarios, hero console feed, topology modal labels/motion, CTA hrefs, and no horizontal overflow.

### 2026-05-19 CST - Package B Panel exact phrase verification

**Files changed:** `SAMPORA_LATEST_REPAIR_HANDOFF.md` only for this verification note. No page source, visible copy, translations, CSS, CTA hrefs, QA evidence outputs, delivery folders, zips, staging, commit, push, or packaging files were intentionally changed.

**Audit item checked:** `[VERIFY-FIRST -> REMOVED]` old source-only subagent report claimed the EN Panel solution card lacked the exact phrase `Owned panel and survey site operations`. Fresh current source and browser evidence disproved the old report, so no Package B implementation task was dispatched.

**Verification run:** source check with `Select-String` found `Owned Panel Operations`, `For teams focused on Owned panel and survey site operations...`, `Member incentives, wallet records and finance logs`, and `Optional Partner Network exposure for sample demand` in `sampora-website-public-v3/solutions.html`. No-screenshot Chrome DOM check at 1681x1270 for EN/ZH/HI confirmed the EN Panel solution card raw text includes `Owned panel and survey site operations`, does not include `Owned respondent operations`, and page horizontal overflow is `0`.

**Remaining failures:** none for this exact-phrase verification item. This was not a full rewrite or full i18n copy review, and no packaging/delivery validation was run.

**Notes for next handoff:** do not treat the old source-only exact-phrase report as current evidence. If a future reviewer wants broader Panel-card terminology or translation polish, re-enter it as a fresh multilingual `Language/i18n` verification item with current evidence before any repair.

### 2026-05-19 CST - Package A urgent hero-left layout user-failed rework

**Files changed:** `sampora-website-public-v3/index.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No other pages, visible copy semantics, translations, CTA hrefs, right hero console/topology, role scenarios, `#contact`, dynamic data, QA evidence outputs, delivery folders, zips, README, staging, commit, push, or packaging files were intentionally changed. `ISSUE_LEDGER.md` was intentionally left untouched because no recurring issue status changed.

**Audit item addressed:** `[CONFIRMED]` Package A homepage hero-left visual rhythm user-failed返工 after earlier PASS reports. The user reported the real browser still looked like the whole left hero block was squeezed into one cluster with too much top/bottom empty space. This repair is limited to scoped CSS for `.hero-copy`, `.hero h1`, `.hero-sub`, `.hero .cta-row`, `.hero-proof-grid`, and `.proof-mini`: desktop hero grid now stretches the left column to the hero row height, centers a taller natural content group, uses language-aware spacing for EN/ZH/HI, gives ZH subtitle width enough wrapping to avoid a short-language cluster, and keeps proof cards readable with larger title/body spacing and no text overflow.

**Verification run:** red browser geometry before this rework showed the old PASS evidence was incomplete: EN at 1680x1270 had content/group height `631.06px` inside `853.86px` grid height (`ratio=0.74`, grid top/bottom empty `111.39/111.41px`), EN at 1440x900 had `ratio=0.73` with top/bottom `113.89/113.89px`, and ZH was worse at `ratio=0.62-0.63` with top/bottom empty about `155-163px`. The previous reported `22/20/26/24px` group gaps did not prove whole-block visual rhythm. Post-repair Chrome geometry passed for EN/ZH/HI at 1680x1270, 1440x900, and 390x900: EN ratios are `0.95` at 1680 and `0.85` at 1440; ZH ratios are `0.76` at 1680 and `0.78` at 1440; HI ratios are `0.98` at 1680 and `1.00` at 1440 because Hindi content is naturally taller, with no horizontal overflow. Desktop group gaps are EN `28/24/34/30px`, ZH `34/28/40/36px`, and HI `22/20/26/24px`; proof cards measure EN/HI `133.25px` high and ZH `128px` high, title/body gap `10px`, card text overflow `0`, and document horizontal overflow `0`. Mobile 390 EN/ZH/HI kept overflow `0`, proof card overflow `0`, and auto-height cards. CTA hrefs stayed `contact.html?intent=start_trial#contact-form` and `contact.html?intent=book_demo#contact-form`. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\active-handoff-mojibake-guard.mjs"` -> PASS.

**Remaining failures:** none found in this scoped Package A hero-left layout rework. Existing dirty workspace changes outside this specific CSS/handoff entry were not reviewed, reverted, staged, committed, packaged, or synced.

**Notes for next handoff:** do not accept hero-left rhythm based only on local gap numbers. Future checks must include content group height, hero-copy/grid height, content ratio, grid top/bottom empty, proof card internal gaps, overflow, and CTA href preservation across EN/ZH/HI desktop plus mobile smoke. Do not undo this by restoring centered shrink-wrap `.hero-copy` without rerunning the whole-block geometry.

### 2026-05-19 CST - Package A hero topology pool node visual alignment

**Files changed:** `sampora-website-public-v3/index.html`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, and `ISSUE_LEDGER.md` for scoped `TOPOLOGY-001` evidence. No other pages, hero-left, hero proof cards, Role scenarios, Operation feed, dynamic project data, translations semantics, CTA hrefs, QA evidence outputs, delivery folders, zips, README files, staging, commit, push, or packaging files were intentionally changed.

**Audit item addressed:** `[CONFIRMED]` Package A hero console topology SVG visual repair for the center `SAMPLE SUPPLY POOL` node. Red browser geometry showed the center pool card was oversized against the neighboring nodes (`190x66` versus Sampora `130x54` and Review/Settle `58x56`), the pool text group was left-biased by about `-22px` in normal and modal views, and the pool subtitle used a much dimmer fill (`rgb(79, 98, 128)`) than nearby node subtitles. The repair narrows the pool node to `160x58`, keeps it slightly wider than Sampora only because the full uppercase title needs room, recenters the title/subtitle with `text-anchor="middle"`, raises subtitle readability to `var(--fg2)`, and synchronizes the router/pool/source path endpoints to the new card edges.

**Verification run:** post-repair Chromium geometry passed for EN/ZH/HI at 1440x900 and 1680x1270 in both normal hero and expanded modal. Pool rect was `160x58`, Sampora `130x54`, Review/Settle `58x56`; pool text center offset stayed within `0.25px` horizontal and about `-1.13px` vertical; title/subtitle overflow was `0/0`; pool title fill was `rgb(123, 227, 255)` and subtitle fill was `rgb(188, 203, 224)`. Motion sampling in the same checks kept `topoDash` active with changing dash offsets and moving packets. Modal live-node check passed: initial `#network` count `1` under `.container.hero-grid`, modal open moves the same node to `.topology-modal__body` with close text `X`, and close restores it to `.container.hero-grid`. Stage labels remained `9px` / weight `700` / `rgba(202, 226, 246, 0.72)`, and document horizontal overflow remained `0`. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\active-handoff-mojibake-guard.mjs"` -> PASS.

**Remaining failures:** none found for this scoped topology pool-node visual alignment task. Existing dirty workspace changes outside this specific source/docs evidence were not reviewed, reverted, packaged, staged, committed, or pushed.

**Notes for next handoff:** keep the pool node near `160x58` unless a future visual check proves the long uppercase title can remain readable at a narrower width. If changing this topology again, remeasure normal and modal node bboxes, text center offsets, subtitle fill, path endpoints, EN/ZH/HI language switching, live-node modal restore, `topoDash` dash-offset changes, packet motion, stage labels, and horizontal overflow.

### 2026-05-19 CST - Package A Hindi hero console localization follow-up

**Files changed:** `sampora-website-public-v3/index.html`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, and `ISSUE_LEDGER.md` for scoped `I18N-001` evidence. No other pages, delivery folders, zip files, QA evidence artifacts, staging, commit, push, or packaging files were intentionally changed.

**Audit item addressed:** `[CONFIRMED]` Package A Hindi hero/topology console peripheral text. Fresh source red checks showed `translations.hi` and `projects.hi` still had English console terms such as `idle`, `Completes`, `QC pass`, `CPI avg`, `14/22/31 reviewed`, `$5.30 plan`, `review pending`, and related English ticker/status wording. Core topology SVG node labels such as `SAMPLE SUPPLY POOL`, `PANEL_01`, and similar system labels were intentionally preserved per topology policy.

**Verification run:** scoped source check after repair confirmed the Hindi `translations.hi` override and `projects.hi` block have no hits for `COMPLETES`, `Completes`, `reviewed`, `plan`, `review pending`, `pending review`, `idle`, `QC pass`, or `CPI avg`. Inline `index.html` script syntax extraction passed for 5 scripts. Static guards passed: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\active-handoff-mojibake-guard.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. Chromium 1440x900 no-screenshot DOM check passed for EN/ZH/HI: EN console stayed English, ZH console stayed Chinese, HI console labels/trends/meta/feed/ticker were Devanagari-first for the sampled running project, document horizontal overflow was `0`, and console section overflow count was `0`.

**Remaining failures:** none found for this scoped Hindi hero console localization follow-up. Broader Hindi copy outside the right hero console/topology peripheral scope was not reviewed or repaired.

**Notes for next handoff:** when touching homepage hero console or topology peripheral copy again, verify the runtime `projects.hi` data in addition to translation keys. Do not rely on `i18n-sweep.js` alone, and keep core uppercase topology node labels in English unless the topology policy changes.

### 2026-05-19 CST - Package B Solutions terminology repair

**Files changed:** `sampora-website-public-v3/solutions.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No other pages, QA evidence outputs, delivery folders, zips, staging, commit, push, packaging files, screenshots, old public folders, or motion/reference files were intentionally changed. `ISSUE_LEDGER.md` was intentionally left unchanged because the recurring `I18N-001` / `TERMINOLOGY-001` routing status did not change.

**Audit item addressed:** `[CONFIRMED]` Package B `solutions.html` terminology conflict under `Language/i18n`. Chinese `heroSub` and `footerDesc` now use the glossary-required `自有样本运营方` instead of the stale Panel Provider Chinese variants. Hindi `statusMid` and the Panel solution-card badge no longer keep the full English `Panel Provider` string; they use Devanagari-first copy while retaining allowed product/industry terms such as `Panel`, `Sample Supplier`, and `Enterprise`.

**Verification run:** red check before editing found current conflicts in `solutions.html`: ZH `heroSub` and `footerDesc`; HI `statusMid` and Panel-card `badge`. Post-repair source checks passed for the scoped conflict terms: no hits for `样本服务商` or `自有 Panel 供应商`; remaining `Panel Provider` hits are EN meta/footer copy only, not Chinese or Hindi body copy. Inline `solutions.html` script syntax extraction passed for 3 scripts. Static guards passed: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\active-handoff-mojibake-guard.mjs"` -> PASS. Chromium no-screenshot DOM smoke at 1440x900 for EN/ZH/HI passed: ZH visible hero/footer had `自有样本运营方`, no ZH bad terms, no visible `Panel Provider`; HI `statusMid` and Panel badge had no full `Panel Provider`; document horizontal overflow was `0` in all three languages.

**Remaining failures:** none found for this scoped Solutions terminology task. Existing dirty `solutions.html` changes outside these terminology strings were not reviewed, reverted, staged, committed, packaged, or synced.

**Notes for next handoff:** future Package B visible-copy work must keep checking `SAMPORA_TERMINOLOGY_GLOSSARY.md` before edits and verify both source strings and runtime DOM text. Do not treat EN `panel providers` meta/footer wording as a Chinese/Hindi terminology conflict unless a future audit explicitly scopes English wording.

### 2026-05-19 CST - Package B/C/D non-home terminology repair

**Files changed:** `sampora-website-public-v3/contact.html`, `sampora-website-public-v3/plans.html`, `sampora-website-public-v3/resources.html`, `sampora-website-public-v3/resource-manuals.html`, and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No `index.html`, `solutions.html`, QA screenshots/evidence outputs, delivery folders, zips, old public folders, staging, commit, push, packaging files, or motion/reference files were intentionally changed. `ISSUE_LEDGER.md` was intentionally left unchanged because the recurring `I18N-001` / `TERMINOLOGY-001` routing status did not change.

**Audit item addressed:** `[CONFIRMED]` non-home small-page terminology conflicts under `Language/i18n`. `contact.html` ZH `rolePanel` and `footerDesc`, `plans.html` ZH `footerDesc` and matrix `主要用户`, `resources.html` ZH `footerDesc`, and `resource-manuals.html` ZH `brand` now use the glossary-required `自有样本运营方` instead of `样本服务商`. Hindi footer/brand strings in `contact.html`, `plans.html`, and `resource-manuals.html` no longer keep the full English `Panel Provider` phrase inside Hindi body copy; they use Devanagari-first wording while retaining allowed terms such as `Panel`, `Sample Supplier`, and `Partner Network`.

**Verification run:** red check before editing used `Select-String` on the four assigned HTML files and confirmed current hits for `样本服务商` in `contact.html`, `plans.html`, `resources.html`, and `resource-manuals.html`, plus Hindi `Panel Provider` hits in `contact.html`, `plans.html`, and `resource-manuals.html`. Post-repair scoped source checks passed: no hits for `样本服务商` or `自有 Panel 供应商` in the four assigned files; no Hindi-pattern hits for `Panel Provider` (`Panel Provider और`, `Panel Provider टीम`, `Panel Provider के`, `Panel Provider की`, `Panel Provider का`, or `Panel Provider `). Remaining `Panel Provider` / `panel providers` hits are EN fallback/meta/value copy only. Static guards passed: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\active-handoff-mojibake-guard.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\resource-manuals-language-smoke.mjs"` -> OK; `node "sampora-website-public-v3\qa-evidence\resource-manuals-browser-language-check.mjs"` -> OK. A lightweight no-screenshot DOM smoke was attempted via Playwright CLI against a temporary localhost server, but the CLI `eval` command could not complete due argument parsing; generated `.playwright-cli` scratch artifacts were removed, and no QA screenshots/evidence outputs were kept or changed.

**Remaining failures:** none found in scoped source/static/resource-manual verification for the assigned terminology conflicts. Runtime DOM overflow checks for `contact.html`, `plans.html`, and `resources.html` were not completed because the local Playwright module was not available and the CLI eval path failed; no browser-only failure evidence was observed.

**Notes for next handoff:** keep EN `panel providers` / form option values as natural English unless a future audit explicitly scopes English wording. Future non-home terminology work should continue to verify both source strings and, when the browser tool is available, EN/ZH/HI runtime text without writing screenshot artifacts.

### 2026-05-20 JST - Package B resources topology mode-switch layout stability

**Files changed:** `sampora-website-public-v3/resources.html`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, and `ISSUE_LEDGER.md` for scoped `TOPOLOGY-001` evidence. No `solutions.html`, `index.html`, QA scripts, QA evidence outputs, delivery folders, zips, README files, staging, commit, push, or packaging files were intentionally changed.

**Audit item addressed:** `[CONFIRMED]` Package B `resources.html` topology/resource mode switching vertical reflow. Red Playwright geometry against current files showed EN desktop setup/supplier/finance changed hero/console/next section by `22.67px`, HI desktop by `24.08px`, and HI mobile by `39.08px`; `.topo-stage` stayed stable while `.console-summary`, `#consoleSub`, and mobile ticker text height changed. The repair adds scoped CSS height reserves for the resource console summary and ticker, with a 1280px desktop reserve and a mobile reserve, without changing visible copy or topology JavaScript.

**Verification run:** post-repair Playwright geometry passed for EN/ZH/HI across setup/supplier/finance at 1280x932, 1440x900, and 390x900: hero height, console/window height, and next-section offset deltas were `0px`; `.topo-stage` stayed `246px` desktop and `240px` mobile. Active flow groups still switched to `flowSetup`, `flowSupplier`, and `flowFinance`. Motion sampling passed after the final CSS change: active `topoDash` remained present, dash offsets changed for all three modes, and packet centers moved. Static checks passed: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `git diff --check` -> exit 0 with CRLF warnings only.

**Remaining failures:** none found in this assigned `resources.html` topology layout stability scope. Existing dirty workspace changes outside the assigned file/docs evidence were not reviewed, reverted, staged, committed, packaged, or synced.

**Notes for next handoff:** keep validating resources topology with live browser geometry, not screenshots only. If future copy changes lengthen mode summary or ticker text, rerun EN/ZH/HI setup/supplier/finance geometry at 1280px, 1440px, and 390px and confirm active `topoDash` plus packet movement remain intact.

### 2026-05-20 JST - Package C plans matrix side whitespace

**Files changed:** `sampora-website-public-v3/plans.html`, `ACCEPTANCE_TESTS.md`, and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No other pages, visible copy/i18n strings, CTA hrefs, footer/legal behavior, contact backend behavior, QA evidence outputs, delivery folders, zips, README/deployment docs, staging, commit, push, or packaging files were intentionally changed. `ISSUE_LEDGER.md` was intentionally left unchanged because this was a one-off visual layout issue, not a recurring ledger item.

**Audit item addressed:** `[CONFIRMED]` Package C `plans.html` `section#matrix` pricing matrix side whitespace. Red browser geometry showed `.matrix-wrap` filled or nearly filled the section container at desktop/tablet sizes: 1280px inner margins `0/0`, 1440px `20/20`, and 1750px `20/20`, while 390px mobile already had document overflow `0` and internal matrix scroll `356/960`. The repair adds a scoped `#matrix .matrix-wrap` desktop/tablet cap of `1120px` with a matching mobile override that preserves `width:100%`, `max-width:100%`, `overflow-x:auto`, and `overflow-y:hidden`. `#matrixTable` keeps `min-width:960px`.

**Verification run:** post-repair Playwright geometry against current `sampora-website-public-v3/plans.html` passed: 1280px container/wrap `1232/1120` with inner margins `56/56`; 1440px `1360/1120` with `120/120`; 1750px `1360/1120` with `120/120`; all desktop/tablet samples had document overflow `0`. Mobile 390px passed with document overflow `0`, wrap client/scroll `356/960`, table min-width `960px`, computed wrap max-width `100%`, `overflow-x:auto`, and `overflow-y:hidden`. Static checks passed: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `git diff --check` -> exit 0 with CRLF warnings only.

**Remaining failures:** none found in this scoped Package C visual layout repair. Existing dirty workspace changes outside the assigned file/docs evidence were not reviewed, reverted, staged, committed, packaged, or synced.

**Notes for next handoff:** keep the matrix wrapper centered and narrower than the section container on desktop/tablet, and keep validating 390px as no document horizontal overflow plus internal `.matrix-wrap` horizontal scroll. If future work changes the matrix width, table min-width, or mobile overflow rule, rerun the same 1280/1440/1750/390 geometry check.

### 2026-05-20 CST - Package A homepage pricing/platform/final CTA repair

**Files changed:** `sampora-website-public-v3/index.html`, `ACCEPTANCE_TESTS.md`, and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No other HTML files, QA evidence outputs, delivery folders, zips, old public folders, staging, commit, push, or packaging files were intentionally changed. Existing unrelated dirty diffs, including role-card/quote layout changes and delivery/QA artifact changes, were preserved. `ISSUE_LEDGER.md` was intentionally left unchanged because no recurring issue status changed.

**Audit items addressed:** `[CONFIRMED]` Package A homepage pricing plan-name localization now uses the terminology-approved dynamic names: EN `Panel / Supplier Network / Enterprise`, ZH `样本库运营版 / 供应商网络运营版 / 企业版`, and HI `Panel ऑपरेशंस संस्करण / सप्लायर नेटवर्क ऑपरेशंस संस्करण / एंटरप्राइज संस्करण`. `[CONFIRMED]` Package A `#platform` capability labels and m1-m9 platform title/title/text/metric copy now have EN/ZH/HI i18n coverage, with Chinese product capability copy localized and Hindi tightened to Devanagari-first where the glossary allows retained product terms such as `API`, `Panel`, and `Workspace`. `[CONFIRMED]` Package A `#contact .final-cta` left-card rhythm now has explicit eyebrow-to-title spacing, body-to-steps and steps-to-button spacing, a small button-to-card-bottom gap, preserved CTA hrefs, and the right FAQ panel keeps equal-height wrapping. `[STRATEGY]` `ACCEPTANCE_TESTS.md` now includes scoped Homepage final CTA layout acceptance coverage.

**Verification run:** source/diff intake confirmed only the pre-existing `index.html` dirty diff in role-card/quote layout before this worker edited. Scoped source check passed for translation completeness and plan-name routing: missing platform keys `[]`, Chinese bad platform hits `[]`, English CJK platform hits `[]`, and old `updatePlanNames.zh` `Panel`/`Enterprise` pattern `false`. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. Inline `index.html` script syntax extraction -> PASS for 5 scripts. Browser validation used a temporary local static server at `http://127.0.0.1:8793/index.html` and `playwright-cli` without keeping screenshots/evidence files. EN/ZH/HI at 1440x900 and 390x900 passed: pricing names matched the expected language; ZH platform title/first/last modules had no `Nine product capabilities`, `Project Delivery Management`, `Supplier Allocation & Pricing`, `Profile records`, `Capability 01`, `Panel & Member Operations`, or `Enterprise` hits; HI first/last modules rendered `क्षमता 01`, `प्रोजेक्ट डिलीवरी प्रबंधन`, `डिलीवरी workflow`, `क्षमता 09`, `वर्कस्पेस और भूमिका शासन`, and `शासन नियंत्रण`; final CTA gaps were desktop `23.03/12/34/38/29px` for kicker-to-title/title-to-body/body-to-steps/steps-to-buttons/buttons-to-bottom and mobile `18/12/24/22/29px`; FAQ height delta was `0-0.02px`; CTA hrefs stayed `contact.html?intent=start_trial#contact-form` and `contact.html?intent=book_demo#contact-form`; horizontal overflow was `0` in every sampled language/viewport. Browser console showed only a non-blocking `favicon.ico` 404 during local-server validation.

**Remaining failures:** none found in this scoped Package A repair. This was not packaging/final delivery work, and no zip, delivery folder, Lighthouse, axe, contact-form behavior, or unrelated page validation was run.

**Notes for next handoff:** future Package A platform-copy work should verify both the early `translations.zh/hi` objects and later `Object.assign` overrides so source checks and runtime behavior agree. Future final CTA work must keep checking EN/ZH/HI desktop/mobile geometry, FAQ equal rows, CTA href preservation, and no reintroduced left-stack `margin-top:auto` or `justify-content: space-between`.

### 2026-05-20 CST - Package B/D Resources entry cards and manual anchors

**Files changed:** `sampora-website-public-v3/resources.html`, `sampora-website-public-v3/resource-manuals.html`, and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No `index.html`, `solutions.html`, `plans.html`, `contact.html`, QA evidence outputs, delivery folders, zips, staging, commit, push, or packaging files were intentionally changed. `ISSUE_LEDGER.md` was intentionally left unchanged because the recurring `I18N-001` / `MANUAL-001` routing state did not change.

**Audit item addressed:** `[CONFIRMED]` Package B/D Resources page user-specified repair. The Resources hero proof metrics were replaced with three linked resource-entry cards for Configuration Guide, Operation Manual, and Rollout Notes with EN/ZH/HI i18n keys. The Resources footer description was aligned to the main-site audience wording in EN/ZH/HI, removing research subcontracting / 调研分包 / उप-ठेका wording. The `detailLinks` order now matches Configuration Guide / Operation Manual / Rollout Notes. The operating path right-side proof panel now uses operating-evidence copy, refreshed four proof cards, retained the mode strip, and adds a compact three-row output module inside the proof panel for EN/ZH/HI. The left six-item setup path list was intentionally left unchanged. Package D received only the minimal `resource-manuals.html` hash anchors required by those links: `configuration-guide`, `operation-manual`, and `rollout-notes`.

**Verification run:** scoped source checks passed: no `data-i18n="metric1"`, `data-i18n="metric2"`, `data-i18n="metric3"`, `"metric1":`, `"metric2":`, `"metric3":`, `research subcontracting`, `调研分包`, `उप-ठेका`, `BACKEND EVIDENCE`, old proof-title copy, `后台证据`, or `बैकएंड प्रमाण` remained in `resources.html`; the three hero card hrefs and `detailLinks` are `resource-manuals.html#configuration-guide`, `resource-manuals.html#operation-manual`, and `resource-manuals.html#rollout-notes`; EN/ZH/HI `metric*Title`, `metric*Text`, `proofOutputTitle`, and `proofOutput` keys exist; EN/ZH/HI footer descriptions match the requested wording; `git diff -U0 -- resources.html` showed no added or removed `"setup":` lines, so the left setup list was not changed in this round; `resource-manuals.html` contains the three target ids. Syntax/static checks passed: `node --check "sampora-website-public-v3\assets\resource-manuals\data.js"` -> exit 0; `node --check "sampora-website-public-v3\assets\resource-manuals\app.js"` -> exit 0; inline script syntax extraction for `resources.html` -> OK with 2 inline scripts; previous inline script syntax extraction for `resource-manuals.html` -> OK with 3 inline scripts; `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\active-handoff-mojibake-guard.mjs"` -> PASS; `git diff --check -- sampora-website-public-v3\resources.html sampora-website-public-v3\resource-manuals.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> exit 0 with CRLF warnings only. Playwright CLI no-screenshot browser smoke against a temporary local server passed for Resources EN/ZH/HI at 1440x900 and 390x900: hero entry-card structure and hrefs were identical and usable in all languages; rendered `surface-link` / `detailLinks` order matched the same three anchors in all languages; footer description switched correctly; proof panel kicker/title/description, four proof cards, mode strip, and three output rows switched correctly in EN/ZH/HI; all output rows were inside `.trust-panel` with `01/02/03`; setup list stayed at 6 rows with the same first/last localized labels; navigation, hero title/buttons, and topology console remained present; document horizontal overflow was `0`. Resource Manuals no-screenshot anchor smoke passed for `#configuration-guide`, `#operation-manual`, and `#rollout-notes`: target ids exist, `#viewHome` remains visible, generated doc cards count is `2`, language buttons count is `3`, and overflow is `0`. The only browser console error was a local `favicon.ico` 404 from the temporary server.

**Remaining failures:** none found in this scoped Package B/D Resources entry-card and anchor repair. Existing dirty workspace changes outside these files, including Package A `index.html`, QA evidence, delivery folder, and zip deletions, were not reviewed, reverted, staged, committed, packaged, or synced.

**Notes for next handoff:** user clarified a general rule in this round: not only copy edits require EN/ZH/HI synchronization. Layout, structure, links, data order, and visible module additions/removals must also be synchronized and verified across EN/ZH/HI before delivery claims. This round explicitly verified hero entry-card structure, `detailLinks`, footer description, and the operating proof/output module across all three languages. If future work changes manual-entry hrefs, verify both `resources.html` card/detailLinks source and the `resource-manuals.html` target ids before claiming the links are usable. Do not package until the user explicitly requests packaging/final delivery.

### 2026-05-20 CST - Package B Resources setup baseline follow-up

**Files changed:** `SAMPORA_LATEST_REPAIR_HANDOFF.md` only. `sampora-website-public-v3/resources.html` was inspected but not modified because the `const DATA` `en` / `zh` / `hi` `setup` arrays already matched `HEAD:sampora-website-public-v3/resources.html` exactly. No `index.html`, `resource-manuals.html`, QA evidence outputs, delivery folders, zips, staging, commit, push, or packaging files were intentionally changed.

**Audit item addressed:** `[CONFIRMED]` read-only review follow-up for Package B Resources left operating-path copy. The requested restore target was verified against current `HEAD`; no source patch was needed because each `DATA.*.setup` array already had 6 items and was byte-equivalent after line-ending normalization to the HEAD baseline, while the intended hero cards, footer, `detailLinks`, proof, and `proofOutput` diffs remain present.

**Verification run:** `git diff -U0 -- sampora-website-public-v3/resources.html` retained the current Resources target changes and `Select-String '"setup"\s*:'` found no setup-array diff lines. A scoped Node check compared current `DATA.en.setup`, `DATA.zh.setup`, and `DATA.hi.setup` against `HEAD:sampora-website-public-v3/resources.html` and returned `setup items=6, equals HEAD=true` for all three languages. Inline `resources.html` script syntax extraction passed for 2 inline scripts. Static guards passed: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS.

**Remaining failures:** none found for this scoped setup-baseline follow-up. Existing dirty workspace changes outside this handoff note were not reviewed, reverted, staged, committed, packaged, or synced.

### 2026-05-20 CST - Package B Resources entry-card copy and layout follow-up

**Files changed:** `sampora-website-public-v3/resources.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No `index.html`, `resource-manuals.html`, other pages, QA evidence outputs, delivery folders, zips, staging, commit, push, packaging files, hero title/buttons, right-side topology console, navigation, footer structure/copy, or manual-entry anchors were intentionally changed. Existing dirty diffs outside this narrow assignment were preserved and not reverted.

**Audit item addressed:** `[CONFIRMED]` Package B `resources.html` hero-adjacent `.resource-entry` cards still read like old compact KPI cards in the current source. The repair keeps the current three entry titles and required manual anchors, updates only the EN/ZH/HI `metric*Text` card descriptions to the user-specified short resource-entry preview copy, and tightens the scoped `.resource-entry-grid` / `.resource-entry` CSS so the three cards read as stable resource previews rather than KPI counters. The card HTML continues to use `01`, `02`, and `03`; `06` and `REG` were not reintroduced in the hero card block.

**Verification run:** scoped source checks passed for the requested CSS values (`max-width: 820px`, `align-items: stretch`, `min-height: 124px`, `grid-template-columns: 48px minmax(0,1fr)`, `gap: 16px`, `padding: 18px 16px`, `.resource-entry::after opacity: .25`, title/small typography, ZH overrides, and mobile one-column `min-height: 104px` / `max-width: 100%`). EN/ZH/HI `metric1Text`, `metric2Text`, and `metric3Text` match the requested strings; old `metric*Text` patterns `role manuals`, `rollout checklist`, `客户 / 供应商`, `后台入口组`, `बैकएंड एंट्री ग्रुप`, and `रोलआउट चेकलिस्ट` are absent from `metric*Text` entries. The three hero card anchors remain exactly `resource-manuals.html#configuration-guide`, `resource-manuals.html#operation-manual`, and `resource-manuals.html#rollout-notes`, and the hero card block contains no `06` or `REG`. Inline `resources.html` script syntax extraction passed for 2 scripts. Static guards passed: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\active-handoff-mojibake-guard.mjs"` -> PASS. `git diff --check -- sampora-website-public-v3/resources.html SAMPORA_LATEST_REPAIR_HANDOFF.md` passed with LF/CRLF warnings only.

**Remaining failures:** none found in this scoped Package B entry-card copy/layout follow-up. Browser screenshot/QA evidence was intentionally not refreshed because this was a narrow source/CSS change and the requested static guards passed.

**Notes for next handoff:** future Resources entry-card work should keep the three card anchors stable and verify both source fallback text and EN/ZH/HI translation-object values. Do not interpret `06` in the separate operating-path setup arrays as a hero-card regression unless it appears inside the `.hero-metrics.resource-entry-grid` block.

### 2026-05-20 CST - Package A homepage hero eyebrow alignment

**Files changed:** `sampora-website-public-v3/index.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No other HTML files, visible copy, i18n strings, dynamic data, CTA hrefs, JavaScript, SVG, QA evidence outputs, delivery folders, zips, old public folders, staging, commit, push, or packaging files were intentionally changed. Existing unrelated dirty diffs were preserved.

**Audit item addressed:** `[CONFIRMED]` Package A homepage `section#top .hero-copy .eyebrow.localized` desktop top alignment. The desktop-only 1241px+ homepage hero-left rhythm rule now starts `.hero-copy` from the top of the stretched hero grid instead of vertically centering the left column. Existing per-language eyebrow-to-H1 spacing variables remain in place, so the desktop eyebrow aligns with the right hero console/topology top while retaining an open title gap.

**Verification run:** scoped browser DOM geometry against a temporary local static server passed for EN/ZH/HI at 1314x1270, 1440x900, and 390x900. Desktop eyebrow top vs console top: 1314x1270 EN `174/174 diff 0`, ZH `166/166 diff 0`, HI `166/166 diff 0`; 1440x900 EN `166/166 diff 0`, ZH `166/166 diff 0`, HI `166/166.01 diff -0.01`. Eyebrow-to-H1 gaps: EN `28px`, ZH `34px`, HI `22px`. Mobile guard at 390x900 kept overflowX `0` for EN/ZH/HI. Static guards passed: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS.

**Remaining failures:** none found for this scoped Package A hero eyebrow alignment repair. This was not packaging/final delivery work; no Lighthouse, axe, zip rebuild, delivery sync, contact-form behavior, or unrelated page validation was run.

**Notes for next handoff:** if future homepage hero work changes `.hero-grid`, `.hero-copy`, `.console`, or language-specific hero rhythm variables, rerun the same EN/ZH/HI geometry at 1314x1270, 1440x900, and 390x900. Keep the desktop eyebrow-console top delta within 0-2px and keep 390px document overflowX at 0.

### 2026-05-20 CST - Package B Resources entry-card closeout verification

**Files changed:** `SAMPORA_LATEST_REPAIR_HANDOFF.md` only by this closeout worker. `sampora-website-public-v3/resources.html` was inspected but not modified in this closeout because the current `.resource-entry*` CSS already matched the user's vertical resource-entry-card target closely enough. Existing dirty `resources.html` diffs from earlier Package B workers were acknowledged and not reverted; no other page, visible copy/i18n, CTA href, hero title, right console, nav, footer, JavaScript, QA evidence output, delivery folder, zip, staging, commit, push, or packaging file was intentionally changed.

**Audit item addressed:** Package B `resources.html` closeout for the Resources hero-adjacent `.resource-entry` cards after Carver's CSS change. The scoped verification checked that the three cards render as vertical resource entry cards with `01/02/03` in the upper-left, title below the number, description below the title, title/description using full content width instead of being squeezed by the number column, desktop card height around the requested resource-card size, mobile one-column layout, and no document horizontal overflow in EN/ZH/HI.

**Verification run:** diff scope inspection found the working tree has broader historical `resources.html` dirty diffs, but this closeout only reviewed the current `.resource-entry-grid`, `.resource-entry`, `.resource-entry strong/span/b/small/::after`, `.resource-entry-warm strong`, and `@media(max-width:860px)` surface. CSS conformance passed: grid is three columns on desktop and one column below 860px; cards are `display:flex` with `flex-direction:column`; number block is 48x48 at top-left; title and description wrappers are block/`width:100%`; warm entry styling is scoped to the warm number block. Browser verification used system Chrome headless via DevTools on port 9223, not bundled Chromium or WSL. EN/ZH/HI at 1314x1270, 1440x900, and 390x900 all passed with `overflowX=0`, card `display:flex`, `flexDirection:column`, `strongTop=19`, `strongLeft=19`, `strongSize=48x48`, title below number, and description below title. Measured card heights: desktop 1314px EN/HI `185.5px`, ZH `168.56px`; desktop 1440px EN/HI `185.5px`, ZH `168.56px`; mobile 390px EN `166.13px`, ZH `148.03px`, HI `146.75-166.13px`. Title lines were `1` for all samples; title/content widths matched the card content width (`167.17-186.72px` desktop and `320px` mobile). Static guards passed: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `git diff --check -- sampora-website-public-v3\resources.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> exit 0 with CRLF warnings only.

**Remaining failures:** none found in this scoped closeout verification. The current desktop card height is around `168.56-185.5px` depending on language and wrapping, rather than an exact `150px`; this still satisfies the user's approximate desktop-card-height target and does not squeeze the Configuration Guide or Operation Manual titles/descriptions. Existing unrelated dirty workspace changes outside this assigned closeout were not reviewed, reverted, staged, committed, packaged, or synced.

**Notes for next handoff:** future Resources entry-card checks should use live browser geometry for EN/ZH/HI at 1314x1270, 1440x900, and 390x900, and should verify title/content width plus title/description vertical order, not only static CSS. Keep this card work scoped to `resources.html` unless the user explicitly assigns manual-page anchors or packaging.

### 2026-05-20 CST - Package B Resources entry-card top-line removal

**Files changed:** `sampora-website-public-v3/resources.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No other page, visible copy, i18n key, CTA href, manual anchor, right-side console, navigation, footer, QA evidence output, delivery folder, zip, staging, commit, push, or packaging file was intentionally changed.

**Audit item addressed:** `[CONFIRMED]` Package B `resources.html` Resources hero `.resource-entry` cards inherited the old `.hero-metric:before` top 1px cyan gradient, and the third `.resource-entry-warm:before` rule supplied an amber top gradient. The repair adds a scoped `.resource-entry::before, .resource-entry-warm::before` override with `display: none` and `background: none`, and removes the legacy warm top-gradient rule. The existing `.hero-metric:before` behavior remains available for non-`.resource-entry` uses.

**Verification run:** scoped source check passed: `.resource-entry::before` and `.resource-entry-warm::before` resolve to `display:none` / `background:none`; no `.resource-entry-warm:before` or `.resource-entry-warm::before` legacy linear-gradient rule remains; the three hero cards remain three `<a>` entries with hrefs `resource-manuals.html#configuration-guide`, `resource-manuals.html#operation-manual`, and `resource-manuals.html#rollout-notes`; the hero card block contains no `06` or `REG`. Static guards passed: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\active-handoff-mojibake-guard.mjs"` -> PASS; `git diff --check -- sampora-website-public-v3/resources.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> exit 0 with CRLF warnings only.

**Remaining failures:** none found in this scoped top-line removal. Existing dirty workspace changes outside the assigned two files were not reviewed, reverted, staged, committed, packaged, or synced.

**Notes for next handoff:** keep the bottom `.resource-entry::after` accent separate from the removed top pseudo-element. If future work changes `.hero-metric:before`, verify whether it should still apply to non-resource-entry metric cards before changing the shared rule.

### 2026-05-20 CST - Package B Resources operating proof panel rhythm

**Files changed:** `sampora-website-public-v3/resources.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No `index.html`, `solutions.html`, `plans.html`, `contact.html`, `resource-manuals.html`, `assets/resource-manuals/*`, QA evidence outputs, delivery folders, zips, README/deployment docs, staging, commit, push, or packaging files were intentionally changed. `ISSUE_LEDGER.md` was intentionally left unchanged because no recurring ledger status changed.

**Audit item addressed:** `[CONFIRMED]` Package B `resources.html` `section#setup` right-side `.trust-panel` proof panel visual rhythm. The previous structure/static checks passed, but current rendered evidence showed the proof panel rhythm was still tight: desktop gaps were mostly `10-18px`, `modeToOutput` was `14px`, row gaps were `10px`, and proof/output rows read as dense. This round kept the existing content order and modules, kept the output module inside the proof panel below the mode bar, and left the left six-item setup list DOM/data/copy unchanged.

**Implementation notes:** added a scoped `.proof-heading` wrapper for the proof kicker/title/description group; changed `.trust-panel` to a vertical flex layout with a single panel gap; adjusted `.mini-proof`, `.mode-fit-card`, `.proof-output`, `.proof-output-row`, `.proof-output-num`, and proof row title/body typography for clearer grouped rhythm, more readable row text, and stable mobile sizing. No visible copy or i18n keys were changed in this round.

**Verification run:** scoped red browser check before editing used Chrome DevTools geometry against `resources.html` at 1440x900 and 390x900 for EN/ZH/HI. It confirmed current rows were inside `.trust-panel` and below `.mode-fit-card`, no horizontal overflow existed, and the left setup list had 6 rows, but rhythm was tight (`miniToMode 18px`, `modeToOutput 14px`, output row gaps `10px`, bottom gap `27px`).

**Technical/static PASS:** inline script syntax for `resources.html` -> `OK: 3 scripts`; `node sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs` -> PASS; `node sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs` -> PASS; `node sampora-website-public-v3\qa-evidence\active-handoff-mojibake-guard.mjs` -> PASS; `git diff --check -- sampora-website-public-v3/resources.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> exit 0 with CRLF warnings only.

**Runtime geometry PASS:** post-repair Chrome DevTools geometry passed for EN/ZH/HI at 1440x900 and 390x900 with overflow `0`; proof/output rows remained inside the `.trust-panel` and below the `.mode-fit-card` mode bar; desktop `.trust-panel` and `.check-panel` top aligned with height deltas `0px`; desktop panel heights were EN `873px`, ZH `799px`, HI `797.69px`; mobile rows stayed intact. The left setup list count stayed `6`; first/last text stayed `Project intake` / `Settlement and finance` for EN, `项目接收` / `结算与财务` for ZH, and `प्रोजेक्ट प्राप्ति` / `निपटान और वित्त` for HI.

**Visual acceptance PASS:** right proof-panel rhythm improved from the tight red-check state to grouped spacing: desktop group gaps became `22px` between heading/proof/mode/output and mobile group gaps became `18px`; output row gaps became desktop `12px` and mobile `10px`; bottom gap stayed modest at desktop `31px` and mobile `25px`. The rendered result had no obvious empty hole and was not compressed.

**Remaining failures:** none found for this scoped Package B proof-panel rhythm repair. This was not a final delivery or packaging task, so Lighthouse, axe, delivery sync, zip rebuild, and unrelated page/manual checks were not run.

**Notes for next handoff:** future Resources operating-path visual work must keep visual acceptance browser-based, not source-only. Continue verifying EN/ZH/HI desktop/mobile geometry for panel height relationship, group spacing, output-row containment, no horizontal overflow, and the unchanged left six-row setup list before claiming visual completion.

### 2026-05-20 CST - Package B Resources operating proof panel rhythm reviewer closure

**Files changed:** `SAMPORA_LATEST_REPAIR_HANDOFF.md` only by this docs-closeout worker. No `sampora-website-public-v3/resources.html`, `contact.html`, `plans.html`, other page source, visible copy/i18n, QA evidence output, screenshot, delivery folder, zip, README/deployment doc, staging, commit, push, or packaging file was intentionally changed.

**Audit item addressed:** `[STRATEGY]` docs closure for Package B Resources operating proof panel rhythm. This entry supplements the prior repair entry with the missing read-only reviewer PASS / visual acceptance closure record; it does not reopen page-source repair.

**Verification run:** read/checked `AGENTS.md`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, `ACCEPTANCE_TESTS.md`, and `ISSUE_LEDGER.md` before editing. Final read-only review evidence reported PASS for current Resources page source/browser state: `.trust-panel > .proof-heading` order is correct; output remains inside the panel and below the mode bar; the left setup list remains 6 items; EN/ZH/HI desktop/mobile document overflow is `0`; desktop left/right panel top delta is `0` and height delta is `0`; remaining page-source failure is none. This entry records that reviewer PASS as the visual acceptance closure for the Resources proof-panel rhythm item.

**Remaining failures:** none for this docs-only closure. Remaining page-source failure for the scoped Resources proof-panel rhythm item: none. Browser screenshots, QA output refresh, Lighthouse, axe, delivery sync, zip rebuild, and page-source edits were intentionally not run or written in this closeout.

**Notes for next handoff:** treat the Resources proof-panel rhythm item as closed unless fresh current evidence reopens it. Future Resources visual changes still need separate technical/static PASS, runtime geometry PASS, and visual acceptance PASS with EN/ZH/HI desktop/mobile rendered evidence before completion claims.

### 2026-05-20 CST - Package C Contact P0 implementation

**Files changed:** `sampora-website-public-v3/contact.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No `index.html`, `solutions.html`, `resources.html`, `plans.html`, `resource-manuals.html`, `assets/resource-manuals/*`, QA evidence outputs, delivery folders, zips, README/deployment docs, staging, commit, push, or packaging files were intentionally changed. `ACCEPTANCE_TESTS.md` and `ISSUE_LEDGER.md` were intentionally left unchanged by this worker because the existing acceptance categories and recurring ledger rows already covered the scoped Contact P0 repair and no recurring issue status changed.

**Audit item addressed:** `[CONFIRMED]` Package C `contact.html` Contact P0 copy and routing repair from `C:\Users\liuge\Downloads\SAMPORA_CONTACT_PAGE_P0_CODEX_INSTRUCTIONS.md` read with UTF-8. Customer-visible implementation wording was removed from meta text, hero/form bullets, privacy, response cards, toast copy, and EN/ZH/HI i18n values. Role options were narrowed to placeholder plus `Panel Provider`, `Sample Supplier`, `Sample Aggregator / Network Operator`, `API-connected Supplier`, `Enterprise / Multi-entity Team`, and `Other`; `optionText.role` and `optionText.business` were synchronized. The required fields and hidden fields (`intent`, `source_page`, `source_section`, `plan`, `lang`) were preserved, as were URL intent preselection and placeholder endpoint behavior. The `.response` three-card layout was retained with new customer-facing copy. A lightweight `#other-ways` section was added after `.response` and before the footer, with only small scoped link styling. Footer columns now render Product / Resources / Get started with the requested links, and customer-visible email is `sales@sampora.com`.

**Verification run:** red check before editing confirmed current `contact.html` still had customer-visible `backend`, `endpoint`, `hidden fields`, `placeholder state`, `backend placeholder`, `backend wiring`, and old backend-response language in meta/form/response/toast/i18n; old role options `Survey Station / Execution Unit` and `Research Subcontractor`; old `Product support` / Contact footer grouping; `jiayu@surveysaas.com`; no `#other-ways` section; and existing hidden fields and intent map. `resource-manuals.html` was read-only checked and contains `configuration-guide`, `operation-manual`, and `rollout-notes` anchors.

**Technical/static PASS:** worker reported inline script syntax extraction passed for 2 inline scripts; `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\active-handoff-mojibake-guard.mjs"` -> PASS; git diff check had CRLF warnings only. Post-repair scoped source assertion passed: no forbidden customer-visible text or stale old role/email/footer text in visible HTML or i18n/toast string values; hidden fields remain present; `optionText.role` and `optionText.business` match the requested arrays.

**Runtime/behavior PASS:** browser/runtime check used a temporary local server and headless Chrome DevTools, then stopped both helpers. EN/ZH/HI at 1440x900 and 390x900 passed: hero/form/response/other-ways/footer visible text matched the UTF-8 translation object; role and request-type options matched requested values; footer links exactly matched the requested Product / Resources / Get started hrefs; direct links were `mailto:sales@sampora.com`, `contact.html?intent=book_demo#contact-form`, and `resources.html`; no `surveysaas.com`; no forbidden customer-visible implementation wording; overflow was `0`. URL intent checks passed for `start_trial`, `book_demo`, `contact_sales`, `enterprise_demo&plan=enterprise`, and `resource_question`, with matching select value, hidden `intent`, hidden `plan`, and hidden `lang`. Placeholder endpoint submit in Hindi showed the fallback toast title from the HI translation, did not show success or failure toast, and preserved hidden `intent=book_demo`, `plan=book_demo`, `lang=hi`; success remains gated by `res.ok`.

**Visual acceptance PASS:** the new Other ways module stayed lightweight and reused the existing direct-card system. At 1440x900, EN/ZH/HI direct cards rendered as one aligned three-column row with equal tops and no overlap; card heights were EN `286px`, ZH `260px`, HI `262px`. At 390x900, cards stacked in one column with increasing top positions, widths at least `320px`, no horizontal overflow, and card heights EN `260/286/286px`, ZH `235/260/260px`, HI `262/288/262px`. The response cards remained a three-card layout with rendered heights at least `218px` desktop/mobile, and the new module flowed directly into the footer without an unintended gap or overlap. This was a current rendered geometry/visual check, not a screenshot-only or CSS-only claim.

**Remaining failures:** none found in this scoped Package C Contact P0 repair. Existing dirty workspace changes in `ACCEPTANCE_TESTS.md`, `ISSUE_LEDGER.md`, and previous handoff entries predated this worker and were not reviewed, reverted, staged, committed, packaged, or synced. The form still intentionally uses `[BACKEND_CONTACT_ENDPOINT]` until a real endpoint is supplied; success toast remains gated behind `res.ok`.

**Notes for next handoff:** future Contact workers should keep the placeholder endpoint and hidden-field plumbing intact until a real endpoint is provided, but customer-visible copy must stay business-facing. If future QA scripts need to identify placeholder behavior, prefer non-visible comments or script assertions rather than restoring customer-facing implementation wording. Do not package until the user explicitly requests packaging/final delivery.

### 2026-05-20 CST - Package C Plans P0 closeout verification

**Files changed:** `SAMPORA_LATEST_REPAIR_HANDOFF.md` only by this closeout step. `sampora-website-public-v3/plans.html` was inspected and browser-verified but not modified because no scoped runtime geometry or visual acceptance failure was found. No `contact.html`, other pages, QA evidence outputs, delivery folders, zips, old public folders, staging, commit, push, or packaging files were intentionally changed.

**Audit item addressed:** Package C `plans.html` P0-1..P0-8 closeout after Bohr's implementation. The closeout verified the current page rather than trusting the prior implementation report: hero selector/version logic, plans/matrix leads, three plan cards, capability matrix, final CTA, footer grouping/email/legal, old wording removal, and EN/ZH/HI rendered copy stability.

**Verification run:** required intake files were read/checked first: `AGENTS.md`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, `ACCEPTANCE_TESTS.md`, `ISSUE_LEDGER.md`, and `SAMPORA_TERMINOLOGY_GLOSSARY.md`. Static recheck passed: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `git diff --check -- sampora-website-public-v3\plans.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> exit 0 with LF/CRLF warnings only before this handoff append. Runtime geometry used system Chrome `C:\Program Files\Google\Chrome\Application\chrome.exe` through CDP against the current `plans.html` file URL, with no screenshots or QA artifacts written. EN/ZH/HI at `1314x1270`, `1440x900`, and `390x900` all passed with document horizontal overflow `0`, no section overlaps, no empty `data-i18n` surfaces, no replacement characters, no repeated-question placeholder runs, and no current hits for `Survey Station`, `members + station`, `jiayu@surveysaas.com`, `MONITOR`, or `fake monitor`. Hero selector rendered `PLAN_LAYER_MAP` with version-logic rows for Panel / Supplier Network / Enterprise or localized ZH equivalents. Plans lead was visible in all 9 samples, with `38px` gap to cards. Plan cards rendered the `Best for` / `Operating scope` / `Upgrade trigger` structure, 3 scope items each, no card overlap, and desktop equal-height cards: 1314 EN/HI `730.83px`, ZH `651.19px`; 1440 EN/HI `705.88px`, ZH `630.23px`. Mobile card heights varied naturally by language/content but stayed stacked with no overlap: EN `701.84/747.05/751.75px`, ZH `630.23/630.23/694.31px`, HI `701.84/772/751.75px`. Matrix rendered 10 rows x 4 columns; desktop matrix width was `1120px` with no internal scroll, while 390px mobile used contained matrix scroll (`clientWidth 356`, `scrollWidth 960`) without document overflow. Final CTA remained visually separated from footer with `86px` desktop and `64px` mobile gap, and CTA hrefs stayed `contact.html?intent=start_trial#contact-form`, `contact.html?intent=book_demo#contact-form`, and `contact.html?intent=contact_sales#contact-form`. Footer rendered Product / Resources / Get started columns with 4 links each, `sales@sampora.com`, ZH legal `© 2026 安徽省嘉禹企业服务有限公司`, and EN/HI legal `© 2026 Anhui Jiayu Enterprise Service Co., Ltd.`.

**Remaining failures:** none found for Package C Plans P0 closeout. `plans.html` required no micro-adjustment. This was not packaging/final delivery work, so Lighthouse, axe, delivery sync, zip rebuild, contact-form behavior, and unrelated page checks were not run.

**Notes for next handoff:** future Plans workers should keep the three acceptance layers separate: technical/static PASS, runtime geometry PASS, and visual acceptance PASS. For Plans visual changes, rerun EN/ZH/HI at `1314x1270`, `1440x900`, and `390x900`, and verify hero selector/version logic, plan-card structure, matrix desktop/tablet/mobile behavior, final CTA intent links, footer grouping/email/legal, and absence of old survey-station/member-station wording from current rendered text.

### 2026-05-20 CST - Sampora About navigation intake implementation

**Files changed:** `sampora-website-public-v3/contact.html`, `sampora-website-public-v3/resource-manuals.html`, `sampora-website-public-v3/about_sampora_issues_fixed.html`, and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No page body, hero, footer, CTA policy, Contact form body/fields, `products.html`, `pricing.html`, QA screenshot output, delivery folder, zip, staging, commit, push, or packaging file was intentionally changed.

**Audit item addressed:** `[CONFIRMED]` the existing about page `about_sampora_issues_fixed.html` was not consistently reachable from all assigned navigation surfaces. Red check showed `contact.html` lacked a desktop About entry and mobile nav generator, `resource-manuals.html` lacked About in desktop/mobile nav, and the about page itself pointed nav/mobile nav to missing `about.html` instead of its current file. `products.html` and `pricing.html` were inspected as pure redirects and intentionally left unchanged.

**Verification run:** technical/static checks passed for the scoped nav surfaces: inline JS syntax extraction for `contact.html`, `resource-manuals.html`, and `about_sampora_issues_fixed.html`; source assertion that all seven nav pages contain `about_sampora_issues_fixed.html`; `git diff --check -- sampora-website-public-v3/contact.html sampora-website-public-v3/resource-manuals.html sampora-website-public-v3/about_sampora_issues_fixed.html SAMPORA_LATEST_REPAIR_HANDOFF.md` exited 0 with LF/CRLF warnings only; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> FAIL on pre-existing about-page footer/body forbidden patterns `2024-2025`, `© 2024`, and `All rights reserved`; those are outside this worker's no-footer/no-body scope and were not repaired.

**Runtime geometry / visual acceptance:** scoped Chrome/CDP check at `1440x900` and `390x900` across EN/ZH/HI covered `index.html`, `plans.html`, `about_sampora_issues_fixed.html`, `solutions.html`, `contact.html`, and `resource-manuals.html`. The three touched pages passed the nav-specific runtime/visual checks: `contact.html`, `resource-manuals.html`, and `about_sampora_issues_fixed.html` had `aboutHref=about_sampora_issues_fixed.html`, EN/ZH/HI About labels `About` / `关于` / `हमारे बारे में`, no document horizontal overflow, no desktop nav/action overlap, readable open mobile drawer at 390px, and the about page active href was `about_sampora_issues_fixed.html`. The broader required coverage did not fully pass: existing `index.html`, `solutions.html`, and one `plans.html` HI samples showed About label language state not matching the requested HI label during the same automated language-switch sequence.

**Remaining failures:** overall task status is FAIL, not delivery-complete. Remaining failures: `final-audit-static-check.mjs` fails on about-page footer/body legacy copyright text outside this scope, and broader nav runtime coverage found existing HI language-state mismatches on untouched `index.html`, `solutions.html`, and `plans.html`. This worker did not modify those pages because the newest instruction limited implementation to `contact.html`, `resource-manuals.html`, and `about_sampora_issues_fixed.html` and forbade broad/body/footer changes.

**Notes for next handoff:** if the controller wants full PASS for the requested cross-page geometry matrix, reassign a scoped language-state/nav worker for the existing HI nav switch behavior on `index.html`, `solutions.html`, and `plans.html`, plus a separate decision on whether about-page footer legacy copyright is now in scope. The touched about-entry surfaces are ready for reviewer inspection but should not be called fully accepted until those broader failures are resolved or explicitly excluded.

### 2026-05-20 CST - Sampora About Page P0 implementation

**Files changed:** `sampora-website-public-v3/about_sampora_issues_fixed.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No Footer structure, Footer links, Footer email, Footer visible copy, all-site navigation structure, language-switching logic, other HTML pages, QA evidence outputs, delivery folders, zips, staging, commit, push, or packaging files were intentionally changed. `ACCEPTANCE_TESTS.md` and `ISSUE_LEDGER.md` were intentionally left unchanged because existing Language/i18n, Forbidden copy, Visual acceptance, Scope, and I18N/TERMINOLOGY ledger coverage already covered this scoped About P0 repair and no recurring issue status changed.

**Audit item addressed:** `[CONFIRMED]` About page P0 copy and metadata repair from `C:\Users\liuge\Downloads\SAMPORA_ABOUT_PAGE_P0_CODEX_INSTRUCTIONS.md` read with UTF-8, applied to the actual current About file `sampora-website-public-v3/about_sampora_issues_fixed.html` rather than creating `about.html`. The page-level positioning was changed from survey-service / domestic qualification framing to international SaaS positioning: `Sampora is an online sample operations platform for panel providers and sample suppliers.` Updated areas include title/meta/OG/Twitter/JSON-LD descriptions, Hero title/body/tags, Hero console metrics and five workflow rows, Brand story, Operating foundation proof cards, Operating collaboration, Core capability, Operating principles/timeline, CTA, and matching EN/ZH/HI i18n values. All touched `data-i18n` default English HTML text was synchronized with `I18N.en`. Lightweight CSS was added only for proof label wrapping, Hindi proof sizing, Chinese balanced headings, and console row wrapping.

**Verification run:** red check before editing confirmed current old-positioning terms in About page source: `Survey SaaS` 12, `survey questionnaire service` 5, `100K+` 10, `48h` 11, `Multi-province` 8, `AI algorithms` 2, `marketing tools` 1, `security check products` 2, `security checks` 8, and `sample collection` 14. Post-repair scoped source check with Footer excluded passed for the required body/metadata old-positioning list: `survey questionnaire service`, `100K+`, `48h`, `Multi-province`, `AI algorithms`, `marketing tools`, `security check products`, and `security checks` all `0`; remaining `Survey SaaS` and `sample collection` hits are only in the unchanged Footer/footer i18n values that were explicitly out of scope. Mojibake guard by source grep passed for repeated question-mark placeholder runs and replacement characters in the About file.

**Technical/static PASS:** inline non-JSON-LD script syntax extraction -> `OK inline scripts: 1`; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `git diff --check -- sampora-website-public-v3/about_sampora_issues_fixed.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> PASS with LF/CRLF warning only. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> FAIL only on pre-existing/out-of-scope About Footer/body copyright patterns `2024-2025`, `© 2024`, and `All rights reserved`; this worker did not repair them because Footer was explicitly forbidden.

**Runtime/i18n PASS:** local static server plus system Chrome CDP verified `about_sampora_issues_fixed.html` at 1440x900 and 390x900 for EN/ZH/HI. Language switching updated Hero, Story, Proof, Operating collaboration, Core capability, Timeline, CTA, document title, meta description, OG/Twitter descriptions, and JSON-LD description. EN/ZH/HI samples had no empty touched sections, no mojibake markers, no forbidden body old-positioning terms outside the unchanged Footer, and no document horizontal overflow.

**Runtime geometry / visual acceptance PASS:** rendered checks passed at 1440x900 and 390x900 for EN/ZH/HI. Hero titles stayed inside viewport; console stayed inside viewport; desktop console row heights were EN/HI `76.5px` and ZH `51.3-76.5px`, with no cramped multi-column collapse; proof four cards were equal-height on desktop (EN `218.6px`, ZH `175.9px`, HI `214.7px`); timeline rendered as 2x2 on desktop with paired top alignment and second row below the first; mobile stacked proof/timeline/core cards in one column with overflow `0`. Mobile console rows naturally wrapped taller (EN/HI `135.9-161.1px`, ZH `137.9px`) but remained readable and contained.

**Remaining failures:** none for the scoped About P0 body/metadata/i18n/visual repair. Known out-of-scope failure remains the About Footer legacy copyright/static-audit blocker (`2024-2025`, `© 2024`, `All rights reserved`) plus unchanged Footer/footer i18n old terms such as `Survey SaaS` and `Sample collection`; these were explicitly not modified. This was not a packaging/final delivery task, so Lighthouse, axe, delivery sync, zip rebuild, and unrelated page checks were not run.

**Notes for next handoff:** About P0 should be reviewed as a scoped body/metadata/i18n repair, not as Footer cleanup. Future About workers must keep Footer/nav out of scope unless the controller explicitly opens them, and should use a local static server rather than `file://` for Chrome/CDP runtime checks to avoid readiness instability.

### 2026-05-20 CST - Package A homepage Role scenarios quote layout

**Files changed:** `sampora-website-public-v3/index.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No nav, hero, footer, CTA, visible copy/i18n, JavaScript, other pages, QA evidence outputs, delivery folders, zips, staging, commit, push, or packaging files were intentionally changed. Existing dirty diffs outside the scoped role-card CSS were preserved.

**Audit item addressed:** `[CONFIRMED]` Package A `index.html` `#customers` / Role scenarios quote-card vertical rhythm. The current CSS had `.quote` as a vertical flex container with `gap: 0`, `.customer-top` and `.quote .type` using bottom margins, and `.quote .impact` using `margin-top: 14px`, so the workflow line was not anchored to the card bottom. This round kept the DOM and text unchanged, gave `.quote` a stable vertical gap, removed the internal bottom-margin stacking, allowed `.quote p` to absorb available vertical space, and pinned `.quote .impact` with `margin-top: auto` plus a small top pad.

**Technical/static PASS:** `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. `git diff --check -- sampora-website-public-v3/index.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> exit 0 with LF/CRLF warnings only. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> FAIL only on pre-existing `about_sampora_issues_fixed.html` forbidden copyright patterns `2024-2025`, `© 2024`, and `All rights reserved`, which are outside this worker's Package A role-card scope.

**Runtime geometry PASS:** Chrome/CDP rendered `index.html` for EN/ZH/HI at `1440x900`, `1314x1270`, and `390x900`. All samples had no positive document horizontal overflow, no quote-card child overlap, no section/next-section overlap, and stable right proof-panel flow. `.impact` bottom gap was exactly `21px` for all three cards in all nine samples, with bottom-gap range `0px`. Desktop/1314 `.impact` bottom alignment delta was `0px`; card height ranges were `0px` for all desktop/1314 samples. Card heights: 1440 EN `356.22px`, ZH `332.7px`, HI `394.97px`; 1314 EN `394.97px`, ZH `356.22px`, HI `394.97px`; 390 EN `244.3px`, ZH `222.53px`, HI `226.59/252.39/252.39px`. The mobile HI height variation came from localized text length, while each card still kept its own `.impact` line pinned with the same `21px` bottom gap and no overlap.

**Visual acceptance PASS:** the user goal is satisfied in the rendered page, not only in CSS: the workflow `.impact` row now sits at a consistent tight bottom position, the card body occupies the available middle space instead of all content clustering at the top, three-card desktop rhythm is even, mobile remains stacked without overflow, and the right proof panel and following section are not disturbed.

**Remaining failures:** none for this scoped Package A Role scenarios layout repair. Existing unrelated dirty diffs and the pre-existing about-page static failure were not repaired, reverted, staged, packaged, or synced.

### 2026-05-20 CST - Package B Resources setup two-column visual rework

**Files changed:** `sampora-website-public-v3/resources.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No `index.html`, `solutions.html`, `plans.html`, `contact.html`, `resource-manuals.html`, QA evidence outputs, screenshots, delivery folders, zips, README/deployment docs, staging, commit, push, or packaging files were intentionally changed. `ISSUE_LEDGER.md` was intentionally left unchanged because `VISUAL-QA-001` remains a strategy guard and no new recurring issue was added.

**Audit item addressed:** `[CONFIRMED]` Package B `resources.html` `section#setup` two-column visual layout rework. The red browser check before editing found the current desktop left `.check-panel` was height-matched to the right panel but visually top-stacked: EN panel height `873px` with `218.17px` bottom void, ZH `799px` with `268.94px` bottom void, and HI `797.69px` with `237.81px` bottom void. The right `.trust-panel` contained 4 proof items, the mode strip, and 3 output rows, all technically inside the panel, but still read as many bordered small cards inside a large card.

**Implementation notes:** kept the two-column order and all required content counts unchanged: 6 left path rows, right heading, 4 proof items, mode strip, and 3 output rows. The left `.check-panel` now uses a desktop flex-column path rhythm so the six `.check-item` rows distribute through the panel instead of leaving a bottom hole, with a stacked mobile fallback to preserve natural content height. The right proof area now reads as a single evidence matrix with internal dividers, the output rows read as one list surface, and the mode strip remains the primary emphasized module. No visible copy, i18n keys, DATA setup/proof/proofOutput content, hero, nav, footer, topology console, or resource entry cards were intentionally changed in this round.

**Technical/static verification:** inline script syntax for `resources.html` -> `OK: 2 inline scripts`; `node sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs` -> PASS. `node sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs` -> FAIL on out-of-scope `about_sampora_issues_fixed.html` forbidden historical/footer patterns (question-mark placeholder run, `2024-2025`, `© 2024`, `All rights reserved`); this Package B worker did not repair that unassigned about-page blocker.

**Runtime geometry PASS:** post-repair Chrome/CDP geometry passed for EN/ZH/HI at desktop `1440x900` and mobile `390x900`: document overflowX stayed `0`; left setup list count stayed `6`; proof count stayed `4`; output row count stayed `3`; proof/output rows remained inside `.trust-panel`; and `.mode-fit-card` stayed before `.proof-output`. Desktop left/right panel top and height deltas were `0px`. Left bottom void dropped to `1px` in EN/ZH/HI. Desktop left row heights became intentionally distributed: EN about `142.2-143.22px`, ZH about `124.53-125.53px`, and HI about `124.31-125.31px`. Mobile stacked naturally with left panel heights EN `759.73px`, ZH `594.34px`, HI `608.78px`, bottom void `1px`, and no horizontal overflow.

**Visual acceptance PASS for scoped Resources setup:** rendered desktop review confirmed the left side now reads as a deliberate six-step operating path, not as a short list with a large empty lower half. The right panel now has a clearer hierarchy: heading text first, a quiet 2x2 proof matrix, a distinct mode strip, and a single output-list surface. The proof/output relationship is readable without the previous “many small cards inside a card” feel. Mobile EN/ZH/HI stack cleanly: the path list keeps natural row height, then the proof panel follows as grouped evidence and output, with no crammed text, overlap, or sideways overflow.

**Remaining failures:** none found for the scoped `resources.html` setup layout. Overall technical/static delivery is not fully green because `final-audit-static-check.mjs` currently fails on the separate about page noted above, outside this worker's write scope.

**Notes for next handoff:** future Resources visual changes should keep the left setup path and right proof/output hierarchy verified separately from overflow-only checks. If the controller needs a full static PASS, reassign the existing about-page final-audit blocker to an owner allowed to edit `about_sampora_issues_fixed.html`; do not treat it as a Package B Resources layout failure.

### 2026-05-20 CST - Package C Plans matrix visual rework

**Files changed:** `sampora-website-public-v3/plans.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No Plans hero, plan cards, final CTA, footer, nav, Contact/About/Resources/Index/Solutions, visible business copy, i18n content, QA screenshots, delivery folders, zips, staging, commit, push, or packaging files were intentionally changed. `ISSUE_LEDGER.md` was intentionally left unchanged because `VISUAL-QA-001` remains a strategy guard and no recurring issue state changed.

**Audit item addressed:** `[CONFIRMED]` Package C `plans.html` `#matrix` capability matrix visual readability. The user's current screenshot invalidated the old Plans P0 closeout visual PASS, so this worker treated the previous `1120px` matrix PASS as downgraded and reran current red checks before editing. Red browser evidence showed no document overflow, but desktop readability was still weak: matrix wrap `1120px`, plan columns about `290.7px`, centered body text, `14px 18px` cell padding, `19.43px` line-height, and dense multi-line rows; mobile correctly used internal matrix scroll but EN/HI dense rows still reached about 4.5 row-height units.

**Implementation notes:** adjusted only scoped matrix CSS. The desktop/tablet matrix now uses a wider `min(100%,1280px)` wrapper, `1040px` table minimum, a 20% dimension column plus wider plan columns, left-aligned body cells, `18px 24px` padding, `1.5` body line-height, slightly smaller `14.25px` body text, stronger first-column labels, and a subtle matrix surface background. No matrix content was removed or rewritten.

**Technical/static PASS for scoped Plans matrix:** inline script syntax extraction for `plans.html` -> `OK: 2 inline scripts`; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `git diff --check -- sampora-website-public-v3/plans.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> exit 0 with LF/CRLF warnings only before this handoff append. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` currently FAILS on pre-existing out-of-scope `about_sampora_issues_fixed.html` forbidden historical/footer patterns (question-placeholder run, `2024-2025`, `© 2024`, `All rights reserved`); this was not repaired because this worker's ownership is limited to Plans matrix CSS and handoff logging.

**Runtime geometry PASS:** fresh Chrome/CDP checks passed for EN/ZH/HI at `1440x900`, `1314x1270`, and `390x900`. Document/body horizontal overflow stayed `0` for all 9 samples. Desktop matrix wrap became `1280px` at 1440 and `1251px` at 1314; plan column widths improved to about `340.8px` and `333.1px`; header/body column width delta stayed `0px`; row overlap was false. Mobile kept contained internal scrolling only: wrap `clientWidth 356`, `scrollWidth 1040`, table width `1040`, document overflow `0`. Header/body alignment remained `0px` delta on mobile too.

**Visual acceptance PASS:** compared with the user's screenshot complaint, the rendered matrix now reads as a broader comparison table instead of narrow centered text blocks. Desktop EN/ZH/HI actual text line boxes maxed at 2 lines per body cell; mobile EN/HI longest cells reached 3 text lines inside the internal scroll table, while ZH maxed at 2. The visible hierarchy is calmer: first-column labels anchor each row, plan text is left-aligned for scanning, row height and padding are more open, and long business phrases no longer look forced into narrow centered stacks. This is a rendered browser acceptance, not a CSS-only or overflow-only claim.

**Remaining failures:** none found for the scoped `plans.html` matrix readability repair. Overall final-audit static remains blocked by the separate about-page issue noted above, outside this worker's scope. Lighthouse, axe, delivery sync, zip rebuild, Contact behavior, and unrelated page checks were intentionally not run.

**Notes for next handoff:** the older Package C Plans P0 closeout matrix PASS should not be reused as current visual proof. Future Plans matrix claims must include rendered EN/ZH/HI desktop/mobile evidence for width, body alignment, line count, padding/line-height, header/body alignment, no overlap, document overflow `0`, and mobile internal scroll behavior.

### 2026-05-20 CST - Package B Resources setup visual reviewer closeout

**Files changed:** `SAMPORA_LATEST_REPAIR_HANDOFF.md` only by controller closeout. No page source, QA evidence output, screenshot, delivery folder, zip, staging, commit, push, or packaging file was intentionally changed in this closeout.

**Audit item addressed:** read-only reviewer closeout for the user-requested `resources.html` `section#setup` two-column visual rework. The reviewer reported **Setup Visual Result: PASS** for the screenshot-scoped left/right layout, but **Overall: FAIL** for clean task scope because the current working-tree diff for `resources.html` also contains older non-setup changes such as hero resource-entry cards, footer description, detail links, and About navigation.

**Verification run:** controller reconciled the reviewer scope note against the current handoff. The non-setup Resources hero cards, footer description, `detailLinks`, proof/output copy, and entry-card follow-ups are already tracked under earlier Package B/D Resources entries. The Resources `nav About` diff belongs to the broader About-navigation/About-page workstream and should not be claimed as part of this setup visual repair. Scoped checks from the read-only reviewer passed: EN/ZH/HI at `1440x900` and `390x900` had `overflowX=0`; left setup count stayed `6`; right proof count stayed `4`; output row count stayed `3`; output rows remained inside `.trust-panel`; `.mode-fit-card` stayed before `.proof-output`; mobile stacked naturally. `git diff --check -- sampora-website-public-v3/resources.html SAMPORA_LATEST_REPAIR_HANDOFF.md` passed with LF/CRLF warnings only, and `node sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs` passed. `node sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs` still fails on out-of-scope `about_sampora_issues_fixed.html` historical/footer patterns, not on this Resources setup layout.

**Visual acceptance PASS for screenshot-scoped setup area:** the reviewer confirmed the rendered left panel no longer reads as a top-stacked six-row list with a large empty lower half. Desktop EN/ZH/HI left/right panel height deltas were `0px`, left bottom void was `1px`, and row heights were intentionally distributed without becoming empty strips. The right panel reads as a clearer hierarchy: heading, quiet 2x2 proof matrix, distinct mode strip, and one output-list surface rather than many small cards crammed into a large card. Mobile EN/ZH/HI remained readable without overlap or sideways overflow.

**Remaining failures:** none for the screenshot-scoped `section#setup` visual repair. Remaining non-setup/workspace failures are separate: the current `resources.html` diff includes prior Resources and About-navigation changes that must be judged under their own handoff entries, and `final-audit-static-check.mjs` remains blocked by the out-of-scope About page legacy/footer static-audit issue.

**Notes for next handoff:** do not reopen this `section#setup` visual repair unless fresh rendered evidence shows a new left/right layout problem. Do not treat a broad `git diff resources.html` as proof that the setup repair overreached; first separate previously logged Resources entry-card/footer/detail-link changes and the separate About-navigation workstream. If the next task is final delivery, resolve or explicitly classify the About page final-audit blocker before claiming global PASS.

### 2026-05-20 CST - Package B Resources proof-output interrupted micro-rework and topology carousel restore

**Files changed:** `sampora-website-public-v3/resources.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No other page, visible copy, DATA/i18n semantics, left setup list, topology SVG markup, QA evidence output, screenshot directory, delivery folder, zip, staging, commit, push, or packaging file was intentionally changed.

**Audit items addressed:** `[CONFIRMED]` Package B `resources.html` hero topology modebar/card carousel no longer auto-rotated. Current source diagnosis found only click handlers for `#topoModebar button` and `.topo-svg .node[data-mode]`; there was no active timer to advance `state.mode`, so the console could remain stuck on the current Project/Supplier/Settlement card. A small carousel controller was added with `modeOrder`, `setTopologyMode()`, and `restartTopologyCycle()` to advance `setup -> supplier -> finance` every `3600ms`; manual card/node selection resets the cycle, and the cycle pauses while the document is hidden or the topology modal is open.

**Interrupted proof-output note:** an earlier same-round proof-output micro-rework worker was interrupted after writing scoped `.proof-output*` CSS but before completing validation or handoff. Its source changes remain limited to `.proof-output`, `.proof-output-title`, `.proof-output-row`, and `.proof-output-num`: the output section was changed toward a lighter in-panel list instead of a bordered nested card. This note records the interruption; do not claim final visual acceptance for that output micro-rework without fresh rendered review against the user's selected output region.

**Technical/static PASS:** inline script syntax extraction for `resources.html` passed with `OK inline scripts: 3`; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `git diff --check -- sampora-website-public-v3/resources.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> PASS with LF/CRLF warnings only.

**Runtime geometry / motion PASS for topology carousel:** custom Chrome/CDP no-screenshot runtime sampling checked `resources.html` at `1967x1270`, `1440x900`, and `390x900`. All three viewports advanced through `supplier -> finance -> setup -> supplier` over timed samples. Each sample had exactly one active modebar button, `#consoleTitle` changed with the active mode (`SUPPLIER Allocation`, `SETTLEMENT Records`, `PROJECT Intake`), active flow animation was `topoDash`, dash offsets changed across samples, and document horizontal overflow stayed `0`.

**Remaining failures:** none found for the scoped topology carousel restore. The proof-output micro-rework remains not fully accepted because it was interrupted before post-change rendered validation. Existing out-of-scope About page final-audit failures remain separate and were not touched.

**Notes for next handoff:** if the user asks about the selected `What this path produces` region again, treat it as an open visual-review item until a fresh EN/ZH/HI rendered pass is completed. If future Resources topology work changes `renderTopology()`, modebar rendering, node click handlers, or modal behavior, re-run timed carousel sampling at `1967x1270`, `1440x900`, and mobile `390x900`; screenshot-only evidence is not enough.

### 2026-05-20 CST - Package B Solutions console tag visual consistency

**Files changed:** `sampora-website-public-v3/solutions.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No DATA, HERO_UI, I18N text, currentFit routing, carousel, modebar structure, topology SVG structure, ticker content, navigation, footer, QA evidence output, delivery folder, zip, staging, commit, push, or packaging file was intentionally changed. Existing dirty diffs outside the scoped console-tag CSS were preserved.

**Audit item addressed:** `[CONFIRMED]` Package B `solutions.html` hero `.solution-console` top `.console-tags .tag` visual inconsistency after switching EN/ZH/HI on the same `PNL_01` layer. Red browser check before editing showed EN tags at `48.5px` because they wrapped into two rows, while ZH and HI tags stretched to the full `105px` `.console-tags` min-height on one line, producing oversized round/oval pills despite matching DOM, active flows, active packets, and active nodes.

**Technical/static PASS:** `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. `git diff --check -- sampora-website-public-v3/solutions.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> PASS with LF/CRLF warnings only. `ISSUE_LEDGER.md` was intentionally left unchanged because `VISUAL-QA-001` remains a strategy guard and `I18N-001` was only a language-switch regression guard for this scoped visual repair.

**Runtime geometry PASS:** Chrome/CDP rendered `solutions.html` for EN/ZH/HI at `1440x900` and `390x900`, forced `PNL_01`, and checked tag rectangles, overlap, document overflow, console bounds, active topology state, and modebar/topology/ticker order. Desktop tag heights are now EN `29/29/29px`, ZH `31/31/31px`, and HI `30/32/30px`; before repair ZH/HI were `105px`. Desktop and mobile document horizontal overflow was `0`, console stayed inside the viewport, tags did not overlap, and `modebar -> topology -> ticker` order stayed unchanged. Active topology state stayed `3` flows, `3` packets, and `1` active/warn node for all sampled languages.

**Visual acceptance PASS:** the rendered console now shows the same small pill visual language across EN/ZH/HI. Chinese and Hindi no longer become large round or oval badges when their three tags fit on one row; English keeps the same small-pill treatment while naturally wrapping to a second line where its longer labels need it. Mobile EN/ZH/HI remained readable, non-overlapping, and contained with no sideways page overflow.

**Remaining failures:** none for this scoped `solutions.html` console-tag visual repair. Broader About-page static blockers and unrelated existing dirty diffs remain outside this task and were not repaired, reverted, packaged, or synced.

### 2026-05-20 CST - Package B Solutions compare table implementation-only patch

**Files changed:** `sampora-website-public-v3/solutions.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. `sampora-website-public-v3/plans.html` was checked and intentionally not edited because its `#matrix` CSS already has the equivalent width, min-width, centered non-first columns, and `18px 28px` cell-padding strategy. No nav, hero, footer, CTA, topology, Operating path, visible copy/i18n, other pages, QA screenshots, delivery folders, zip/package, staging, commit, push, or packaging files were intentionally changed.

**Implementation-only note:** the user's screenshot/feedback overturned the older visual PASS for this comparison-table surface. This round did only the exact CSS implementation patch requested by the controller plus static checks. It must not be treated as final completion: runtime geometry and real visual perception / 真实观感 visual acceptance still require an independent read-only reviewer/browser pass.

**Patch summary:** `#compare .compare-wrap` now uses full-width containment, internal horizontal scrolling, hidden vertical overflow, and the same darker gradient surface strategy as the Plans matrix. `#compareTable` now uses `width:100%`, `min-width:1080px`, fixed table layout, a 20% first column, three 26.666% comparison columns, `18px 28px` cell padding, `1.5` body line-height, top vertical alignment, break-word wrapping without forced word breaks, `12px/1.25` uppercase mono headers, `14.25px` body text, and stronger first-column labels.

**Verification run:** inline JS syntax extraction/check for `plans.html` and `solutions.html` passed; `node "sampora-website-public-v3/qa-evidence/page-layer-static-check.mjs"` passed; `git diff --check -- sampora-website-public-v3/plans.html sampora-website-public-v3/solutions.html SAMPORA_LATEST_REPAIR_HANDOFF.md` passed with LF/CRLF warnings only.

**Remaining failures / next agent:** runtime geometry PASS and real visual perception / 真实观感 visual acceptance are not complete in this worker by user instruction. Next reviewer should render `solutions.html` compare table across EN/ZH/HI desktop and mobile, compare it to the user's screenshot target and Plans matrix baseline, and explicitly report whether the table now looks meaningfully wider, less cramped, better aligned, and readable rather than merely CSS-matching.

### 2026-05-20 CST - Controller visual acceptance for Plans and Solutions comparison tables

**Files changed:** `SAMPORA_LATEST_REPAIR_HANDOFF.md` only by this controller closeout. No page source, QA evidence output, screenshots, delivery folders, zip/package, staging, commit, push, or packaging files were intentionally changed in this verification step.

**Audit item addressed:** read-only browser acceptance after the user's current screenshots overturned the previous comparison-table visual PASS. This closeout verifies the current `plans.html` `#matrix` table and `solutions.html` `#compare` table after the implementation-only CSS patch. The acceptance target was the user's real visual standard: table frames should use the available section width, cell text should keep comfortable inset from the inner borders, and non-first-column headers such as `ENTERPRISE` must align to the visual center of the body text below rather than to the first letter.

**Verification run:** controller used a temporary local static server and system Chrome CDP, then removed the temporary browser profile and stopped helpers. Samples covered `plans.html` and `solutions.html` across EN/ZH/HI at `1440x900`, `1314x1270`, and `390x900`. Both pages had document/body horizontal overflow `0` in all 18 samples. Desktop/tablet table frames now match the section container width: Plans `1360/1360px` at 1440 and `1251/1251px` at 1314; Solutions `1360/1360px` at 1440 and `1251/1251px` at 1314. Mobile keeps contained internal scroll only: wrap `358px`, client `356px`, table/scroll width `1080px`, document overflow `0`. Cell padding computed as `18px 28px` on both pages. Non-first-column header/body text-center deltas measured `0px` for all sampled languages and viewports; header-to-cell-center deltas were also `0px`. Minimum text-to-cell-edge gaps stayed comfortable: Plans non-first columns were at least `28.4px` desktop/tablet and `29px` mobile; Solutions were at least `31.6px` desktop/tablet and `30.7px` mobile. Max body text line counts stayed within readable bounds: Plans EN/HI reached `1.8` desktop/tablet and `2.8` mobile in the longest Enterprise cells; ZH stayed `0.8-1.8`; Solutions stayed `0.8-1.0` desktop/tablet and at most `1.8` mobile EN.

**Runtime geometry PASS:** current rendered geometry passes for both comparison tables across the sampled EN/ZH/HI desktop/tablet/mobile viewports. Frames are full-width against their content containers, document overflow is `0`, mobile scrolling is confined to the table wrapper, and header/body text centers align at `0px` delta for the comparison columns.

**Visual acceptance PASS:** against the user's screenshot feedback, both tables now read as full-width comparison surfaces instead of narrow centered tables. The cell content keeps visible breathing room from borders through `18px 28px` padding plus measured text-edge gaps above about `28px`. `ENTERPRISE` and the other comparison headers sit on the same visual center line as the text blocks below, not on the body text's first-letter edge. This is a current rendered browser acceptance with real visual perception / 真实观感 evidence, not a CSS-only or overflow-only claim.

**Remaining failures:** none for the scoped Plans/Solutions comparison-table visual acceptance. This does not resolve the separate About-page final-audit/static blocker or the `solutions.html` non-fullscreen vertical jitter item, which remains `[VERIFY-FIRST]` because the earlier read-only investigation did not obtain a valid browser time series.

### 2026-05-20 CST - Package B Resources/Solutions topology manual-lock implementation pending runtime review

**Files changed:** `sampora-website-public-v3/resources.html`, `sampora-website-public-v3/solutions.html`, and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No visible copy/i18n semantics, screenshots, QA evidence JSON, delivery folder, zip/package, staging, commit, push, or packaging files were intentionally changed in this controller note.

**Audit items addressed:** `[CONFIRMED]` Package B `resources.html` topology modebar/card carousel kept force-rotating after manual selection, and `[CONFIRMED]` Package B `resources.html` topology line-flow looked visibly step-like during automatic mode changes. Read-only verification found manual clicks called `setTopologyMode(...)` with the default cycle-restart path, while motion sampling showed automatic transitions reset dash offset to `0`, jumped packet paths, and rebuilt `#topoModebar`, creating visible stutter. The user's request also required other rotating card effects to reuse the manual-stop rule; `solutions.html` topology selectors were included because they already had a `userSelectedFit` lock path but modebar/node interactions were not using it.

**Implementation notes:** `resources.html` now has a topology manual-lock path: modebar clicks plus SVG node click/Enter/Space call `setTopologyMode(..., { userSelected: true })`, clear the cycle interval, and block future automatic rotation after manual selection. Initial automatic rotation is still started on page load. `renderModebar()` now avoids rebuilding the modebar DOM when the language/mode signature is unchanged and instead updates active state/`aria-pressed`, reducing transition churn. `solutions.html` modebar and SVG node click/keyboard selection now call `setTopologyFit(..., { userSelectedFit: true })`, and `startTopologyRouteCycle()` respects the existing user-lock flag.

**Verification run:** inline script parse check for `resources.html` and `solutions.html` -> PASS (`3` scripts each). `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. Source inspection confirmed manual handlers use the user-lock path in both files. Browser/CDP runtime verification was attempted but not completed in this controller step because the local Node environment lacked Playwright/puppeteer/CRI packages and the temporary headless Chrome debug endpoint did not become reachable.

**Remaining failures / next agent:** runtime behavior and real visual perception / 真实观感 acceptance are still incomplete for this topology manual-lock/motion task. Next reviewer must use a working browser path to verify: `resources.html` auto-rotates before manual interaction, then stays fixed for more than `3600ms` after modebar click, SVG node click, and keyboard selection; `solutions.html` auto-rotates before manual interaction, then stays fixed after modebar/node selection; Resources active flow still animates and the mode transition no longer has the obvious step-like pause. Until that browser review passes, do not claim this topology task fully complete.

### 2026-05-20 CST - Package B Resources/Solutions topology manual-lock runtime closeout

**Files changed:** `sampora-website-public-v3/resources.html`, `sampora-website-public-v3/solutions.html`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, and `ISSUE_LEDGER.md`. No visible copy/i18n semantics, screenshots, QA evidence JSON, delivery folder, zip/package, staging, commit, push, or packaging files were intentionally changed.

**Audit items addressed:** runtime closeout for the immediately previous topology manual-lock/motion task. The controller completed this browser review directly because the implementation worker reported browser tooling was blocked and a narrow follow-up worker became unresponsive before returning evidence.

**Implementation notes:** `resources.html` keeps initial automatic cycling, but modebar click, SVG node click, and SVG node Enter/Space now call the manual selection path, clear the cycle interval, and prevent later forced auto-rotation. `resources.html` `renderModebar()` now updates active/ARIA state without rebuilding the modebar when the language/mode signature is unchanged. The Resources `topoDash` keyframe target is now `-68`, matching four full periods of the current `stroke-dasharray: 4 13` pattern, so the CSS loop no longer has the previous non-periodic phase jump. `solutions.html` modebar and SVG node mouse/keyboard selection now reuse the existing `userSelectedFit` lock path, and `startTopologyRouteCycle()` respects that lock.

**Technical/static PASS:** inline script parse check passed for `resources.html` and `solutions.html` (`3` scripts each). `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. `git diff --check -- sampora-website-public-v3/resources.html sampora-website-public-v3/solutions.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> PASS with LF/CRLF warnings only before this handoff append.

**Runtime behavior PASS:** system Chrome CDP rendered file URLs directly. `resources.html` desktop `1440x900`: initial `supplier` auto-advanced to `finance`, then to `setup`; active modebar count stayed `1`, `topoDash` stayed active, document overflow stayed `0`, and modebar mutation sampling during an auto transition reported `childList: 0`, `attr: 0`. After manual modebar click, SVG node click, and SVG node keyboard selection, the selected mode/title remained unchanged for more than `4200ms` while dash offsets continued moving. `resources.html` mobile `390x900` also auto-advanced first, then stayed locked after manual modebar selection with overflow `0`. `solutions.html` desktop `1440x900` and mobile `390x900` both auto-advanced first, then stayed locked after modebar selection and SVG node selection for more than `3500ms`, with `topoDash` still active and document overflow `0`.

**Motion / visual acceptance PASS for scoped topology issue:** the user-facing forced-rotation problem is no longer visible after manual selection: a normal user who clicks a Resources or Solutions topology card/mode no longer sees the card overridden by the timer. For the Resources line-flow stutter, the two confirmed visible causes were removed: automatic transitions no longer rebuild the modebar DOM, and the dash animation now loops on a full dash-pattern period (`4px + 13px = 17px`, keyframe `-68`). Rendered browser samples still show active line motion after selection and during auto cycling. No remaining scoped failure was found for the manual-lock/topology-flow task.

**Remaining failures / next agent:** none for this scoped Resources/Solutions topology manual-lock and Resources dash-loop smoothness closeout. This does not resolve separate non-topology items already recorded in this handoff, including the Resources proof-output interrupted visual review, the Solutions non-fullscreen vertical jitter `[VERIFY-FIRST]` item, or the About-page final-audit blocker.

### 2026-05-20 CST - Controller closeout for remaining verification-only items

**Files changed:** `SAMPORA_LATEST_REPAIR_HANDOFF.md` only in this closeout step. No page source, visible copy/i18n semantics, screenshots, QA evidence JSON, delivery folder, zip/package, staging, commit, push, or packaging files were intentionally changed.

**Audit items addressed:** verification closeout for three previously open items: the interrupted `resources.html` proof-output visual review, the `solutions.html` non-fullscreen vertical-jitter `[VERIFY-FIRST]` item, and the separate About-page final-audit blocker.

**Verification run:** `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `git diff --check -- sampora-website-public-v3/resources.html sampora-website-public-v3/solutions.html SAMPORA_LATEST_REPAIR_HANDOFF.md ISSUE_LEDGER.md` -> PASS with LF/CRLF warnings only before this handoff append. Browser/CDP verification rendered current file URLs directly.

**Resources proof-output runtime geometry PASS:** `resources.html` `#setup .proof-output` was sampled across EN/ZH/HI at desktop `1440x900` and mobile `390x900`. All six samples had document overflow `0`, `rowCount=3`, `.proof-output` fully inside `.trust-panel`, `.proof-output` after `.mode-fit-card`, all rows inside the panel, and `rowOverlap=false`. Desktop proof-output heights stayed within the panel: EN `312.28px`, ZH `268.03px`, HI `268.03px`; mobile proof-output heights were EN `388.78px`, ZH `322.41px`, HI `344.53px`, all contained.

**Resources proof-output visual acceptance PASS:** the current rendered output section reads as a single in-panel output list below the mode-fit card, not as an unfinished floating/nested card. The three numbered rows are separated, aligned, and contained across EN/ZH/HI desktop/mobile, with no crammed overlap, no sideways overflow, and no row escaping the right proof panel. This closes the earlier interrupted proof-output visual review without changing source in this closeout step.

**Solutions non-fullscreen jitter verification PASS / not reproduced:** `solutions.html` was sampled across EN/ZH/HI at `1280x932` and `390x900` while switching `panel`, `network`, and `enterprise`. Desktop EN/ZH/HI hero, grid, console, window, `#topoText`, `#topoModebar`, and next-section top/height deltas were all `0px`. Mobile EN and HI deltas were all `0px`; mobile ZH had only `1px` max top/height delta across hero/grid/console/window/`#topoText`/`#topoModebar`/next section. All samples had overflow `0`. The previous non-fullscreen vertical-jitter item is therefore not reproduced in current files and should not be repaired unless fresh browser evidence shows a visible current failure.

**Remaining failures:** none for the scoped verification-only items above. Current full final-audit static is PASS. Packaging, zip rebuilds, Lighthouse/axe, and delivery sync were intentionally not run because the user has not requested final packaging.

### 2026-05-20 CST - Controller closeout for pending visual/topology reviews

**Files changed:** `SAMPORA_LATEST_REPAIR_HANDOFF.md` and `ISSUE_LEDGER.md` only by this controller closeout. No page source, visible copy/i18n, CSS/JS behavior, QA evidence output, screenshots, delivery folder, zip/package, staging, commit, push, or packaging file was intentionally changed in this closeout. A Package B Resources topology implementation worker was interrupted after the user clarified the expected behavior and reported that it had made no file changes.

**Audit items closed:** pending verification items for Package B Resources `proof-output`, Package B Resources/Solutions topology manual-lock runtime behavior, Package B Solutions comparison table visual acceptance, and the earlier Solutions non-fullscreen vertical-jitter `[VERIFY-FIRST]` note.

**User clarification:** manual selection is expected to stop/lock topology rotation. A modebar or topology-node click should keep the chosen card/mode fixed and should not resume automatic cycling. Do not repair this lock-out behavior unless the user explicitly reverses this rule.

**Resources proof-output visual acceptance PASS:** read-only reviewer Hume verified current `resources.html` `section#setup` across EN/ZH/HI at `1967x1270`, `1440x900`, and `390x900`. The left/right desktop top delta and height delta were `0px`, left bottom void `1px`, right bottom void `29px`, output remained inside `.trust-panel`, `.mode-fit-card` stayed before `.proof-output`, counts stayed 6 left checks / 4 proof items / 3 output rows, and `.proof-output` had `border: 0` with transparent background. The reviewer judged the real visual perception PASS: right-side hierarchy reads as heading -> 2x2 proof matrix -> mode strip -> lightweight output list, not nested card clutter.

**Resources topology runtime PASS under corrected manual-lock rule:** controller Chrome/CDP sampling confirmed natural auto-rotation still works and manual selection intentionally locks. Natural `resources.html` sequence at `1440x900` advanced `supplier -> finance -> setup -> supplier`; each sample had exactly one active button, one active flow group, one active packet group, three active packets, `topoDash`, changing dash offsets, moving packet centers, and non-positive horizontal overflow. Manual modebar selection stayed fixed on `setup` across more than `7200ms` while dash/packet motion continued. Manual SVG node selection stayed fixed on `supplier` across more than `7200ms` while dash/packet motion continued. Because the user clarified manual lock is desired, this is now PASS. The current default first visible mode is supplier; no user requirement currently says the first card must be setup.

**Solutions topology runtime PASS and vertical-jitter not reproduced:** controller Chrome/CDP sampling verified `solutions.html` across EN/ZH/HI at desktop `1440x900` and mobile `390x900`. Natural auto sequences were `panel -> network -> enterprise -> panel` for every sampled language/viewport. Desktop modebar manual selection stayed fixed on `network` across EN/ZH/HI for more than `6600ms` while active `topoDash`, active packets, and packet movement continued. Mobile SVG node selection stayed fixed on `enterprise`; opening the topology modal creates a clone, so active counts are duplicated there, but the chosen mode remains locked and motion continues. Horizontal overflow was `0` or non-positive in every sample. The prior Solutions non-fullscreen vertical-jitter item is not reproduced: desktop hero/console/next/topoText/modebar deltas were `0px`; mobile deltas were `0px` except ZH `hero/console/next` at `1px`, below a visible-jitter threshold.

**Solutions compare table visual acceptance PASS:** read-only reviewer Epicurus verified `solutions.html` `#compare` / `#compareTable` across EN/ZH/HI at `1440x900`, `1314x1270`, and `390x900`. Document overflow was `0`, desktop/tablet compare-wrap and table used the available width, header/body alignment deltas were `0px`, body text maxed at 1-2 lines depending on viewport/language, no overlap was found, and mobile horizontal scroll stayed inside the table wrapper only. The reviewer judged real visual perception PASS: the table is meaningfully wider, less cramped, better aligned, and readable.

**Static/guard verification:** `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. A previous same-turn check had found About-page footer copyright patterns, but a fresh rerun after the parallel About work reported `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS and `Select-String` found no remaining `2024-2025`, `© 2024`, or `All rights reserved` hits in `sampora-website-public-v3/about_sampora_issues_fixed.html`. `ISSUE_LEDGER.md` records this as `ABOUT-001` `[REMOVED]` / verify absence only.

**Remaining failures:** no remaining pending visual/topology verification failures for the scoped Resources proof-output, Resources/Solutions topology manual-lock behavior, Solutions compare table, or Solutions vertical-jitter item. No current final-audit static failure reproduced in the fresh rerun. Existing dirty delivery folders/zips and QA evidence outputs were not reviewed, reverted, rebuilt, staged, committed, packaged, or synced.

**Pitfall / symptom:** controller/reviewer misclassified manual topology selection lock as a failure.
**Root cause:** the previous verification assumed manual interaction should reset and continue carousel cycling, while the user's intended behavior is that a manual card/node click stops the carousel on the selected item.
**Prevention rule:** topology reviews must separate natural automatic cycling from manual-selection behavior. Natural cycling should be time-sampled; manual click/keyboard behavior should be verified against the user's current rule before being called a bug.
**Verification:** Resources natural sequence `supplier -> finance -> setup -> supplier`; Resources manual modebar/node selections stayed fixed for more than `7200ms` with active dash/packet motion; Solutions natural sequence `panel -> network -> enterprise -> panel`; Solutions manual modebar/node selections stayed fixed with active dash/packet motion.
**Owner package:** Package B topology review / controller evidence synthesis.

### 2026-05-20 CST - Package B Resources hero/topology desktop alignment

**Files changed:** `sampora-website-public-v3/resources.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No visible copy, i18n/DATA text, render JS, SVG topology markup, carousel logic, navigation, footer, QA evidence output, delivery folder, zip/package, staging, commit, push, or packaging file was intentionally changed.

**Audit item addressed:** `[CONFIRMED]` Resources hero left-column resource-card bottom did not align with the right `#topoStage` bottom in ZH/HI, and the prior desktop `#topoStage` height repair was English-only. The EN-only `html[lang="en"] .resource-console:not(.modal-open) .topo-stage` rules were replaced with language-neutral desktop rules, and desktop hero spacing variables now distribute the required extra space across eyebrow-to-title, title-to-subtitle, subtitle-to-CTA, and CTA-to-cards gaps.

**Technical/static PASS:** `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. Source inspection found no remaining EN-only `.resource-console:not(.modal-open) .topo-stage` rule.

**Runtime geometry PASS:** system Chrome CDP rendered `resources.html` via file URL and clicked the real language buttons after `document.fonts.ready`. Desktop bottom deltas for `.resource-entry-grid.bottom - #topoStage.bottom` were within target: `1440x900` EN `-0.22px`, ZH `-7.53px`, HI `-4.2px`; `1600x1000` EN `0.47px`, ZH `-7.53px`, HI `-4.2px`; `1365x768` EN `7.33px`, ZH `4.56px`, HI `3.27px`. Desktop and mobile document/body horizontal overflow stayed `0`. Mobile `390x900` retained stacked behavior with no forced desktop equal-height alignment.

**Visual acceptance PASS:** rendered ZH/HI desktop gaps now read as deliberately distributed vertical rhythm rather than one isolated oversized gap: `1440/1600` ZH gaps are `66/50/62/68px`, HI gaps are `62/46/58/63px`, and `1365` ZH/HI use smaller but still even spacing. The three resource-card lower borders visually land on the same baseline as the topology stage within the requested tolerance, while mobile remains readable and stacked.

**Remaining failures / next agent:** none for this scoped `resources.html` hero/topology desktop alignment repair. This does not claim final delivery packaging, Lighthouse/axe, zip rebuild, or unrelated Resources/Solutions/About workstreams.

### 2026-05-20 CST - Package E short write-lock policy update

**Files changed:** `AGENTS.md`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, `ACCEPTANCE_TESTS.md`, and `ISSUE_LEDGER.md`. No page source, visible copy/i18n semantics, QA evidence output, screenshots, delivery folder, zip/package, staging, commit, push, or packaging files were intentionally changed.

**Strategy item addressed:** user clarified that concurrent-agent protection should lock only during the actual file-write window and unlock immediately after writing. The policy now distinguishes short write locks from read-only verification/review: implementation agents must check latest handoff/current diff for active or recent overlapping ownership before writing, announce the exact file/selector/function region, write only if there is no overlap, and release the lock immediately after the write completes. Browser checks, grep/static checks, visual review, and evidence synthesis remain read-only and do not hold write locks.

**Verification run:** docs/process policy update only. `ISSUE_LEDGER.md` now includes `HANDOFF-006` as a `[STRATEGY]` guard, and `ACCEPTANCE_TESTS.md` includes `[BLOCKED: overlapping write ownership]` as the no-edit status for overlapping writers.

**Remaining failures:** none for this docs/process policy update. Future implementation prompts must include the short write-lock rule; overlapping same-file/selector/function writers must stop as `[BLOCKED: overlapping write ownership]` instead of attempting to merge or overwrite.

### 2026-05-20 CST - Controller global closeout: About static blocker + Solutions jitter loop

**Files changed:** `sampora-website-public-v3/about_sampora_issues_fixed.html`, `sampora-website-public-v3/solutions.html`, `ISSUE_LEDGER.md`, and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No other page/source package, QA screenshot output, delivery folder, zip/package, staging, commit, push, or packaging file was intentionally changed.

**Items addressed:**
- `[CONFIRMED]` `ABOUT-001` final-audit static blocker on about page footer legacy copyright copy.
- `[CONFIRMED]` Solutions non-fullscreen vertical jitter loop (formerly verify-first), now tracked as `STICKY-003`.

**Implementation summary:**
- `about_sampora_issues_fixed.html`: removed legacy footer patterns (`2024-2025`, `© 2024`, `All rights reserved`) from fallback + EN/ZH/HI `footerCopyright`; aligned to 2026 legal company copy; fixed footer About link to `about_sampora_issues_fixed.html`.
- `solutions.html`: updated `syncTopChrome()` with hysteresis (`>56` enter condensed, `<=20` exit) to stop threshold ping-pong caused by layout-shifted `scrollY`.

**Verification run:**
- `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS.
- `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS.
- `git diff --check -- sampora-website-public-v3/solutions.html sampora-website-public-v3/about_sampora_issues_fixed.html ISSUE_LEDGER.md SAMPORA_LATEST_REPAIR_HANDOFF.md` -> PASS with LF/CRLF warnings only.
- Browser time-series sampling (system Chrome + local static server):
  - Pre-fix `solutions.html` at `1314x900` near threshold reproduced loop: frame-level alternation `scrollY 58 <-> 23`, `mastTop 35 <-> 0`, `nav-condensed false <-> true`.
  - Post-fix same sampling: `toggles=0`, stable frame series, no oscillation loop.
  - Full-page same-method scan (`index`, `solutions`, `resources`, `plans`, `contact`, `resource-manuals`, `about_sampora_issues_fixed`) found no other confirmed oscillation loop.

**Acceptance status separation:**
- `technical/static PASS`: yes (both static scripts PASS).
- `runtime geometry PASS`: yes (post-fix Solutions frame sampling stable, no loop; cross-page scan no confirmed same-type loop).
- `visual acceptance PASS`: yes for the jitter issue (non-fullscreen vertical jump loop no longer visible under reproduced trigger path).

**Remaining failures:** none for this scoped closeout. Packaging/final delivery zip rebuild remains deferred until explicit user request.

### 2026-05-20 CST - Package A homepage desktop HI About nav label

**Files changed:** `sampora-website-public-v3/index.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No `solutions.html`, `plans.html`, About page, QA evidence output, delivery folder, zip/package, staging, commit, push, or packaging file was intentionally changed.

**Implementation notes:** short write-lock scope was `sampora-website-public-v3/index.html` footer/chrome helper `dict.hi.about` in the `[data-sampora-nav]` relabeling region. Red source check found the main `navAbout` HI translation was already `हमारे बारे में`, but the later chrome/footer helper still had `hi.about = 'About'`, overwriting only the desktop nav after language switching. Updated only that helper value; EN and ZH helper labels remain `About` and `关于`.

**Verification run:** inline `index.html` script parse -> `OK: 4 inline scripts parsed`; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `git diff --check -- sampora-website-public-v3/index.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> PASS with LF/CRLF warnings only. Chrome CDP browser checks rendered `index.html` directly: desktop `1440x900` EN About=`About`, ZH About=`关于`, HI About=`हमारे बारे में`, HI href=`about_sampora_issues_fixed.html`, active HI state correct, overflow `0`; mobile `390x900` HI drawer About=`हमारे बारे में`, href correct, active HI state correct, overflow `0`.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; visual/language acceptance PASS for the scoped homepage nav label because the rendered desktop HI nav visibly shows the intended Hindi About label without layout overflow, while EN/ZH and mobile HI remain unchanged.

**Remaining failures / next agent:** none for this scoped Package A homepage desktop HI About nav label repair. `ISSUE_LEDGER.md` was intentionally left unchanged because `I18N-001` remains a regression guard and no recurring status changed.

### 2026-05-20 CST - Package B page-level bgDrift performance repair

**Files changed:** `sampora-website-public-v3/solutions.html`, `sampora-website-public-v3/resources.html`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, and `ISSUE_LEDGER.md`. No `topoDash`, topology SVG/packets/modal behavior, carousel/manual lock logic, route logic, visible copy, i18n data, CTA/footer/legal behavior, QA evidence outputs, delivery folders, zips, staging, commit, push, or packaging files were intentionally changed.

**Audit item addressed:** `[CONFIRMED]` page-level `body::before` / `body:before` `bgDrift` background animation on `solutions.html` and `resources.html` caused the desktop active rAF p95 to sit around `33ms`, while in-memory `animation:none` and post-animation samples were about `17ms`. The repair removed only the page-level `animation: bgDrift 14s ease-in-out 1 alternate` references and the now-unused `@keyframes bgDrift` blocks, preserving the static radial background layers and topology `topoDash` motion.

**Verification run:** source check found no remaining `bgDrift`, `ambientDrift`, `signalSweep`, or `sectionBreath` references in `solutions.html` or `resources.html`, while `topoDash` remained present. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. `git diff --check -- sampora-website-public-v3/solutions.html sampora-website-public-v3/resources.html SAMPORA_LATEST_REPAIR_HANDOFF.md ISSUE_LEDGER.md` -> PASS with LF/CRLF warnings only. Browser spot check rendered desktop `solutions.html` and `resources.html`: `body::before` computed `animationName` was `none` / `animationDuration` `0s`, document overflow remained `0`, and hero/next-section geometry stayed unchanged (`0px` deltas). Local system-Chrome rAF evidence was interpreted against a same-browser blank-page baseline because this headless environment produced p95 spikes even on a blank page (`p95` about `23.7-26.2ms`): after manually locking topology to avoid carousel tick noise, Solutions measured `p50 17.0-17.5ms`, `p95 24.6-30.0ms`; Resources measured `p50 16.6-17.1ms`, `p95 24.0-27.3ms`. Longer auto-carousel windows still showed p95 spikes (`32.7-57.2ms`), so the scoped runtime claim is that the active `bgDrift` animation is removed and page samples are near the local no-page/locked baseline; unrelated auto-carousel timing spikes were not repaired in this task.

**Remaining failures / next agent:** none for this scoped page-level `bgDrift` performance repair. A read-only review subagent is still recommended if the controller requires independent final/visual review under the standard Sampora workflow; no package rebuild, Lighthouse/axe, or delivery sync was run in this scoped worker task.

### 2026-05-20 CST - Three-page topology line-flow speed unification

**Files changed:** `sampora-website-public-v3/index.html`, `sampora-website-public-v3/resources.html`, `sampora-website-public-v3/solutions.html`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, and `ISSUE_LEDGER.md`. No visible copy, i18n data, CTA/footer/legal behavior, workflow/card timers, topology packet `animateMotion`, `topoScan`, pulse/hubPulse, QA evidence output, screenshots, delivery folder, zip/package, staging, commit, push, or packaging file was intentionally changed.

**Audit item addressed:** `[CONFIRMED]` topology line-flow speed inconsistency across the three topology pages. Read-only source verification found current active line durations differed: homepage `topoDash 2.8s` plus amber override `3.1s`, Resources active line `2.65s` plus amber `2.95s`, and Solutions active line `2.4s` plus amber `2.8s`. Per the user's instruction, the shared target was the fastest current topology line-flow value, `2.4s`.

**Implementation notes:** Package A updated only homepage topology active-flow duration and amber active-line override to `2.4s`. Package B updated only Resources active-flow / flow-group / amber topology line duration to `2.4s` and Solutions amber active-line override to `2.4s`; Solutions normal active line was already `2.4s`. Packet `animateMotion` durations, scan/pulse effects, workflow/card logic, and copy were left untouched.

**Verification run:** implementation workers reported scoped static PASS for the targeted rules, `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS for Package B, and `git diff --check` -> PASS with LF/CRLF warnings only. Read-only final reviewer confirmed static target values at `index.html`, `resources.html`, and `solutions.html`: active flow / flow-group / amber override are all `2.4s`, with no old `1.15s`, `2.65s`, `2.8s`, `2.95s`, or `3.1s` attached to `topoDash` or active amber overrides. Runtime browser sampling with system Chrome + Playwright covered EN/ZH/HI at desktop `1440x900` and mobile `390x900`; each page reported computed `animationName=topoDash`, `animationDuration=2.4s`, and changing `strokeDashoffset`. Representative desktop EN samples: homepage `-17.7993px -> -37.2393px`, Resources `-60.4863px -> -17.7133px`, Solutions `-41.8094px -> -5.43542px`. All sampled pages had `overflowX=0`, active line motion remained visible, and active packets/animateMotion still existed.

**Acceptance status separation:** technical/static PASS; runtime motion PASS; visual acceptance PASS. The read-only reviewer judged the real browser experience consistent across desktop/mobile and EN/ZH/HI: a normal user sees all three topology line flows moving at the same visual rhythm, with no page visibly faster/slower, no stopped line, and no missing topology surface.

**Remaining failures / next agent:** none for this scoped three-page topology line-flow speed unification. Packaging, Lighthouse/axe, delivery sync, zip rebuild, and unrelated carousel/manual-lock checks were not run in this scoped task.

### 2026-05-20 CST - Controller closeout for topology label alignment, homepage live-dot pulse, and Resources output visual

**Files changed:** `sampora-website-public-v3/index.html`, `sampora-website-public-v3/solutions.html`, `sampora-website-public-v3/resources.html`, and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No About page, AGENTS/acceptance/ledger/glossary docs, QA evidence output, screenshots, delivery folders, zips, staging, commit, push, or packaging files were intentionally changed by this closeout.

**Audit items addressed:** `[CONFIRMED]` homepage topology `SAMPLE SUPPLY POOL` label was visually offset from the pool card/title center; `[CONFIRMED]` homepage top status live dot had lost its breathing/pulse motion; `[CONFIRMED]` Solutions topology internal column/node labels were left-shifted; `[CONFIRMED]` Resources topology internal column/node labels were left-shifted; `[CONFIRMED]` Resources selected setup right-panel output region still read as a heavy nested bordered block rather than a calm output continuation.

**Implementation notes:** Package A updated only `index.html`: restored `pulse-lime 1.9s ease-out infinite` on `.status .live::before` and moved homepage `topoLayersHead` from `x="313"` to `x="300"`. Package B Solutions updated only `solutions.html`: centered `.topo-col`, moved `#colInput/#colLayer/#colControl/#colEvidence` to corresponding hub/card centers, and centered `#labelPanel/#labelNetwork/#labelEnterprise` label rows while preserving i18n ids and copy semantics. Package B Resources updated only `resources.html`: centered topology column and node labels, moved SVG label x positions to hub/node centers, changed `.mode-fit-card` from a heavy bordered filled card to a lightweight divider strip, and made `.mode-fit-tags` read as plain output metadata with mobile normal text flow.

**Verification run:** implementation workers reported inline script parsing PASS for touched files, `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` PASS, `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` PASS, and `git diff --check` PASS for touched pages with LF/CRLF warnings only. Independent read-only final visual review rerendered current files: `index.html` EN/ZH/HI at `1440x900`, `1967x1270`, and `390x900` showed homepage pool-label-to-card max delta `0.5px`, pool-label-to-title max delta `0.39px`, overflow `0`, and desktop `.status .live::before` computed `pulse-lime | 1.9s | infinite` with changing box-shadow samples. `solutions.html` EN/ZH/HI across `1440x900`, `1967x1270`, and `390x900` plus `panel/network/enterprise` modes showed column delta max `0.5px`, node-label horizontal delta max `0.45px`, overflow `0`, active `topoDash`, changing dash offsets, moving packets, and manual locks held after modebar/SVG selection. `resources.html` EN/ZH/HI across all three viewports and `setup/supplier/finance` modes showed column delta max `0.5px`, node-label horizontal delta max `6.35px` with no label overflow, overflow `0`, active `topoDash`, changing dash offsets, moving packets, and manual locks held. Resources setup right panel had transparent/no-shadow/no-side-border `.mode-fit-card`, borderless transparent `.proof-output`, three contained non-overlapping rows, and mobile tags with no extra horizontal scroll.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; runtime motion PASS; real visual perception / 真实观感 visual acceptance PASS. The final reviewer judged the homepage pool label centered, the desktop live dot visibly breathing, Solutions/Resources topology labels centered and stable across languages/modes, and the Resources setup output area a calm lightweight list rather than a heavy nested card stack.

**Remaining failures / next agent:** none for this scoped topology label alignment, homepage live-dot motion, and Resources selected-output visual round. Subsequent or parallel bgDrift and topology line-flow speed entries remain separate workstreams. Packaging, zip rebuild, Lighthouse/axe, delivery sync, and unrelated dirty workspace files remain intentionally untouched until explicitly requested.

### 2026-05-20 CST - Homepage and Resources hero baseline visual alignment closeout

**Files changed:** `sampora-website-public-v3/index.html`, `sampora-website-public-v3/resources.html`, and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No visible copy/i18n semantics, topology SVG structure, topology/workflow JavaScript behavior, modal behavior, other pages, QA evidence output, delivery folders, zips, staging, commit, push, or packaging files were intentionally changed.

**Items addressed:**
- `[CONFIRMED]` Package B Resources first-viewport visual failure: left three resource-entry cards did not baseline-align with the right `#topoStage`, and the next `#surfaces` section showed an unwanted top horizontal rule.
- `[CONFIRMED]` Package A Homepage first-viewport visual failure: the left `.hero-proof-grid` card-row bottom did not align with the right `#network` console bottom. A prior worker report measured the wrong wrapper; controller rechecked the correct selector pair before closeout.

**Implementation summary:**
- `resources.html`: removed the immediate `#surfaces` / `.hero + .section` top border and adjusted existing desktop `--resource-hero-*` spacing variables for the remaining failing viewports/languages. The final change preserved the already-passing 1440/1600 states and mobile stacked layout.
- `index.html`: adjusted the desktop homepage hero CSS so the left hero copy column stretches against the right console and the `.hero-proof-grid` card row lands on the same bottom baseline as `#network`. The scoped repair is desktop-only; mobile remains stacked.

**Verification run:**
- `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS.
- `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS.
- `git diff --check -- sampora-website-public-v3/index.html sampora-website-public-v3/resources.html` -> PASS with LF/CRLF warnings only.
- Resources browser geometry with system Chrome + local Playwright, correct selector pair `.resource-entry-grid.bottom - #topoStage.bottom`: `1314x900` EN `-0.19px`, ZH `0.47px`, HI `-0.05px`; `1365x768` EN `0.06px`, ZH `4.56px`, HI `3.27px`; `1440x900` EN `-0.22px`, ZH `-7.53px`, HI `-4.20px`; `1600x1000` EN `0.47px`, ZH `-7.53px`, HI `-4.20px`; all sampled horizontal overflow `0`. `#surfaces` computed `border-top: 0px none`.
- Homepage browser geometry with system Chrome + local Playwright, after clearing language state and reloading per language, correct selector pair `.hero-proof-grid.bottom - #network.bottom`: `1314x900`, `1365x768`, `1440x900`, and `1600x1000` all EN/ZH/HI deltas `0px`, with horizontal overflow `0`. Mobile `390x900` EN/ZH/HI horizontal overflow stayed `0`; mobile is stacked and not a desktop baseline-alignment target.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; visual acceptance PASS. The Resources selected region no longer shows the unwanted horizontal rule and the card row/topology stage reads aligned across the tested desktop language states. The Homepage left proof-card row now visually shares the right console bottom baseline across EN/ZH/HI desktop states; the left content reads distributed down the column instead of ending high above the console.

**Remaining failures / next agent:** none for these two scoped visual-alignment items. Packaging, Lighthouse/axe, delivery sync, zip rebuild, and unrelated page/content work remain deferred unless explicitly requested.

### 2026-05-20 JST - Package A homepage status live-dot clearance

**Files changed:** `sampora-website-public-v3/index.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. The previous controller-intake/no-active-queue note that was appended without a page repair was removed from this handoff tail. No visible copy/i18n semantics, JavaScript behavior, QA evidence output, screenshots, delivery folder, zip/package, staging, commit, push, or packaging file was intentionally changed.

**Audit item addressed:** `[CONFIRMED]` newest user browser screenshot comment for the homepage top status strip: the left live dot's breathing/pulse effect was partly clipped by the selected region's left edge. Owner package is Package A homepage; acceptance category is Visual acceptance / motion.

**Implementation summary:** added `padding-left: 10px` to `.status .inner` in `index.html`. The existing `pulse-lime` keyframe expands the dot shadow to `9px`, so `10px` gives a 1px safety clearance while moving the status-strip content only about one letter to the right. The live-dot size, `pulse-lime 1.9s` animation, status copy, order, and language data were left unchanged.

**Verification run:** `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. `git diff --check -- sampora-website-public-v3/index.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> PASS with LF/CRLF warnings only. Chrome/CDP file-URL geometry at desktop `1314x900` across EN/ZH/HI measured `.status .inner` `padding-left: 10px`, live-dot left clearance `10px`, required pulse clearance `9px`, `animationName: pulse-lime`, `animationDuration: 1.9s`, and document horizontal overflow `0`. Additional `900x900` desktop check measured the same `10px` clearance with overflow `0`; at `760px` and `390px` the status bar is hidden by the existing mobile rule and document overflow remained `0`.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; real visual perception / 真实观感 visual acceptance PASS. A normal desktop user should now see the live dot breathing without its left halo being cut off, while the movement is limited to the small one-letter offset requested and mobile remains unaffected because the status strip is hidden there.

**Remaining failures / next agent:** none for this scoped homepage status live-dot clearance item. Packaging, Lighthouse/axe, delivery sync, zip rebuild, and unrelated page/content work remain deferred unless explicitly requested.

### 2026-05-20 JST - Package C Contact hidden-field and mobile-header repair

**Files changed:** `sampora-website-public-v3/contact.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No other HTML page, shared asset, QA evidence output, screenshot, delivery folder, zip/package, staging, commit, push, or packaging file was intentionally changed.

**Audit items addressed:** `[CONFIRMED]` Contact form hidden `plan` field did not update immediately after a manual `#business_type` change; `[CONFIRMED]` Contact mobile header controls were clipped at 390px even while document overflow stayed hidden.

**Implementation summary:** added `change` and `input` listeners on `#business_type` to rerun the existing `setHiddenFields()` path before submit. Added scoped Contact mobile mast CSS below 760px/480px so the header uses a constrained two-column grid, hides the brand subtitle, keeps language buttons and the mobile-nav toggle inside the action track, and hides the header Book demo/Start trial buttons at 390px where they cannot fit safely. Visible copy, i18n semantics, CTA hrefs, backend placeholder policy, and submit behavior were not changed.

**Verification run:** inline `contact.html` script parse -> `OK: 2 inline scripts parsed`; `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `git diff --check -- sampora-website-public-v3/contact.html` -> PASS with LF/CRLF warning only. Browser check with system Chrome + Playwright at `390x900`: before edit, changing `#business_type` from `start_trial` to `resource_question` left hidden `plan=start_trial`; after edit it immediately updates to `plan=resource_question`. Placeholder submit payload kept `intent=start_trial`, `plan=resource_question`, `business_type=resource_question`, `lang=en`, showed fallback toast, and did not show success/failure toast. EN/ZH/HI mobile header geometry passed with document overflow `0`, brand inside viewport, `.mast .header-actions` right edge `374px`, language switch right edge `330px`, `.mobile-nav-toggle` `338-374px`, and language buttons switched active `en -> zh -> hi` correctly.

**Acceptance status separation:** technical/static PASS; runtime behavior PASS; runtime geometry PASS; real visual perception / 真实观感 visual acceptance PASS. At 390px a normal user now sees the Contact mobile brand, language switcher, and menu button as a single unclipped header row; the too-wide header CTAs are not shown on this narrow mobile header, avoiding the previous partly clipped controls while preserving form and page CTA policy elsewhere.

**Remaining failures / next agent:** none found for this scoped Package C Contact repair. `CONTACT-001`, `CTA-001`, `I18N-001`, `VISUAL-QA-001`, and `HANDOFF-006` remain regression/strategy guards; `ISSUE_LEDGER.md` was intentionally left unchanged for controller review.

### 2026-05-20 JST - Header brand and navigation consistency repair

**Files changed:** `sampora-website-public-v3/index.html`, `sampora-website-public-v3/solutions.html`, `sampora-website-public-v3/plans.html`, `sampora-website-public-v3/resource-manuals.html`, `sampora-website-public-v3/qa-evidence/header-consistency-check.mjs`, and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No About source edit, redirect page, CTA strategy, footer/content/topology/contact-form behavior, screenshot, delivery folder, zip/package, staging, commit, push, or packaging file was intentionally changed.

**Audit item addressed:** `[CONFIRMED]` newest screenshot comment requested the About-page header brand/nav data as the canonical pattern across main pages. The repair aligned brand home links to `index.html`, kept the six desktop routes in canonical order, preserved per-page active state, added the missing static About route to Resource Manuals, and kept `resource-manuals.html` mobile active state under the Resources channel. About's header CTA still lacks `intent=` and remains a recorded blocker/warning only; it was not copied to other pages.

**Verification run:** red check before page edits: `node "sampora-website-public-v3\qa-evidence\header-consistency-check.mjs"` failed on brand href mismatches and Resource Manuals missing/static About subtitle/nav evidence, with the expected About CTA warning. Post-repair: the same command passed across 7 main pages and still reported the About CTA intent warning. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` PASS. Browser smoke with local Playwright covered EN/ZH/HI at `1440x900` and `390x900` for all 7 main pages: 42 header states checked, canonical route order and About presence passed, per-page active href passed, brand href was `index.html`, and horizontal overflow was `0`.

**Remaining failures / next agent:** no remaining failure for this scoped header brand/nav consistency item. Remaining concern is the intentionally untouched `[BLOCKED]` About header CTA without `intent=` (`contact.html#contact-form`), which should be handled only by a future CTA-policy task. `ISSUE_LEDGER.md` was intentionally left unchanged because `CTA-001`, `I18N-001`, `VISUAL-QA-001`, and `HANDOFF-006` remain guards rather than changed recurring statuses.

### 2026-05-20 JST - Cross-page top status live-dot clearance and breathing reuse

**Files changed:** `sampora-website-public-v3/index.html`, `about_sampora_issues_fixed.html`, `solutions.html`, `resources.html`, `plans.html`, `contact.html`, `resource-manuals.html`, and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. `products.html` and `pricing.html` were checked and intentionally not edited because they do not have a visible top status strip. No visible copy/i18n semantics, topology/workflow JavaScript behavior, contact form behavior, QA evidence output, screenshots, delivery folder, zip/package, staging, commit, push, or packaging file was intentionally changed.

**Audit item addressed:** newest user request to reuse the homepage live-dot clipping repair across other pages, planning per page according to top-strip content length, and adding breathing motion where the top live dot was static or only animated once.

**Implementation summary:** all visible status strips now reserve `10px` left clearance for the live dot. The required clearance is `9px` because the dot is `7px` and the breathing shadow expands to `9px`; `10px` preserves a 1px safety margin while moving content only a small one-letter amount. Existing infinite breathing was preserved on `index.html` and About. `solutions.html`, `resources.html`, and `plans.html` were changed from one-shot `1` iteration to continuous `infinite` breathing. `contact.html` and `resource-manuals.html` were changed from static glow to continuous breathing. For long top-strip pages, `solutions.html` and `resources.html` now let the right status text shrink with ellipsis, and at narrow desktop widths (`<=980px`) hide the third status segment plus its separator so the live dot and core status text remain visible without right-side clipping.

**Verification run:** `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. `git diff --check -- sampora-website-public-v3/index.html sampora-website-public-v3/about_sampora_issues_fixed.html sampora-website-public-v3/solutions.html sampora-website-public-v3/resources.html sampora-website-public-v3/plans.html sampora-website-public-v3/contact.html sampora-website-public-v3/resource-manuals.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> PASS with LF/CRLF warnings only. Source scan confirmed visible status pages have `padding-left:10px` and `1.9s infinite` live-dot animation; `products.html` and `pricing.html` have no status strip.

**Runtime geometry / motion PASS:** Chrome/CDP file-URL checks covered `index.html`, About, Solutions, Resources, Plans, Contact, and Resource Manuals across EN/ZH/HI at desktop `1314x900`, narrow desktop `900x900`, and mobile `390x900`. Visible desktop status strips measured live-dot left clearance `10px` against required `9px`, pseudo width `7px`, animation duration `1.9s`, iteration `infinite`, and top-strip content right overflow `0` after the long-text rules. Mobile `390px` kept the existing behavior: status strips are hidden and page overflow stayed `0`. Targeted long-text recheck for Solutions/Resources at `1314x900` and `900x900` confirmed `statusContentRightOverflow=0` across EN/ZH/HI. `solutions.html` at `900x900` EN still shows a separate document-level `48px` overflow from non-status page content (`#fitSelectors` / fit-card area), but the status strip itself passes and that layout area was intentionally left outside this top-strip task.

**Visual acceptance PASS:** a normal desktop user should now see the live dot breathe without the left halo being clipped on every page with a visible top status strip. Pages whose top text is short keep the full strip. Long-text pages keep full text on wider desktop and simplify the strip on narrow desktop rather than letting the right side clip. Contact and Resource Manuals now have the same breathing affordance instead of a static dot.

**Remaining failures / next agent:** none for the scoped cross-page top status live-dot clearance and breathing-motion reuse. The unrelated `solutions.html` `900x900` EN non-status overflow should be treated as a separate `[VERIFY-FIRST]` layout item only if the user asks to audit or repair that section. Packaging, Lighthouse/axe, delivery sync, and zip rebuild remain deferred until explicitly requested.

### 2026-05-20 JST - Controller closeout for all-page carousel/card switching acceptance

**Files changed:** controller closeout updates were limited to `SAMPORA_LATEST_REPAIR_HANDOFF.md` and `ISSUE_LEDGER.md`. The page/source repair made in this controller round was delegated to a Package C Contact implementation subagent and touched `sampora-website-public-v3/contact.html`. Other existing dirty page diffs for status/live-dot clearance in `index.html`, `about_sampora_issues_fixed.html`, `plans.html`, `resource-manuals.html`, `resources.html`, and `solutions.html`, plus untracked `sampora-website-public-v3/qa-evidence/header-consistency-check.mjs`, `archives/`, and `动态/`, were not approved as part of the carousel/card switching closeout and were not reverted.

**Scope decision:** newest user instruction required the controller to stay orchestration-only and dispatch one agent per page before final acceptance. Read-only page agents covered `index.html`, `solutions.html`, `resources.html`, `plans.html`, `contact.html`, `resource-manuals.html`, `about_sampora_issues_fixed.html`, `products.html`, and `pricing.html`. Only Contact had current `[CONFIRMED]` issues in this scope: mobile header controls clipped at 390px and hidden `plan` not updating immediately after manual `#business_type` changes. Homepage, Solutions, Resources, and Resource Manuals were `[REGRESSION]` guard PASS for their switching/rotation surfaces. Plans and About were `[REMOVED]` for carousel/card-switch repair because they have static surfaces only. Products and Pricing were `[REMOVED]` because they are redirect-only files.

**Verification run:** independent read-only final review reported PASS for the scoped round. Fresh checks: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\active-handoff-mojibake-guard.mjs"` -> PASS; `git diff --check` -> PASS with LF/CRLF warnings only. Runtime review sampled homepage workflow/project/topology motion, Solutions and Resources auto/manual mode switching, Resource Manuals doc/search/modal switching, and Contact repair behavior. Contact 390px EN/ZH/HI header controls stayed inside the viewport with overflow `0`, and hidden `planField` updated immediately on `change` and `input` before submit.

**Acceptance status separation:** technical/static PASS; runtime behavior and geometry PASS; real visual perception / 真实观感 PASS for the scoped carousel/card switching and Contact repair round. No normal-user-visible broken switching state, blank/undefined state, stolen manual selection, or Contact mobile clipping remained in the verified scope.

**Remaining failures / next agent:** none found for the user-requested all-page carousel/card switching closeout and scoped Contact repair. About old-style CTA links noted by the About verifier are out of scope for this round and should enter a new pending verification pool if the user asks for CTA cleanup. The `solutions.html` `900x900` EN non-status overflow noted in the cross-page live-dot entry remains separate `[VERIFY-FIRST]` only if explicitly assigned. Packaging, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, and push remain deferred until explicitly requested.

### 2026-05-20 JST - Header action parity with Resources reference

**Files changed:** `sampora-website-public-v3/index.html`, `sampora-website-public-v3/solutions.html`, `sampora-website-public-v3/plans.html`, `sampora-website-public-v3/about_sampora_issues_fixed.html`, `sampora-website-public-v3/contact.html`, `sampora-website-public-v3/resource-manuals.html`, and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. `sampora-website-public-v3/resources.html`, `ISSUE_LEDGER.md`, `ACCEPTANCE_TESTS.md`, QA scripts/evidence, delivery folders, zips, body CTAs, footer CTAs, forms, topology/workflow/manual app data, staging, commit, push, and packaging were intentionally untouched by this worker.

**Audit item addressed:** `[CONFIRMED]` newest user scope: align the page-header right action area to the `resources.html` browser-selected reference, make header `Start trial` hover/active motion match that reference, make header `Book a demo` consistently bold, and repair the About header from one bare old CTA to the same two intent-routed CTAs. Owner packages are Package A homepage, Package B Solutions, Package C Plans/Contact, Package D Resource Manuals, and the assigned About page. Acceptance categories are CTA policy, Language/i18n, Navigation/sticky/header consistency, Visual acceptance, and Scope.

**Implementation summary:** added a scoped `Header actions parity with resources.html` CSS block to each non-reference page, limited to `.mast .header-actions` / `.mast .nav-actions` / `.mast .actions` and `.mast .lang` selectors. Desktop action tracks are 430px, language switchers use the Resources geometry, header ghost buttons use 800 weight / 13.5px / 12px 18px / cyan hover lift, and header primary buttons use 800 weight / Resources gradient and stronger hover/active shadow lift. About header now has `Book a demo` -> `contact.html?intent=book_demo#contact-form` and `Start trial` -> `contact.html?intent=start_trial#contact-form`, with the mobile nav toggle after both links. About EN/ZH/HI header keys now render `Book a demo` / `预约演示` / `डेमो बुक करें` and `Start trial` / `开始试用` / `ट्रायल शुरू करें`. Plans and About received narrow-mobile header rules so 390px keeps the action area inside the viewport while preserving existing mobile CTA hiding behavior.

**Verification run:** red browser check before editing confirmed About header mismatch (`.lang` 118.41x36, no ghost CTA, bare `contact.html#contact-form`, no hover lift) and confirmed inconsistent `Book a demo` weights / Contact sizing / Resource Manuals ghost hover. Post-repair: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `git diff --check -- sampora-website-public-v3/index.html sampora-website-public-v3/solutions.html sampora-website-public-v3/plans.html sampora-website-public-v3/about_sampora_issues_fixed.html sampora-website-public-v3/contact.html sampora-website-public-v3/resource-manuals.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> PASS with LF/CRLF warnings only. Inline Playwright/Node browser check, no file writes, covered `index.html`, `solutions.html`, `resources.html`, `plans.html`, `about_sampora_issues_fixed.html`, `contact.html`, and `resource-manuals.html` at desktop `1440x900`: every page measured action track `430px`, language switcher about `145x42`, language button font weight `800`, header `Book a demo` font weight `800`, header `Start trial` font weight `800`, primary hover `translateY(-1px)` with about `42px` shadow blur, ghost hover `translateY(-1px)` with cyan text/border and `rgba(20,40,69,.22)` background, and desktop overflow `0`. Mobile `390x900` overflow was `0` on all seven pages; Plans action track was `190-360px`, Contact `204-374px`, and About `217.86-374px`. About EN/ZH/HI header browser check rendered the two correct texts and intent hrefs with no arrows, question marks, or header bare `contact.html#contact-form`.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; real visual perception / 真实观感 evidence ready for reviewer. The normal desktop header now reads consistently against the Resources reference: equal-width right action area, same language-pill size, bold paired CTAs, and matching primary/ghost lift behavior. On mobile, existing CTA hiding is preserved where needed and the visible language/menu controls no longer push the header action area outside 390px.

**Remaining failures / next agent:** none for this scoped header-action parity repair. About body/final/footer CTAs that still contain bare `contact.html#contact-form` were intentionally left untouched because this worker was explicitly limited to header right actions and About header CTA markup. `ISSUE_LEDGER.md` was intentionally unchanged because `CTA-001`, `I18N-001`, `VISUAL-QA-001`, and `HANDOFF-006` remain guard/strategy statuses and did not need reclassification in this scoped worker task.

### 2026-05-20 JST - Package B Resources proof-output marker rail lightening

**Files changed:** `sampora-website-public-v3/resources.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No visible copy, i18n data, topology JavaScript/SVG behavior, CTA/footer/legal, QA evidence output, delivery folder, zip/package, staging, commit, push, or packaging file was intentionally changed.

**Audit item addressed:** `[CONFIRMED]` newest user visual feedback for Resources `What this path produces`: the 01/02/03 left number track still looked like an overly heavy vertical numbered rail after earlier selected-output visual work. Owner package is Package B Resources; acceptance category is Visual acceptance / Scope.

**Implementation summary:** changed only `.proof-output-row`, `.proof-output-num`, `.proof-output-row:not(:last-child) .proof-output-num:after`, and the matching mobile rules. Desktop grid number column changed from `38px` to `24px`, the marker became a `20px` subtle pill with `2px` left breathing room, the text start moved from `53px` to `36px`, and the connector line is centered under the marker with lower alpha. Mobile number column changed from `34px` to `22px`, marker to `18px`, and text start from `46px` to `32px`.

**Verification run:** red browser check before repair confirmed EN/ZH/HI desktop `gridTemplateColumns: 38px ...`, number width `38px`, text left `53px`, line alpha `rgba(56,210,255,.28)`; mobile `gridTemplateColumns: 34px ...`, number width `34px`, text left `46px`. Post-repair browser checks with system Chrome + Playwright covered EN/ZH/HI at `1440x900` and `390x900`: desktop `24px` number column, `20px` marker, `2px` marker left inset, text left `36px`, line alpha `rgba(56,210,255,.16)`, overflow `0`; mobile `22px` number column, `18px` marker, text left `32px`, overflow `0`. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. `resources.html` inline script syntax check -> 2 scripts OK. `git diff --check -- sampora-website-public-v3/resources.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> PASS with LF/CRLF warnings only.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; real visual perception / 真实观感 visual acceptance PASS. Rendered desktop EN and mobile HI clips showed the 01/02/03 area as small markers plus a faint guide line, with the copy pulled closer and no wide left column. A normal user should no longer read this region as a prominent vertical numbered block.

**Remaining failures / next agent:** none found for this scoped proof-output marker-rail item. `ISSUE_LEDGER.md` was checked; no dedicated recurring row matched this specific Resources proof-output rail, so the ledger was not updated. `VISUAL-QA-001` remains a general strategy guard. Existing unrelated dirty workspace changes were preserved. Scope caveat: implementation before-write diff already showed unrelated dirty hunks in `resources.html`, including status/live-dot, hero/topology layout, and SVG viewBox changes; current brand/nav copy hunks are unattributed from available before-write evidence. This round owns/reviews only the `.proof-output*` marker-rail changes and this handoff entry.

### 2026-05-20 JST - Package B Solutions #fit narrow desktop overflow repair

**Files changed:** implementation subagent changed only `sampora-website-public-v3/solutions.html`; documentation closeout appended this handoff entry in `SAMPORA_LATEST_REPAIR_HANDOFF.md`. `ISSUE_LEDGER.md` was intentionally not changed because this was a one-off `solutions.html` `#fit` narrow desktop layout overflow repair, not a new recurring issue. No status/live-dot, topology, CTA, i18n copy, compare behavior, other pages, QA evidence output, delivery folder, zip/package, staging, commit, push, or packaging file was intentionally changed.

**Controller/subagent scope decision:** the user required the main agent to stay controller-only; current-file verification, implementation, and read-only final review were performed by subagents. A read-only verification subagent reclassified the `solutions.html` `900x900` EN non-status horizontal overflow from `[VERIFY-FIRST]` to `[CONFIRMED]`. Red evidence: `documentElement.scrollWidth=933`, `body.scrollWidth=933`, `innerWidth=900`, overflow `+33px`; status right overflow `0px`; overflow source was the `#fit` area, where `#fitSelectors`, `.recommend`, and `.select-btn` had right edge `933px`, container width was `852px`, and `.fit-panel` grid track was `909px`.

**Implementation summary:** the implementation subagent edited only the `@media(max-width:1100px)` fit-area CSS in `sampora-website-public-v3/solutions.html`. The repair added or adjusted `minmax(0, 1fr)`, `min-width: 0`, `max-width: 100%`, and necessary wrapping for `.fit-panel`, `#fitSelectors`, `.recommend`, `.select-btn`, and `.mini-metrics`. Status/live-dot, topology, CTA, i18n copy, compare behavior, and other pages were not touched.

**Verification run:** post-repair runtime evidence reported `900x900` EN document/body overflow `0px`, status right overflow `0px`, and fit selectors/recommend/select buttons right edge `876px`, matching container right `876px`. `900x900` ZH/HI overflow was `0`; `390x900` EN/ZH/HI overflow was `0`. Compare internal scroll was preserved: `1080/850` at `900px` and `1080/356` at `390px`. Static/technical checks passed: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `git diff --check -- sampora-website-public-v3/solutions.html` -> exit 0 with LF/CRLF warning only.

**Read-only review:** independent review subagent reported PASS. Diff scope was task-scoped; no new status/live-dot, topology, CTA, i18n, compare, or other-page change was found in the reviewed fit area. Runtime geometry PASS. Visual acceptance PASS: at `900px` EN, fit cards and recommendation are inside page width, metric chips fit without overlap, and text remains readable; ZH/HI and mobile remain natural.

**Remaining failures / next agent:** none for this scoped non-status `solutions.html` `#fit` overflow fix. Dirty baseline caveat: the repo still has older dirty files/hunks, so do not treat full `git diff --name-only` as this task scope. Packaging, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, and push were not run and remain deferred until explicitly requested.

### 2026-05-20 JST - Header brand/nav data consistency final closeout

**Files changed:** `sampora-website-public-v3/index.html`, `sampora-website-public-v3/solutions.html`, `sampora-website-public-v3/resources.html`, `sampora-website-public-v3/plans.html`, `sampora-website-public-v3/contact.html`, `sampora-website-public-v3/resource-manuals.html`, `sampora-website-public-v3/about_sampora_issues_fixed.html`, `sampora-website-public-v3/qa-evidence/header-consistency-check.mjs`, and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. `products.html`, `pricing.html`, delivery folders, zips, screenshots, staging, commit, push, and packaging were intentionally untouched. Existing dirty workspace items from other rounds were preserved.

**Audit item addressed:** `[CONFIRMED]` newest browser screenshot comment requested that all pages reuse the About-page header brand/logo-position data and remain consistent. The scoped repair aligned the visible main-page header data across 7 main pages: brand link, brand name, brand subtitle source values, desktop nav route order, About route presence, per-page active state, mobile drawer route order, and mobile EN/ZH/HI label order. `resource-manuals.html` remains under the Resources active channel. About header/body/footer bare CTA links were treated as a separate CTA-policy blocker and were not copied to other pages.

**Implementation summary:** added `qa-evidence/header-consistency-check.mjs` to guard header consistency. After first final review found a real visual failure in multilingual brand subtitles, the script was strengthened to check canonical brand subtitle source values and full EN/ZH/HI mobile label order. The canonical brand subtitle values are EN `Operations console`, ZH `操作控制台`, and HI `संचालन कंसोल`. The page sources were updated to use those values consistently, while keeping existing intent-routed header CTAs on pages that already had them.

**Verification run:** red check before repair confirmed brand href/nav/resource-manuals header failures; a second red check after strengthening the script confirmed brand subtitle and mobile-label inconsistencies. Final controller sanity check: `node "sampora-website-public-v3\qa-evidence\header-consistency-check.mjs"` -> PASS across 7 main pages, with redirect pages skipped. Independent read-only final review reran `header-consistency-check.mjs`, `final-audit-static-check.mjs`, `page-layer-static-check.mjs`, and targeted `git diff --check`; all passed with LF/CRLF warnings only. Browser-rendered review covered 42 states: 7 main pages x EN/ZH/HI x desktop `1440x900` and mobile `390x900`.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; real visual perception / 真实观感 PASS. Desktop headers now show the same Sampora brand position, six-item nav structure, About presence, and correct active state; brand subtitle source values are canonical per language and visually consistent across pages. Mobile headers keep the subtitle hidden by design, open a consistent six-item drawer, preserve correct active state, and have horizontal overflow `0`.

**Remaining failures / next agent:** none for this scoped header brand/nav data consistency item. Remaining blocker is separate `[BLOCKED]` CTA-policy cleanup for About page bare links such as `contact.html#contact-form` / `contact.html`; do not treat it as repaired by this round. `ISSUE_LEDGER.md` was not updated for this closeout because `CTA-001`, `I18N-001`, `VISUAL-QA-001`, and `HANDOFF-006` remain guard/strategy statuses. Packaging, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, and push remain deferred until explicitly requested.

### 2026-05-20 JST - Package B Resources proof-output compact-list rebuild second pass

**Files changed:** `sampora-website-public-v3/resources.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No visible copy, i18n data, JavaScript/topology behavior, CTA/footer/legal/nav, QA evidence output, delivery folder, zip/package, staging, commit, push, or packaging file was intentionally changed.

**Audit item addressed:** `[CONFIRMED]` second-pass visual acceptance failure for Resources `What this path produces`: the previous fix only narrowed/lightened the 01/02/03 rail, but the user screenshot standard required removing the independent vertical number-track/timeline feeling entirely. Owner package is Package B Resources; acceptance category is Visual acceptance / real visual perception.

**Implementation summary:** rebuilt the scoped `.proof-output*` CSS as a compact list component instead of patching the old rail. `.proof-output` now spaces three independent list rows; `.proof-output-row` is a compact content block with subtle border/background and no grid number column; `.proof-output-num` is a faint top-right micro badge inside each row; `.proof-output-row:not(:last-child) .proof-output-num:after` is disabled with `content:none` / `display:none`. The three semantic items remain unchanged: Configured project record, Supplier delivery trail, and Settlement-ready evidence.

**Verification run:** scoped static guard PASS: row rebuilt as block with no `grid-template-columns`, number badge is top-right with no left positioning, vertical connector disabled, and old `38px/34px/24px/22px` proof-output rail widths absent. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. `resources.html` inline scripts parsed OK: 2 scripts. Playwright/Chrome browser checks covered EN/ZH/HI at `1440x900` and `390x900`; all states had `overflowX=0`, each row computed `display:block`, `gridTemplateColumns:none`, the number badge computed `position:absolute` at the right side, and `::after` computed `content:none`.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; real visual perception / 真实观感 PASS. The current rendered component reads as three compact horizontal content blocks with a weak per-row number in the upper-right corner. A normal user should not see an independent left rail, timeline, vertical guide, blue vertical block, or aligned left number column on desktop or mobile.

**Remaining failures / next agent:** none for this scoped second-pass Resources proof-output rebuild. `ISSUE_LEDGER.md` was checked; no dedicated recurring row matches this one-off proof-output component failure, so the ledger was not updated. Existing unrelated dirty hunks in `resources.html` and other files were preserved.

### 2026-05-20 JST - Package B Resources topology restoration and hero-to-topology alignment

**Files changed:** page/source repair currently present in the working tree is `sampora-website-public-v3/resources.html`; this Package E docs/handoff subagent changed only `SAMPORA_LATEST_REPAIR_HANDOFF.md` and `ISSUE_LEDGER.md` to record the closeout. No page source edit, formatting, packaging, staging, commit, push, delivery folder, zip, screenshot, or QA evidence output was performed by this docs subagent.

**Audit items addressed:** `[CONFIRMED]` Resources first-viewport topology display had been visually distorted with abnormal lower blank space; `[CONFIRMED]` the left-side three-card/left-column content was not aligned with the right topology graphic's lowest visible border. Owner package is Package B Resources; acceptance categories are Topology motion, Visual acceptance, and Scope.

**Implementation summary:** current working-tree `resources.html` has converged to a restored right-side topology with `.topo-stage` using natural proportion and no negative-margin compensation. The left side is now distributed as an overall flex column with `space-between`, so the entire left stack descends evenly; the target alignment is the visible `#topoStage` topology border, not a one-off downward move of only the cards. This entry records the prior implementation subagent's result and the read-only final review; it does not claim page implementation by this Package E docs subagent.

**Verification run:** final read-only runtime review reported PASS across EN/ZH/HI. At `1314x900`, `.resource-entry-grid.bottom - #topoStage.bottom = 0px`, `#topoStage.bottom - actualSvgGraphic.bottom = 20.98px`, overflow `0`, `topoDash` active, dash offset changed, and packet movement was `14.85-16.64px`; neighboring left module gaps measured EN four segments at `31.59px`, ZH about `39.42-39.44px`, and HI about `34.55-34.56px`. At `1440x900`, entry/topo delta was `0px`, topo/SVG gap `22.73px`, overflow `0`, and packet movement `16.74-18.71px`. At `1365x768`, entry/topo delta was `0px`, topo/SVG gap `21.93px`, overflow `0`, and packet movement `16.14-16.77px`. Static evidence from the earlier stage: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `git diff --check -- sampora-website-public-v3/resources.html` reported only the existing CRLF warning.

**Acceptance status separation:** technical/static PASS; runtime geometry/motion PASS; real visual perception / 真实观感 PASS. A normal user can see the right topology restored as a more complete horizontal topology with no obvious bottom void, and the left column now reads as an evenly lowered stack aligned to the bottom of the topology border across EN/ZH/HI.

**Remaining failures / next agent:** none for this scoped Resources first-viewport topology restoration and left-column alignment task. Unrelated visible-copy dirty hunks already present in the working tree, including `brandSub` / `brandSub2` changing from `OPERATIONS CONSOLE` to `Operations console` and HI `navContact` changing from `संपर्क करें` to `संपर्क`, were not treated as part of this task and were not edited by this docs subagent. Other dirty files and untracked workspace items were preserved. No packaging, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, or push was run.

### 2026-05-20 JST - Header brand visual parity with About reference

**Files changed:** `sampora-website-public-v3/index.html`, `solutions.html`, `resources.html`, `plans.html`, `contact.html`, `resource-manuals.html`, `qa-evidence/header-brand-visual-check.mjs`, and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. `about_sampora_issues_fixed.html` was read only as the visual baseline. No right-side header actions, nav order/active state, body, footer, topology, contact form, delivery folder, zip/package, staging, commit, or push was intentionally changed.

**Audit item addressed:** `[CONFIRMED]` newest user feedback: About header left-side Sampora brand/logo block is the target style, while Resources and other pages still rendered different brand block height/top, mark style, text weight, subtitle row, and visual alignment. Owner packages are Package A/B/C/D page headers; acceptance categories are Navigation/sticky/header consistency, Visual acceptance, and Scope.

**Implementation summary:** added a scoped `header-brand-visual-parity` style block to the six target pages only. The override is limited to `.mast .brand`, `.mast .brand .mark`, brand name, and brand subtitle selectors: About-equivalent flex/gap, 30px gradient S mark, 800 brand-name weight, canonical subtitle color/font/line-height/letter-spacing, desktop natural brand width, and mobile subtitle hiding. The existing header action/nav parity blocks and language/CTA behavior were left untouched.

**Verification run:** red check before repair: `node "sampora-website-public-v3\qa-evidence\header-brand-visual-check.mjs"` failed on all target pages with current About-vs-target differences, including brand block height/top, fixed 167.36px width in ZH/HI states, 700/900 brand-name weights, Contact mark background/border/radius/font differences, and subtitle height/color/font-family/top mismatches. Post-repair: `header-brand-visual-check.mjs` -> PASS across 6 target pages against About at desktop `1440x900` plus mobile `390x900` brand visibility across EN/ZH/HI; `header-consistency-check.mjs` -> PASS; `final-audit-static-check.mjs` -> PASS; `page-layer-static-check.mjs` -> PASS; `git diff --check -- <touched files>` -> PASS with LF/CRLF warnings only.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; real visual perception / 真实观感 PASS. Browser geometry summary covered 18 desktop target states and 18 mobile target states: desktop maximum About-vs-target deltas were height `0.31px`, top `0.16px`, center `0px`, width `1.17px`; all sampled target brand names computed `font-weight: 800` and subtitles computed `rgb(112, 132, 158)`. Mobile max overflow was `0`, minimum mark width was `30px`, and no brand block clipped the viewport. A normal user should now read the left Sampora brand block as the same About-style logo/brand/subtitle unit on the six target pages, while mobile keeps the compact visible brand mark/name without clipping.

**Remaining failures / next agent:** none for this scoped header brand visual parity task. Existing unrelated dirty workspace changes and previously recorded About/header-action/CTA history were preserved. No `ISSUE_LEDGER.md` update was needed because no dedicated recurring row changed status; `VISUAL-QA-001` and `HANDOFF-006` remain strategy guards.

### 2026-05-20 JST - About body/final/footer CTA intent cleanup

**Files changed:** `sampora-website-public-v3/about_sampora_issues_fixed.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. Write scope was limited to the About page `.hero-actions a[data-i18n="heroPrimary"]`, `.cta-section .cta-row a[data-i18n="ctaPrimary"]`, `.cta-section .cta-row a[data-i18n="ctaSecondary"]`, footer Access `a[data-i18n="footerTrial"]`, footer Access `a[data-i18n="footerSales"]`, and this appended handoff entry. Header nav/mobile nav/footer Company Contact, header action CTAs, visible copy, class/layout, contact form, other pages, QA evidence output, delivery folders, zips, staging, commit, and push were intentionally untouched.

**Audit item addressed:** `[CONFIRMED]` About page body/final/footer conversion CTAs still used bare `contact.html#contact-form` or `contact.html` without required `intent=` routing. Pre-edit scoped red check failed on all five target keys: `heroPrimary`, `ctaPrimary`, `ctaSecondary`, `footerTrial`, and `footerSales`.

**Implementation summary:** updated only the five assigned hrefs. `heroPrimary`, `ctaPrimary`, and `footerTrial` now route to `contact.html?intent=start_trial#contact-form`; `ctaSecondary` and `footerSales` now route to `contact.html?intent=contact_sales#contact-form`.

**Verification run:** scoped Node source/DOM-style link check PASS: all five target keys have the expected href, target text/class values stayed unchanged, ordinary `navContact` links remain `contact.html`, and header `bookDemo` / `startTrial` remain `contact.html?intent=book_demo#contact-form` and `contact.html?intent=start_trial#contact-form`. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS.

**Remaining failures / next agent:** none for this scoped About CTA intent cleanup. `CTA-001`, `I18N-001`, `VISUAL-QA-001`, and `HANDOFF-006` remain guard/strategy rows; no ledger status changed in this worker task. Existing unrelated dirty workspace changes were preserved.

### 2026-05-20 JST - Package B Solutions compare table visual correction second pass

**Files changed:** implementation subagent changed only `sampora-website-public-v3/solutions.html`; this Package E documentation record subagent appended only this closeout entry in `SAMPORA_LATEST_REPAIR_HANDOFF.md`. `ISSUE_LEDGER.md` was intentionally not changed because this is a scoped compare-table visual correction; `VISUAL-QA-001` remains a strategy guard. No table data/copy/i18n, `renderCompare` JavaScript, topology, CTA, status/live-dot, `#fit` area, other pages, QA evidence output, delivery folder, zip/package, staging, commit, push, or packaging file was intentionally changed.

**User feedback / strategy clarification:** the user reported that `solutions.html #compare` decision matrix was still not fixed: the left side had too little blank space and the right side too much; the first column should not be first-letter/left aligned and must be center-aligned as a whole column like the other columns, including the first-column header; the amber/yellow portions inside table content must be restored; and future agents must first look at the previous failure experience. The controller clarified that "scannability" was only the old handoff rationale for leaving the first column left aligned as row-heading text, not a user requirement or hard rule. The old handoff had explicitly said "unless future audit explicitly assigns that behavior"; this user message explicitly overrides that old strategy. Future agents must not use "scannability" to move the first column back to left alignment.

**Confirmed red evidence:** the read-only verification agent classified the issue as `[CONFIRMED]`. Current state before repair still had first-column header/body left or first-letter aligned, and `.part` was not amber because current table CSS overrode `.part { color: var(--amber) }`. Red measurements: 1440 EN first header gaps `28/122.64`, body `28/123.86`; 1314 EN header `28/103.83`, body `28/105.05`; 390 EN header `28/67.03`, body `28/68.25`. Computed `.part` color was `rgb(194,208,223)`. Historical/reference `动态/solutions.html` and git history both supported `.part` using `var(--amber)`.

**Implementation summary:** the implementation subagent edited only the `#compare` / compare-table CSS in `sampora-website-public-v3/solutions.html`: restored `#compare .compare-wrap { max-width:1120px; margin-inline:auto }`; changed first-column `th`/`td` to `text-align:center`; and added scoped `#compareTable td.part { color: var(--amber) }`.

**Verification run:** implementation red/green check changed from `RED_FAILURE_COUNT=9` to `GREEN_FAILURE_COUNT=0`. First-column `th`/`td` computed `text-align:center`, with text center delta `0-0.01px`; non-first columns remained centered; `.part` color became `rgb(255,180,84)`. Desktop wrap gaps balanced at 1440 `120/120` and 1314 `73/73`. Mobile document/body overflow stayed `0`, while compare internal scroll remained available at `356/1080`.

**Independent read-only review:** PASS. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. `git diff --check -- sampora-website-public-v3/solutions.html` -> PASS with LF/CRLF warning only. Runtime review covered EN/ZH/HI at 1440, 1314, and 390: document/body overflow `0`; desktop margins balanced; max text-center vs column-center delta `0.008px`; 30 `.part` cells were amber `rgb(255,180,84)`; mobile internal scroll was preserved at `356/1080`.

**Visual acceptance PASS:** desktop/tablet no longer reads as left-tight/right-empty; the table as a whole is centered; the first-column title and cells are naturally centered rather than looking first-letter or left-edge aligned; amber/yellow emphasis is visibly restored; mobile has no page-level horizontal overflow and keeps the expected internal table scroll.

**Remaining failures / next agent:** none for this scoped compare-table fix. Dirty baseline caveat: the repo still has older dirty files/hunks unrelated to this task, so do not treat full `git diff --name-only` as this task scope. Packaging, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, and push remain deferred until explicitly requested.

### 2026-05-20 JST - Solutions sticky jitter regression guard and cross-page sampling

**Files changed:** `SAMPORA_LATEST_REPAIR_HANDOFF.md` only. No page source, visible copy/i18n, CTA/footer/legal, topology, QA evidence output, delivery folder, zip/package, staging, commit, or push was intentionally changed.

**User item checked:** possible `solutions.html` non-fullscreen vertical page jitter similar to the historical `STICKY-003` scroll-threshold loop, followed by a cross-page check for the same class of issue. Owner for the original guard is Package B Solutions; acceptance categories are Navigation/sticky, Visual acceptance, and Scope.

**Issue status table:** `solutions.html` stayed `[REGRESSION] guard PASS`, not `[CONFIRMED]`, because current 60-frame samples did not reproduce the historical `scrollY 58 <-> 23`, `mastTop 35 <-> 0`, or `nav-condensed false <-> true` loop. `index.html`, `resources.html`, `resource-manuals.html`, `plans.html`, and `contact.html` also stayed `[REGRESSION] guard PASS` for this sampled sticky-threshold behavior. `about_sampora_issues_fixed.html` is `[REMOVED]` / not applicable for the same threshold repair because it has ordinary sticky positioning without the `nav-condensed` threshold mechanism.

**Verification run:** read-only source/history subagent confirmed the historical root cause and current `solutions.html` still contains the hysteresis guard in `syncTopChrome()` (`wasCompact ? window.scrollY > 20 : window.scrollY > 56`). Read-only browser subagent sampled `solutions.html` at `1314x900`, `1280x932`, `900x900`, and `390x900`; the non-fullscreen desktop evidence showed `nav-condensed` toggles `0`, stable `mastTop=0`, and no frame-level oscillation. A second read-only subagent sampled `plans.html`, `contact.html`, and `about_sampora_issues_fixed.html`; no `58<->23`-style loop or repeated mast/body-class flip was found. Because the `index.html`, `resources.html`, and `resource-manuals.html` subagent sampling was blocked/incomplete, the controller ran a minimal read-only sanity check only for the missing evidence gap using local Playwright/Chrome, with no file writes. Settled 60-frame samples at `1314x900` and targets `57`/`58` opened `solutions.html`, `index.html`, `resources.html`, `resource-manuals.html`, `plans.html`, `contact.html`, and `about_sampora_issues_fixed.html`; all had `loopFailure=false`, `condensedToggles=0`, stable `mastTop`, and `hero/next` geometry range `0` after settling. `solutions.html` settled at `scrollY=[22]` for target `57` and `scrollY=[23]` for target `58`, with `mastTop=[0]` and no loop.

**Remaining failures / next agent:** no current sticky-threshold jitter failure was confirmed, so no implementation subagent was dispatched and no page source repair was made. `ISSUE_LEDGER.md` was intentionally not updated because `STICKY-003` remains a regression guard rather than a reclassified current failure. If the user can still reproduce visible non-fullscreen shaking manually, the next agent should capture that exact viewport, scroll position, and interaction path first, then rerun the same 60-frame `scrollY` / `mastTop` / `nav-condensed` time-series before editing.

### 2026-05-21 JST - Package B Resources first-viewport dead-band correction

**Files changed:** `sampora-website-public-v3/resources.html`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, and `ISSUE_LEDGER.md`. Page-source write scope was limited to the Resources desktop hero/topology layout selectors: `.hero` bottom padding, `.hero-grid > .reveal:not(.resource-console)`, and `.resource-console:not(.modal-open) .topo-stage`. No visible copy/i18n semantics, CTA/footer/legal/routes, topology JavaScript, `.proof-output*`, `.status`, header brand parity, `brandSub` / `brandSub2`, HI `navContact`, QA evidence output, screenshot, delivery folder, zip/package, staging, commit, or push was intentionally changed.

**Corrective status:** the prior `Package B Resources topology restoration and hero-to-topology alignment` visual PASS was overturned by the user's current screenshot. The old PASS metric `.resource-entry-grid.bottom - #topoStage.bottom = 0px` only proved local alignment between the card grid and topology stage, not the actual content-to-next-section gap. The corrected completion metric for this surface is now `#surfaces.top - Math.max(.resource-entry-grid.bottom, .resource-console.bottom)` plus `hero-grid.bottom - contentBottom` and actual SVG graphic bottom gap.

**Red evidence before repair:** Chrome/Playwright against current files showed `#surfaces.top - Math.max(.resource-entry-grid.bottom, .resource-console.bottom) = 150px` and `hero-grid.bottom - contentBottom = 72px` across EN/ZH/HI at `1365x900`, `1314x900`, and `1440x900`. The source/computed ratio mismatch was confirmed: `.topo-stage` computed `aspect-ratio: 820 / 300` while SVG used `viewBox="0 0 820 270"`, and browser geometry measured `#topoStage.bottom - actualSvgGraphic.bottom = 20.98-22.73px`.

**Implementation summary:** desktop `.hero` bottom padding was reduced from `78px` to `56px`; the left hero reveal now fills the full grid height with `height: 100%` instead of `height: calc(100% - 72px)`; the desktop topology stage ratio now matches the SVG viewBox at `aspect-ratio: 820 / 270`. The left column still distributes the whole content stack with flex `space-between`; the chosen right-side visible bottom alignment is now the full resource console component bottom rather than only the `#topoStage` border, because the console includes the visible ticker below the SVG.

**Verification run:** green browser geometry with Chrome/Playwright covered EN/ZH/HI at `1365x900`, `1314x900`, and `1440x900`: `#surfaces.top - contentBottom` measured about `56px`; `hero-grid.bottom - contentBottom` measured `0px`; `#topoStage.bottom - actualSvgGraphic.bottom` measured `12.16-13.11px`; `.resource-entry-grid.bottom - .resource-console.bottom` measured `0px`; desktop overflow was `0`; `topoDash` remained active; dash offsets changed; packet movement remained visible. Mobile `390x900` EN/ZH/HI retained stacked readability with overflow `0`. Static checks passed: `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; inline `resources.html` scripts parsed OK: 2 scripts; `git diff --check -- sampora-website-public-v3/resources.html SAMPORA_LATEST_REPAIR_HANDOFF.md ISSUE_LEDGER.md` -> PASS with LF/CRLF warnings only.

**Visual acceptance status:** current browser geometry no longer matches the user's screenshot failure mode: a normal user should not see the previous roughly `150px` dead empty band between the hero content and `#surfaces`. The remaining intended gap is the normal section transition spacing of about `56px`; the right topology no longer has the ratio-induced fake bottom void that the old `820 / 300` stage created.

**Remaining failures / next agent:** none known for the scoped Resources first-viewport dead-band correction. Future reviewers must not use `.resource-entry-grid.bottom - #topoStage.bottom = 0px` alone as visual PASS for this surface. Use content-to-next-section, hero-grid-to-content, and actual SVG graphic metrics. Existing unrelated dirty hunks in this workspace were preserved.

### 2026-05-21 JST - Horizontal divider repair closeout

**Files changed:** implementation subagents changed only `sampora-website-public-v3/solutions.html`, `sampora-website-public-v3/resources.html`, and `sampora-website-public-v3/about_sampora_issues_fixed.html`; this docs/process worker appended only this closeout entry in `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No page/source file, shared asset, QA script/evidence output, delivery folder, zip/package, staging, commit, push, or packaging file was changed by this docs/process worker.

**Issue status / scope:** the user screenshot on `solutions.html` showed a selected-region-like unwanted horizontal line / empty divider band. Controller dispatched one read-only verification subagent per page. First-pass status table confirmed repairs only for `solutions.html`, `resources.html`, and `about_sampora_issues_fixed.html`; `index.html`, `plans.html`, `contact.html`, `resource-manuals.html`, `products.html`, and `pricing.html` were `[REMOVED]`, `[VERIFY-FIRST -> not reproduced]`, or redirect helpers, so no repair was dispatched for them.

**Implementation summary:** `solutions.html` received scoped CSS for `#compare` / `#compare + #workflow`, with desktop `52px`, mobile `44px`, and inherited section top border removed only at the compare-to-workflow boundary while compare-wrap/table borders stayed. `resources.html` received scoped CSS for `#surfaces` / `#surfaces + #setup`, with desktop `56px`, mobile `48/50px`, and inherited section top border removed only at the surfaces-to-setup boundary while card/panel/header/footer borders stayed. `about_sampora_issues_fixed.html` removed only the page-level `.section` border-top from About `.section`, preserving card/timeline/footer/component borders.

**Verification run:** implementation subagents reported `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS, `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS, targeted `git diff --check` PASS with LF/CRLF warnings only, and inline script parse PASS where applicable. Read-only final visual review reported PASS for Solutions, Resources, and About: technical/static PASS; runtime geometry PASS; real visual perception / 真实观感 PASS. Solutions desktop/mobile EN/ZH/HI had `#workflow border=0px none`, overflow `0`, compare-wrap/table borders retained, and no full-width divider. Resources desktop/mobile EN/ZH/HI had `#setup border-top=0px none`, overflow `0`, local borders retained, and no detached divider. About desktop/mobile EN/ZH/HI target sections `#story/#proof/#what/#timeline` had top/bottom borders `0`, boundary scan found `0` full-width line candidates, and component borders stayed intact.

**Remaining failures / next agent:** none for the scoped horizontal divider issue. Packaging, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, and push remain deferred unless explicitly requested. `VISUAL-QA-001` and `HANDOFF-006` remain strategy guards; no `ISSUE_LEDGER.md` reclassification was needed. Existing unrelated dirty workspace changes from prior rounds were preserved and not reviewed.

### 2026-05-21 JST - Package B Resources proof-output left-side inline-number correction

**Files changed:** `sampora-website-public-v3/resources.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. Page-source write scope was limited to the assigned `.proof-output`, `.proof-output-row`, `.proof-output-num`, content-span, connector, and matching mobile CSS rules for Resources `What this path produces`. No visible copy, DOM text, JavaScript, topology, nav/header, CTA/footer/legal, other pages, QA evidence output, delivery folder, zip/package, staging, commit, push, or packaging file was intentionally changed.

**Audit item addressed:** `[CONFIRMED]` newest user visual feedback after the prior second-pass compact list: `01/02/03` had been moved to each row's right side, but the user explicitly said `放在左边` and noted other numbering patterns are left-side. Owner package is Package B Resources; acceptance category is Visual acceptance / real visual perception.

**Red evidence before repair:** scoped source check showed `.proof-output-row` was `display:block` with `padding-right` reserved for a right badge, and `.proof-output-num` used `position:absolute; right:14px` on desktop plus `right:12px` on mobile. The vertical connector was already disabled, but the row number placement contradicted the left-side numbering requirement.

**Implementation summary:** changed the compact rows to `display:flex; align-items:flex-start` with a small gap, made `.proof-output-num` `position:static; flex:0 0 auto`, and removed the right-side padding reservation. The number badge remains a very light inline pill on the left of each row, close to the title start. The connector rule remains `content:none` / `display:none`, so no independent rail, vertical line, timeline, or blue vertical block is restored.

**Verification run:** scoped static guard PASS: `.proof-output-row` renders as flex, `.proof-output-num` is static with no `right:` in the number rule, connector pseudo content is `none`, and mobile uses the smaller left-inline gap. `git diff --check -- sampora-website-public-v3/resources.html` passed with LF/CRLF warning only. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. Chrome/Playwright runtime geometry covered Resources EN/ZH/HI at desktop `1440x900` and mobile `390x900`: all samples had document overflow `0`, `rowCount=3`, row display `flex`, number position `static`, connector `afterContent=none`, no overlap, and no right-badge placement. Desktop row metrics were `leftGap=15px`, `numToCopyGap=11px`, and `numRightGap=639.81px`; mobile row metrics were `leftGap=14px`, `numToCopyGap=9px`, and `numRightGap=272.83px`.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; real visual perception / 真实观感 PASS. Current rendered desktop and mobile crops show each row as a normal compact list item: a subtle left-side number badge followed by title and description. The right side no longer carries the number, and the left side does not read as a continuous rail, connector line, timeline, or vertical blue block.

**Remaining failures / next agent:** none for this scoped proof-output left-side inline-number correction. `ISSUE_LEDGER.md` was checked; no dedicated recurring row matches this specific one-off Resources proof-output placement failure, so the ledger was not updated. `VISUAL-QA-001` remains the general strategy guard. Existing unrelated dirty hunks in `resources.html`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, `ISSUE_LEDGER.md`, other page files, untracked QA scripts, archives, and reference folders were preserved and not staged.

### 2026-05-21 JST - Package B Solutions compare-wrap width rollback

**Files changed:** implementation subagent changed only `sampora-website-public-v3/solutions.html`; this Sampora documentation record subagent appended only this closeout entry in `SAMPORA_LATEST_REPAIR_HANDOFF.md`. `ISSUE_LEDGER.md` was intentionally not changed because this is a scoped compare-wrap width rollback; `VISUAL-QA-001` remains a strategy guard. No QA evidence output, delivery folder, zip/package, staging, commit, push, or packaging file was changed by this documentation subagent.

**User feedback / correction:** the user gave severe feedback that nobody authorized shrinking the entire `solutions.html #compare` table width. The previous round's change from the intended large table to `max-width:1120px` was a mistaken edit. The current task was only to restore the large table width.

**Keep / do not rollback:** the first column's whole-column center alignment and `.part` amber emphasis are correct and must be preserved. Future rollback work must not undo first-column centering or `td.part` amber.

**Confirmed red evidence before rollback:** read-only verification confirmed `[CONFIRMED]`: before this rollback, current `#compare .compare-wrap` was `width:100%; max-width:1120px; margin-inline:auto`, changed from the earlier large-table strategy of `max-width:100%; margin-inline:0`. Prior handoff target evidence had recorded full-width behavior: at 1440, `1360/1360px`; at 1314, `1251/1251px`. Current browser measurement before rollback showed 1440 container `1360px`, wrap `1120px`, unused `240px`, margins `120/120`; and 1314 container `1266px`, wrap `1120px`, unused `146px`, margins `73/73`. The same check confirmed the first column center alignment and `.part` amber were already correct.

**Implementation summary:** implementation agent changed only the two assigned `#compare .compare-wrap` lines in `sampora-website-public-v3/solutions.html`: restored `max-width:100%` and `margin-inline:0`. The agent did not change first-column centering, `td.part` amber, `#compareTable` other layout rules, table data/copy/i18n, `renderCompare`, topology, CTA, status/live-dot, `#fit` area, other pages, QA output, delivery/zip, staging, commit, or push.

**Verification run:** post-implementation browser checks passed for EN/ZH/HI. At 1440, wrap `1360px`, container `1360px`, delta `0`, margins `0/0`. At 1314, wrap `1266px`, container `1266px`, delta `0`, margins `0/0`. First-column center max delta stayed `0.01px`; `.part` color stayed `rgb(255,180,84)`. Mobile 390 kept document/body overflow `0` and internal compare-table scroll `356/1080`.

**Independent read-only review:** PASS. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. `git diff --check -- sampora-website-public-v3/solutions.html` -> PASS with LF/CRLF warning only. Runtime EN/ZH/HI checks at 1440, 1314, and 390 passed.

**Visual acceptance PASS:** desktop/tablet table is restored to full container width and no longer reads as an unintended `1120px` small table. The first column remains centered and the amber emphasis remains visible. Mobile has no page-level horizontal overflow and preserves the intended internal table scroll.

**Remaining failures / next agent:** none for this scoped compare-wrap width rollback. Dirty baseline caveat: the current repo has historical dirty hunks, so do not use full `git diff` to infer this task scope. Packaging, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, and push remain deferred until explicitly requested.

- **Pitfall / symptom:** future agents may interpret "left/right whitespace balance" as permission to shrink the entire `solutions.html #compare` table.
- **Root cause:** the previous correction treated outer table width as the balancing lever even though the user had not authorized reducing the full decision-matrix footprint.
- **Prevention rule:** for `solutions.html #compare`, desktop/tablet table width must remain full container width unless the user explicitly authorizes width reduction. Alignment fixes must be done inside cells/columns without changing overall table width.
- **Verification:** browser checks must confirm `.compare-wrap` equals its container on desktop/tablet with delta `0` and margins `0/0`, while separately confirming first-column center alignment, `.part` amber, and mobile no page-level overflow with internal table scroll preserved.
- **Owner package:** Package B Solutions / visual acceptance review.

### 2026-05-21 JST - Package B Solutions supplier badge copy cleanup

**Files changed:** `sampora-website-public-v3/solutions.html` and this handoff entry. No compare table, topology, CTA, header, footer, other page, QA script, delivery folder, zip/package, staging, commit, or push was intentionally changed.

**Audit item addressed:** `[CONFIRMED]` `solutions.html #solutionCards` second card badge had an extra `Recommended` prefix in EN and matching recommendation wording in ZH. Owner package is Package B Solutions/Resources; acceptance categories are Language/i18n, Visual acceptance, and Scope.

**Verification run:** source DATA check confirmed `DATA.en.solutions[1].badge = "for sample suppliers"`, `DATA.zh.solutions[1].badge = "面向在线样本供应商"`, and `DATA.hi.solutions[1].badge = "Sample Supplier टीमों के लिए"`. Playwright file-URL DOM check at `1440x900` across EN/ZH/HI confirmed `#solutionCards .plan-card.network .badge` rendered as `FOR SAMPLE SUPPLIERS`, `面向在线样本供应商`, and `SAMPLE SUPPLIER टीमों के लिए`, with `text-transform: uppercase` and page overflow `0`. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `git diff --check -- sampora-website-public-v3/solutions.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> PASS with LF/CRLF warnings only before this handoff append.

**Acceptance status separation:** technical/static PASS; runtime DOM PASS; real visual perception / 真实观感 PASS. The second solution card badge now matches the other two cards' neutral `for ...` format instead of reading as a special recommendation.

**Remaining failures / next agent:** none for this scoped supplier badge copy cleanup. `ISSUE_LEDGER.md` was checked and intentionally left unchanged because no dedicated recurring ID exists; I18N/terminology/visual remain general guards.

### 2026-05-21 JST - Package B Solutions supplier badge short-pill guard

**Files changed:** `sampora-website-public-v3/solutions.html` and this handoff entry. Page-source write scope was limited to `DATA.en/zh/hi.solutions[1].badge`, base `.badge` CSS, and the mobile badge override inside `final-mobile-overflow-fix`. No `#compare` table width/column/color rules, topology, CTA, header/footer, other page, QA script, delivery folder, zip/package, staging, commit, or push was intentionally changed.

**Audit item addressed:** `[CONFIRMED]` newest user visual target: the second solution-card badge must read like a short pill such as `FOR PANEL TEAMS`, with all words on one row, not a long strip, not wrapped, and not enlarging the card. Owner package is Package B Solutions/Resources; acceptance categories are Language/i18n, Visual acceptance, and Scope. No dedicated recurring ledger ID exists; I18N/TERMINOLOGY/VISUAL remain general guards.

**Red evidence before repair:** source check showed `DATA.en.solutions[1].badge = "for sample suppliers"` and base `.badge` allowed `white-space: normal` plus `overflow-wrap: anywhere`; mobile override also forced `.badge` back to `white-space:normal; overflow-wrap:anywhere`. Chrome/CDP browser evidence at `1440x900` showed EN second badge `for sample suppliers` rendered with `lineCount=2`, `height=40`, while first/third badges were one line at `27px`; HI second badge also rendered with `lineCount=2`, `height=42`. Page overflow remained `0`, but the second badge did not match the short-pill target.

**Implementation summary:** changed the second solution badge copy to `For supplier teams`, `面向供应商团队`, and `Supplier टीमों के लिए`. Added a scoped single-line pill guard to `.badge`: `width: fit-content`, `white-space: nowrap`, `overflow-wrap: normal`, `word-break: normal`, and left-aligned pill text. The mobile badge override now keeps the same `fit-content` / `nowrap` / normal-wrap guard instead of re-enabling wrapping.

**Verification run:** source checks confirmed the three new EN/ZH/HI values and the single-line CSS guard. Chrome/CDP browser checks covered EN/ZH/HI at desktop `1440x900` and mobile `390x900`: second badge text rendered as `For supplier teams`, `面向供应商团队`, and `Supplier टीमों के लिए`; computed `white-space=nowrap`, `overflow-wrap=normal`, `lineCount=1`, right overflow `0`, and document/body overflow `0` in all six states. Desktop card heights stayed equal within each language. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. `git diff --check -- sampora-website-public-v3/solutions.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> PASS with LF/CRLF warnings only.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; real visual perception / 真实观感 PASS. A normal user should now see the second solution-card badge as a short, single-row pill comparable to the first-card `For panel teams` badge; it no longer wraps or reads as a long recommendation strip, and it does not enlarge the card or create page overflow on desktop or mobile.

**Remaining failures / next agent:** none for this scoped supplier badge short-pill guard. Existing unrelated dirty workspace changes were preserved and not reviewed. Packaging, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, and push remain deferred unless explicitly requested.

### 2026-05-21 JST - Package B Resources console ticker sweep restoration

**Files changed:** `sampora-website-public-v3/resources.html`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, and `ISSUE_LEDGER.md`. Page-source write scope was limited to the existing Resources `.console-ticker:after` sweep layer and a scoped `#consoleTicker:after` reduced-motion fallback. No `topoDash`, `topoScan`, `consoleSheen`, SVG flow/packet, topology manual-lock logic, ticker text/i18n data, other pages, QA evidence output, delivery folder, zip/package, staging, commit, or push was intentionally changed.

**Audit item addressed:** `[CONFIRMED]` newest Package B Resources request: restore the bottom ticker `#consoleTicker` sweep animation by reusing the Solutions `console-ticker:after` / `tickerSweep` pattern. Owner package is Package B Resources; acceptance categories are Topology motion, Visual acceptance, and Scope.

**Red evidence before repair:** current `resources.html` had `div.console-ticker#consoleTicker`, base `.console-ticker`, `.console-ticker:before`, `.console-ticker:after`, and `@keyframes tickerSweep`, but `.console-ticker:after` lacked `animation: tickerSweep 4.8s linear infinite;` and `pointer-events: none;`. Current diff/handoff inspection showed unrelated dirty hunks in `resources.html`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, and `ISSUE_LEDGER.md`, but no active or recent overlapping write ownership for `.console-ticker:after` / `#consoleTicker`.

**Implementation summary:** added `animation: tickerSweep 4.8s linear infinite;` and `pointer-events: none;` to Resources `.console-ticker:after`. Added a scoped `@media (prefers-reduced-motion: reduce)` fallback for `#consoleTicker:after` that disables animation, parks the light at `left: calc(100% - 68px)`, and lowers opacity to `.2`. Existing `tickerSweep` keyframes were reused and not duplicated.

**Verification run:** `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. Inline `resources.html` script parse -> `2/2` parsed. Chrome/Playwright file-URL checks using the repo-local Playwright dependency covered Resources EN/ZH/HI at desktop `1440x900` and mobile `390x900`: every sample had `#consoleTicker::after animationName=tickerSweep`, `animationDuration=4.8s`, `animationIterationCount=infinite`, `pointerEvents=none`, non-empty ticker text, horizontal overflow `0`, and changing pseudo-element `left` over `700ms`. Reduced-motion browser context measured `animationName=none`, right-side `left`, and `opacity=0.2`.

**Acceptance status separation:** technical/static PASS; runtime geometry/motion PASS; real visual perception / 真实观感 PASS. A normal user should now see the Resources bottom ticker sweep move again in the same restrained left-to-right pattern as the Solutions topology ticker, without changing ticker text, topology flow motion, layout, or page width.

**Remaining failures / next agent:** none for this scoped `#consoleTicker` sweep restoration. `TOPOLOGY-001` remains `[REGRESSION]`; `VISUAL-QA-001` and `HANDOFF-006` remain `[STRATEGY]`. Existing unrelated dirty hunks and untracked folders were preserved and not staged. Packaging, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, and push remain deferred unless explicitly requested.

### 2026-05-21 JST - Header brand visual parity QA strategy guard

**Files changed:** `ISSUE_LEDGER.md`, `ACCEPTANCE_TESTS.md`, and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No page HTML, CSS, visual implementation, QA script, QA output, delivery folder, zip/package, staging, commit, or push was intentionally changed.

**Implementation summary:** added dedicated recurring guard `HEADER-BRAND-001` while preserving `VISUAL-QA-001` as the general visual-acceptance bucket. `ACCEPTANCE_TESTS.md` now has a header brand visual parity acceptance item that requires separate reports for data/nav/header text consistency and About-referenced visual brand parity. Future header/logo/brand/Sampora mark/selected header region/About-reference tasks must run both `header-consistency-check.mjs` and `header-brand-visual-check.mjs`; the former must not substitute for the latter.

**Verification run:** `node "sampora-website-public-v3\qa-evidence\header-brand-visual-check.mjs"` -> PASS across 6 target pages against About at `1440x900` plus mobile `390x900` brand visibility across EN/ZH/HI. `node "sampora-website-public-v3\qa-evidence\header-consistency-check.mjs"` -> PASS across 7 main pages, with redirect pages skipped. `node "sampora-website-public-v3\qa-evidence\active-handoff-mojibake-guard.mjs"` -> PASS. `git diff --check -- ISSUE_LEDGER.md ACCEPTANCE_TESTS.md SAMPORA_LATEST_REPAIR_HANDOFF.md` -> PASS with LF/CRLF warnings only. Targeted `Select-String` checks found `HEADER-BRAND-001`, `header-brand-visual-check.mjs`, `header-consistency-check.mjs`, and `real visual perception` / `真实观感` in the strategy positions.

**Remaining failures / next agent:** none found for the scoped docs/QA strategy task. If a future screenshot or browser evidence contradicts a header PASS, downgrade that PASS to needs recheck and require a fresh visual parity run, not only data/nav consistency.

- **Pitfall / symptom:** header/logo/brand work can be marked PASS because data, nav order, hrefs, and header text checks pass, while Resources or another page still shows the wrong left-side visual brand/logo block instead of reusing the About reference.
- **Root cause:** the previous round treated data reuse / navigation consistency as equivalent to visual block reuse. The script coverage did not match the user's screenshot target: `header-consistency-check.mjs` checked data/nav/header text, but the failure was in rendered brand/logo geometry and visual styling.
- **Prevention rule:** when a task mentions header, logo, brand, Sampora mark, selected header region, or About reference, first check `HEADER-BRAND-001`. Run and report `header-consistency-check.mjs` and `header-brand-visual-check.mjs` separately. Do not use data/nav/header text PASS as visual parity PASS. Required closeout has three layers: technical/static, runtime geometry, and real visual perception / 真实观感.
- **Verification:** `node "sampora-website-public-v3\qa-evidence\header-consistency-check.mjs"` must PASS for data/nav/header text consistency, and `node "sampora-website-public-v3\qa-evidence\header-brand-visual-check.mjs"` must PASS for About-referenced brand visual parity across EN/ZH/HI desktop/mobile. For a same-class repair, the red check before fixing must fail through the visual script or equivalent browser evidence, then PASS after repair.
- **Owner package:** shared header / Package A/B/C/D implementation owner for page repairs, with Package E QA strategy and read-only visual review responsible for enforcing the guard.

### 2026-05-21 JST - Package B Resources hero eyebrow copy shortening

**Files changed:** `sampora-website-public-v3/resources.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. Page-source write scope was limited to the Resources hero eyebrow fallback and the EN/ZH/HI `heroKicker` values. No other copy keys, brand/nav/contact/footer/CTA/legal text, CSS/layout, JavaScript, other pages, QA evidence output, delivery folder, zip/package, staging, commit, push, or packaging file was intentionally changed. `ISSUE_LEDGER.md` was checked and intentionally left unchanged because no dedicated recurring row matches this one-off hero eyebrow length issue.

**Audit item addressed:** `[CONFIRMED]` visible-copy length issue: Resources hero eyebrow `RESOURCES · INTERFACE PLAYBOOKS` and localized equivalents were too long for the current hero. Historical lookup did not prove a shorter original string; this round follows the user's current feedback by shortening the visible label.

**Verification run:** write-before red check found fallback `RESOURCES - INTERFACE PLAYBOOKS`, EN `heroKicker: "RESOURCES · INTERFACE PLAYBOOKS"`, ZH `heroKicker: "资源中心 · 接口入口手册"`, and HI `heroKicker: "संसाधन · इंटरफ़ेस गाइड"`. Post-repair static grep found only fallback `RESOURCES`, EN `heroKicker: "RESOURCES"`, ZH `heroKicker: "资源"`, and HI `heroKicker: "संसाधन"` for the touched surface, with no remaining `INTERFACE PLAYBOOKS`, `接口入口手册`, or `इंटरफ़ेस गाइड` hits in the hero fallback/`heroKicker` values. `git diff --check -- sampora-website-public-v3/resources.html SAMPORA_LATEST_REPAIR_HANDOFF.md` passed with LF/CRLF warnings only. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. Local HTTP Playwright runtime checks at `1440x900` and `390x900` passed EN -> ZH -> HI -> EN switching: rendered eyebrow values were `RESOURCES`, `资源`, `संसाधन`, and back to `RESOURCES`; each sample had `lineCount=1`, horizontal overflow `0`, inside-viewport true, above-title true, and inside-hero true. Text widths were desktop EN/ZH/HI `73.27px / 25.09px / 40.95px`, mobile EN/ZH/HI `73.27px / 25.09px / 40.95px`.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; real visual perception / 真实观感 PASS. A normal user now sees a short Resources eyebrow in each language instead of the long interface-playbooks phrase; the label does not wrap, overflow, or disrupt hero placement on desktop or mobile.

**Remaining failures / next agent:** none for this scoped copy-shortening task. Historical lookup did not prove a shorter original string, so this should stay recorded as a user-feedback-driven shortening rather than a rollback to a proven historical value. Dirty baseline caveat: the current workspace still has existing unrelated dirty baseline changes, including multiple page HTML files, `ACCEPTANCE_TESTS.md`, `ISSUE_LEDGER.md`, untracked QA/reference directories, and non-this-round `resources.html` hunks for CSS/layout/ticker/header brand/`brandSub`/HI `navContact`; this round owns/reviews only the hero eyebrow fallback plus EN/ZH/HI `heroKicker` shortening. Packaging, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, and push remain deferred unless explicitly requested.

### 2026-05-21 JST - Package B Resources hero eyebrow fit-to-content correction

**Files changed:** `sampora-website-public-v3/resources.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. Page-source write scope was limited to the Resources `.eyebrow` fit guard, hero eyebrow fallback span, and EN/ZH/HI `heroKicker` values. No other copy keys, topology, ticker, right console, header/nav/footer, CTA links, QA outputs, delivery folders, zips, staging, commit, push, or packaging file was intentionally changed. `ISSUE_LEDGER.md` was checked; no recurring row status changed because this is a one-off Resources hero visual/copy correction guarded by `I18N-001`, `TERMINOLOGY-001`, `VISUAL-QA-001`, and `HANDOFF-006`.

**Audit item addressed:** `[CONFIRMED]` Package B Resources hero eyebrow regression. The user's intended repair was to keep the semantic long eyebrow and make the outer pill shrink to the text; the previous round shortened/deleted the text while the desktop frame still stretched too wide.

**Red evidence before repair:** source showed fallback `RESOURCES`, EN `heroKicker: "RESOURCES"`, ZH `heroKicker: "资源"`, and HI `heroKicker: "संसाधन"`. Current handoff had an overlapping historical entry for hero eyebrow copy shortening, but current `git diff -- sampora-website-public-v3/resources.html` showed no active dirty diff in `resources.html`; no active or recent overlapping ownership was found for `.eyebrow` / `heroKicker`. Chrome/Playwright file-URL red geometry at `1440x900` showed the shortened desktop eyebrow border was `702.14px` wide while text widths were only EN/ZH/HI `73.27px / 25.09px / 40.95px`, proving the frame still stretched across the column. Mobile `390x900` already fit text plus `46px` padding/dot/gap.

**Implementation summary:** restored the long semantic values: fallback and EN `RESOURCES · INTERFACE PLAYBOOKS`, ZH `资源中心 · 接口入口手册`, and HI `संसाधन · इंटरफ़ेस गाइड`. Added a scoped `.eyebrow` guard with `align-self:flex-start`, `width:fit-content`, `max-width:100%`, `white-space:nowrap`, `overflow-wrap:normal`, and `word-break:normal` so the pill frame fits its content instead of stretching as a flex-column item.

**Verification run:** source checks confirmed restored EN/ZH/HI `heroKicker` values and the fit-content/nowrap CSS guard. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. `git diff --check -- sampora-website-public-v3/resources.html` -> PASS with LF/CRLF warning only. Chrome/Playwright file-URL checks using the repo-local Playwright dependency covered Resources EN/ZH/HI at desktop `1440x900` and mobile `390x900`: rendered texts matched the restored long labels, `lineCount=1`, `whiteSpace=nowrap`, `overflowWrap=normal`, `wordBreak=normal`, `alignSelf=flex-start`, inside-hero true, above-title true, document/body overflow `0`, and right overflow `0`. Pill width now equals text width plus `46px` for padding/dot/gap/border: desktop EN/ZH/HI `298.34px / 195.83px / 188.83px`; mobile EN/ZH/HI the same.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; real visual perception / 真实观感 PASS. A normal user now sees the full intended Resources hero eyebrow text in each language, with the outer frame fitted to the label rather than deleted text inside a long unchanged desktop strip.

**Remaining failures / next agent:** none for this scoped hero eyebrow fit-to-content correction. The short write lock was held only for the actual `resources.html` `.eyebrow` / hero fallback / `heroKicker` write and released immediately afterward. Existing unrelated dirty workspace changes were preserved and not reviewed. Packaging, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, and push remain deferred unless explicitly requested.

### 2026-05-21 JST - Package B Solutions solution-card badge position correction

**Files changed:** `sampora-website-public-v3/solutions.html` scoped to `#solutionCards .plan-top` / `.badge` CSS, plus this handoff entry. No `#compare` table width/column/color rules, topology, CTA, header/footer, other page, QA script, delivery folder, zip/package, staging, commit, or push was intentionally changed. `ISSUE_LEDGER.md` was checked and intentionally left unchanged per assignment.

**Audit item addressed:** `[CONFIRMED]` newest user correction: the prior overflow repair moved all `#solutionCards` badges to the title column's left edge, so the second-card badge no longer matched the original/right-side card badge position pattern. Owner package is Package B Solutions/Resources; acceptance categories are Visual acceptance and Scope.

**Red evidence before repair:** Playwright file-URL check at `1440x900` showed all three badges left-aligned under the title column, with `leftOffset=25px`; second badge was at `left=524.33`, `top=1467.77`, text stayed inside the pill, but the visual position pattern failed the requested reference.

**Implementation summary:** restored desktop `#solutionCards .plan-top` to two columns with `grid-template-columns: minmax(0, 1fr) max-content`, kept the left text column shrinkable, and right-aligned the badge with `justify-self:end`, `align-self:start`, `flex:0 0 auto`, and `max-width:none`. Added a scoped `max-width:680px` override so mobile keeps the existing single-column layout and badge start alignment.

**Verification run:** Playwright file-URL checks covered EN/ZH/HI at `1907x1278`, `1440x900`, `1280x900`, `1200x900`, and `390x900`. Desktop samples all had the second badge inside the network card, text range inside the badge, no h3/short/left-column overlap, document/body overflow `0`, third-card text clearance `53px`, and consistent badge right offsets of `25px` across all three cards. Mobile `390x900` returned to one-column `#solutionCards .plan-top`, badge left offsets `25px`, no card/text overflow, no title overlap, and document/body overflow `0`.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; real visual perception / 真实观感 PASS. A normal desktop user should now see the badge back in the same top-right card position pattern as the neighboring solution cards, while the second badge remains a contained single-line pill and mobile keeps the expected stacked layout.

**Remaining failures / next agent:** none found for this scoped badge-position correction. Existing unrelated dirty workspace changes were preserved and not reviewed. Packaging, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, and push remain deferred unless explicitly requested.

### 2026-05-21 JST - Package B Solutions residual compare-to-workflow grid-line mask

**Files changed:** `sampora-website-public-v3/solutions.html` by the implementation subagent, plus this handoff entry. Page-source write scope was limited to CSS around `#compare` / `#compare + #workflow`: local pseudo-element masks for the compare-to-workflow spacer, desktop `52px`, mobile `44px`, with `rgba(7,18,33,.985)`. No copy, i18n, table width/color/alignment, cards, topology, CTA/header/footer, QA output, delivery folder, zip/package, staging, commit, or push was intentionally changed.

**Audit item addressed:** `[CONFIRMED]` newest 2026-05-21 user screenshot showed a remaining horizontal line between the `solutions.html` compare table and the `04 / 运营路径` workflow heading. Controller downgraded the old visual PASS to needs recheck per `VISUAL-QA-001` because the prior acceptance checked `#workflow border-top=0` too narrowly and missed the selected empty band.

**Root cause:** read-only root-cause review confirmed the visible free-floating line came from the global `body` background grid (`linear-gradient` 1px lines with `background-size:44px 44px`) showing through the transparent compare-to-workflow spacer. Legitimate compare-wrap/table borders were retained and were not the selected-region line source; the previous repair only removed `#workflow border-top`.

**Verification run:** implementation verification reported `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS, `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS, and `git diff --check -- sampora-website-public-v3/solutions.html` -> PASS with LF/CRLF warnings only. EN/ZH/HI at `1440x900` and `390x900` had `#workflow border-top=0`, overflow `0`, compare-wrap border retained, desktop full-width table retained, mobile internal scroll retained, and spacer crop scan found `0` horizontal line candidates. Read-only final review passed technical/static, runtime geometry, and real visual perception / 真实观感; ZH desktop band from `wrap.bottom 404.94` to `kicker.top 494.94` had continuous mask coverage with `maxUncoveredGapPx=0`, and EN/HI desktop plus mobile samples also had `maxUncoveredGapPx=0`. CDP screenshot capture timed out, so final review relied on browser-rendered geometry and computed paint coverage rather than screenshot pixels.

**Remaining failures / next agent:** none for the scoped residual grid-line issue. `VISUAL-QA-001` remains a strategy guard; `ISSUE_LEDGER.md` was checked and no row update is needed for this one-off pitfall. Packaging, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, and push remain deferred.

- **Pitfall / symptom:** old PASS missed the residual line because the selected empty band still exposed the body grid.
- **Root cause:** acceptance checked only `#workflow border-top` and component borders, not all paint sources in the selected region.
- **Prevention rule:** for visual line/band complaints, reviewers must inspect all paint sources in the selected region: element borders, pseudo-elements, shadows, and ancestor/body backgrounds/grid, and must prove no free-floating line remains in the actual empty band.
- **Verification:** browser geometry/computed paint coverage or screenshot/crop scan of the selected region, across relevant languages/viewports; metric like `maxUncoveredGapPx=0` or no line candidates.
- **Owner package:** Controller visual acceptance + Package B Solutions for this surface.

### 2026-05-21 CST - Package B Solutions Supplier Network badge text expansion

**Files changed:** `sampora-website-public-v3/solutions.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. Page-source write scope was limited to the second `#solutionCards` / Supplier Network badge values in EN/ZH/HI. No badge CSS, card positioning, compare table, topology, workflow, CTA, header/footer, QA output, delivery folder, zip/package, staging, commit, or push was intentionally changed.

**Audit item addressed:** `[CONFIRMED]` newest browser comment on `solutions.html`: the selected second-card badge currently rendered `FOR SUPPLIER TEAMS`; user requested `FOR SUPPLIER OPERATIONS TEAMS` and required it to stay on one line. Owner package is Package B Solutions/Resources; acceptance categories are Language/i18n, Visual acceptance, and Scope. `ISSUE_LEDGER.md` was checked; no dedicated recurring row changed because this is a one-off badge copy adjustment guarded by `I18N-001`, `TERMINOLOGY-001`, `VISUAL-QA-001`, and `HANDOFF-006`.

**Red evidence before repair:** current browser evidence selected `div#solutionCards > article.plan-card.network:nth-of-type(2) > div.plan-top > span.badge` with text `FOR SUPPLIER TEAMS`. Source check found EN `badge: "For supplier teams"`, ZH `badge: "面向供应商团队"`, and HI `badge: "Supplier टीमों के लिए"`. Current diff/handoff inspection found unrelated dirty handoff entries but no active or recent overlapping write ownership for the Supplier Network badge value.

**Implementation summary:** changed the second solution-card badge copy to EN `For supplier operations teams`, ZH `面向供应商运营团队`, and HI `Supplier operations टीमों के लिए`. Existing `nowrap` / normal wrapping guards were preserved; no CSS change was needed.

**Verification run:** `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. Source checks found only the new EN/ZH/HI values for the touched badge. Chrome/Playwright file-URL checks using repo-local Playwright and system Chrome covered Solutions EN/ZH/HI at desktop `1440x900` and mobile `390x900`: each sample had the expected rendered text, `white-space=nowrap`, `overflow-wrap=normal`, `word-break=normal`, `lineCount=1`, badge inside card true, text inside badge true, no title overlap, and document/body horizontal overflow `0`. Badge widths were desktop EN/ZH/HI `228.81px / 120.81px / 226.88px` inside a `441.33px` card and mobile EN/ZH/HI `228.28px / 120.81px / 220.08px` inside a `358px` card.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; real visual perception / 真实观感 PASS. A normal user should now see the second-card badge as the requested single-line `FOR SUPPLIER OPERATIONS TEAMS` pill without wrapping, clipping, title collision, or page overflow on desktop or mobile.

**Remaining failures / next agent:** none for this scoped Supplier Network badge text expansion. Existing unrelated dirty workspace changes were preserved and not reviewed. Packaging, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, and push remain deferred unless explicitly requested.

### 2026-05-21 JST - Package B Resources Preview A first-viewport topology proportion repair

**Files changed:** `sampora-website-public-v3/resources.html`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, and `ISSUE_LEDGER.md`. Page-source write scope was limited to the Resources desktop hero/topology layout selectors in the assigned CSS block: `.hero` desktop top padding, `.hero-grid`, `.hero-grid > .reveal:not(.resource-console)`, `.hero .eyebrow + h1`, `.hero-sub`, `.cta-row`, and `.resource-console:not(.modal-open) .topo-stage`. No visible copy/i18n, topology JavaScript, topology SVG, ticker/right-console content, header/nav/footer, CTA links, QA evidence output, delivery folders, zips, staging, commit, push, or packaging file was intentionally changed.

**Audit item addressed:** `[CONFIRMED]` Package B Resources first-viewport visual regression. User approved Preview A from `output/playwright/resources-hero-preview-a.png`: restore the right topology visual size/proportion from the flattened `820 / 270` repair, keep the left cards bottom-aligned with the right console/topology bottom, and distribute the left content evenly instead of clustering.

**Red evidence before repair:** current source and browser evidence showed `.resource-console:not(.modal-open) .topo-stage` at `aspect-ratio: 820 / 270`; Chrome at `1440x900` measured `#topoStage` `557.84x183.67px` where Preview A target `820 / 300` would be about `204.09px` tall at that width. Left stack used `display:flex; justify-content:space-between`, and h1 margins were `0px`, matching the previous flattened source rather than the user-approved Preview A direction.

**Implementation summary:** implemented Preview A-equivalent desktop CSS: `.hero` desktop `padding-top:64px`; `.hero-grid` stays stretched; the left reveal is now a five-row grid with `align-content:space-between`, `row-gap:clamp(16px, 2vh, 24px)`, and `min-height:100%`; h1 top/bottom margins use `clamp(12px, 1.7vh, 18px)`; `.hero-sub` and `.cta-row` keep zero bottom margin; `.topo-stage` returns to `aspect-ratio: 820 / 300`.

**Verification run:** `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. Inline `resources.html` scripts parsed `2/2`. Source grep confirmed the Preview A CSS values. Chrome/Playwright using system Chrome covered Resources EN/ZH/HI at `1280x826`, `1440x900`, and mobile `390x900`; a verification screenshot was written to `output/playwright/resources-hero-after-preview-a-source.png`.

**Runtime geometry PASS:** desktop `#topoStage` returned to `820 / 300`: at `1440x900`, stage `557.84x204.08px`, expected `820/300` height `204.09px`, old `820/270` height would have been `183.68px`; at `1280x826`, stage `498.67x182.44px`, expected `182.44px`, old `820/270` height would have been `164.20px`. EN/ZH/HI desktop `.resource-entry-grid.bottom - .resource-console.bottom` stayed within `0.05px`; `#surfaces` gap stayed `56px`; horizontal overflow was `0`. Left content used `display:grid`, `align-content:space-between`, readable distributed gaps, and h1 margins around `14.04px` at `1280x826` and `15.3px` at `1440x900`. Mobile `390x900` stayed stacked with `display:block`, stage height `240px`, and overflow `0`.

**Topology motion PASS:** desktop EN/ZH/HI samples reported `animationName=topoDash`, changing dash offsets, and packet movement. Mobile EN rerun after active-flow wait reported `topoDash -> topoDash`, dash offset `-23.615px -> -43.9239px`, and packet movement `45.05px`; ZH/HI mobile also showed active dash changes and packet movement.

**Visual acceptance PASS / 真实观感:** a normal user now sees the right topology restored to the taller, fuller Preview A proportion rather than the flattened 270-ratio panel. The left headline, copy, CTAs, and three cards are spread through the left column with visible top/mid/bottom rhythm, and the card bottoms remain parallel with the right console bottom without the page looking squeezed, top-clustered, or bottom-clustered.

**Remaining failures / next agent:** none for this scoped Resources first-viewport Preview A repair. The short write lock was held only for the actual `resources.html` desktop hero/topology CSS block write and released immediately after writing; all verification was read-only. Existing unrelated dirty workspace changes, including other files, screenshot artifacts, delivery folders/zips, and unrelated `resources.html` eyebrow/i18n hunks, were preserved and not staged.

### 2026-05-21 CST - Package B Solutions card short-label and border-motion cleanup

**Files changed:** `sampora-website-public-v3/solutions.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. Page-source write scope was limited to the second `#solutionCards` Supplier Network `short` values in EN/ZH/HI and scoped `#solutionCards` card CSS for the Supplier Network short layout plus active/hover border-shadow stabilization. No compare table, topology SVG/route timing, workflow, CTA, header/footer, other page, QA output, delivery folder, zip/package, staging, commit, or push was intentionally changed.

**Audit items addressed:** `[CONFIRMED]` newest browser comments on `solutions.html`: 1) second card `Supplier network layer` must become `External supplier coordination layer` and must not wrap; 2) first `Owned Panel Operations` solution card should not show an outer border effect and should stay visually consistent with the neighboring cards. Owner package is Package B Solutions/Resources; acceptance categories are Language/i18n, Visual acceptance, and Scope. `ISSUE_LEDGER.md` was checked; no dedicated recurring row changed because these are one-off card-surface corrections guarded by `I18N-001`, `TERMINOLOGY-001`, `VISUAL-QA-001`, and `HANDOFF-006`.

**Red evidence before repair:** current browser evidence selected the second-card short label `Supplier network layer` and the first solution card outer-frame surface. Source showed EN `short: "Supplier network layer"`, ZH `short: "供应商网络运营版"`, and HI `short: "आपूर्ति नेटवर्क स्तर"`. Source also showed `startTopologyRouteCycle()` changing `currentFit` every `3000ms`, and `.plan-card.active` changing solution-card `border-color` and `box-shadow`, with `.plan-card:hover` doing the same on hover. Current diff/handoff inspection found unrelated dirty handoff/resources entries and this same-round badge change, but no active or recent overlapping ownership for the Supplier Network short value or scoped `#solutionCards` active/hover border-shadow rules.

**Implementation summary:** changed the second solution-card short label to EN `External supplier coordination layer`, ZH `外部供应商协同层`, and HI `बाहरी आपूर्तिकर्ता समन्वय स्तर`. Added scoped `#solutionCards .plan-card.network` layout rules so the long short label spans the full card width below the title/badge row and stays `nowrap`. Added scoped `#solutionCards .plan-card:hover` / `.active` overrides so auto-active route changes and hover no longer change solution-card outer border color or shadow. The JS active state and topology route cycle were preserved; only the card border/shadow visual effect was neutralized.

**Verification run:** `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. `git diff --check -- sampora-website-public-v3/solutions.html` -> PASS with LF/CRLF warning only. Source checks found the new EN/ZH/HI short values and scoped `#solutionCards` border-shadow guards. Chrome/Playwright file-URL checks using repo-local Playwright and system Chrome covered Solutions EN/ZH/HI at desktop `1440x900` and mobile `390x900`: the second-card short rendered as the expected EN/ZH/HI text, `white-space=nowrap`, `lineCount=1`, text inside short true, short inside card true, no badge overlap, and document/body overflow `0`. Text widths were desktop/mobile EN `280.81px`, ZH `104px`, and HI `167.38px`, fitting within the card. Browser time sampling across route-cycle states `network -> enterprise -> panel -> network` showed the `active` class still moved, but each card's computed `borderColor` and `boxShadow` stayed constant. Hover sampling for panel/network/enterprise also showed no borderColor or boxShadow changes.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; real visual perception / 真实观感 PASS. A normal user should now see the requested `External supplier coordination layer` on one line, and the solution cards no longer show an outer border/shadow animation when the automatic selection state changes or when the browser/comment tool hovers a card.

**Remaining failures / next agent:** none for this scoped short-label and card-border cleanup. Existing unrelated dirty workspace changes were preserved and not reviewed. Packaging, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, and push remain deferred unless explicitly requested.

### 2026-05-21 CST - Package B Solutions card-header copy and alignment repair

**Files changed:** `sampora-website-public-v3/solutions.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. Page-source write scope was limited to the second `#solutionCards` Supplier Network `cn` values in EN/ZH/HI, `renderSolutions()` card-header markup, and scoped `#solutionCards` header layout selectors for `.plan-top`, `.plan-kicker`, `.badge`, `h3`, and `.short`. No card border color, box shadow, active/hover selected effect, topology, CTA, footer, other page, QA output, delivery folder, zip/package, staging, commit, or push was intentionally changed.

**Audit items addressed:** `[CONFIRMED]` newest Package B assignment: second-card top kicker must render EN `EXTERNAL SAMPLE SUPPLY` instead of `EXTERNAL SAMPLE SUPPLY OPERATIONS`; the second-card badge must return to the top-right header area; the three card version names must align on one horizontal row; top badges should remain one-line and aligned without document overflow. Owner package is Package B Solutions/Resources; acceptance categories are Language/i18n, Visual acceptance, and Scope. `ISSUE_LEDGER.md` was checked; no recurring row update was needed because `I18N-001`, `TERMINOLOGY-001`, `VISUAL-QA-001`, and `HANDOFF-006` remain guards.

**Red evidence before repair:** source showed EN `cn: "External sample supply operations"`, ZH `外部样本供应协同`, and HI `बाहरी सैंपल आपूर्ति संचालन`. Browser geometry at `1440x900` showed the network badge below the title/short: EN badge top `1473.86` versus neighboring badge tops `1356.42`, ZH `1436.22` versus `1314.78`, and HI `1434.13` versus `1314.69`; network `h3` tops were also offset by about `2px` in ZH/HI. Source also showed the network-only `display: contents` / `.short grid-column: 1 / -1` workaround.

**Implementation summary:** changed the second solution-card top kicker to EN `External sample supply`, ZH `外部样本供应`, and HI `बाहरी सैंपल आपूर्ति`. Reworked `renderSolutions()` so card-header children are direct grid items: kicker, badge, title, and short. Replaced the network-only header workaround with a consistent `#solutionCards .plan-top` grid: row 1 kicker plus top-right badge, row 2 title spanning full width, row 3 short spanning full width; mobile stacks kicker, badge, title, and short. Tightened only the `#solutionCards .badge` letter spacing/padding enough to keep the long supplier badge one-line with the restored top-row layout.

**Verification run:** `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. `git diff --check -- sampora-website-public-v3/solutions.html` -> PASS with LF/CRLF warning only. Chrome/Playwright file-URL checks using repo-local Playwright and system Chrome covered Solutions EN/ZH/HI at desktop `1440x900` and mobile `390x900`: EN network kicker rendered exactly `External sample supply`; changed EN/ZH/HI surfaces had no repeated-question-mark or replacement-character markers; desktop badge top spread was `0px` in EN/ZH/HI; desktop `h3` top spread was `0px` in EN/ZH/HI; all badges had `lineCount=1`; no kicker/badge/title/short overlaps were detected; document/body horizontal overflow was `0`. Mobile EN/ZH/HI had document/body overflow `0`, one-line badges, readable stacked header content, and no header overlap.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; real visual perception / 真实观感 PASS. A normal desktop user now sees all three badges in the same top header row, the Supplier Network badge back in the top-right area, and the three card version names aligned; mobile remains readable and stacked without overflow.

**Remaining failures / next agent:** none for this scoped card-header copy and alignment repair. Existing unrelated dirty workspace changes were preserved and not reviewed. Packaging, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, and push remain deferred unless explicitly requested.

### 2026-05-21 CST - Cross-page contact footer reuse and S mark parity

**Files changed:** `sampora-website-public-v3/contact.html`, `index.html`, `solutions.html`, `resources.html`, `resource-manuals.html`, `plans.html`, `about_sampora_issues_fixed.html`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, and `ISSUE_LEDGER.md`. Page-source write scope was limited to footer markup, footer-specific CSS, footer i18n/data mapping needed for EN/ZH/HI rendering, and the contact-style footer S mark. `products.html` and `pricing.html` redirect pages, body sections, header/nav logic, topology, contact form behavior, delivery folders, zips, QA screenshots/JSON, staging, commit, push, and packaging were intentionally untouched.

**Audit item addressed:** `[CONFIRMED]` newest browser comment on `contact.html`: reuse the selected contact footer across the other complete pages, but first make the footer top-left `S` mark match the navigation `S` mark for sitewide S-icon consistency. Owner is shared footer across Package A/B/C/D/About; acceptance categories are Scope, Legal/footer, Language/i18n, Header brand visual parity, and Visual acceptance. Matching ledger guards are `I18N-001`, `TERMINOLOGY-001`, `HEADER-BRAND-001`, `VISUAL-QA-001`, and `HANDOFF-006`.

**Red evidence before repair:** read-only verification confirmed `contact.html` footer existed around the selected region and `.sampora-footer-mark` used a different visual style from `.mast .brand .mark` (`34x34`, radius `10px`, different gradient/border/font versus nav `30x30`, radius `8px`). Other complete pages had non-identical footer structures/copy: `index.html`, `solutions.html`, `resources.html`, `resource-manuals.html`, `plans.html`, and `about_sampora_issues_fixed.html`; `about_sampora_issues_fixed.html` still used an older `<footer class="footer">` structure. `products.html` and `pricing.html` were verified as redirects and excluded.

**Implementation summary:** aligned `.sampora-footer-mark` to the nav S mark style (`30x30`, radius `8px`, same dark blue gradient, cyan border, `13px` mono font, and no box shadow). Reused the corrected contact footer structure across the complete pages with Product / Resources / Get started columns, `sales@sampora.com`, `400-660-0224`, the Hengyang Guangcheng address, contact-intent CTA links, and footer i18n mappings for EN/ZH/HI. The implementation worker was closed after a long silence, so the controller treated the file changes as implementation output and sent the result to an independent read-only final review before closeout.

**Verification run:** read-only final review reported `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS, `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS, and `git diff --check` on scoped source/handoff/ledger files -> PASS with LF/CRLF warnings only. Footer source consistency passed across all seven complete pages: each uses `sampora-site-footer`, `about_sampora_issues_fixed.html` no longer uses `<footer class="footer">`, footer regions contain `sales@sampora.com`, old `jiayu@surveysaas.com` is absent from target footer regions, and the contact-style footer columns/links are present. Runtime browser review covered all 7 pages x EN/ZH/HI x desktop `1440x900` and mobile `390x900` = 42 samples: no horizontal overflow, footer overlap, empty footer strings, question placeholders, ICP text, stale email, or broken legal text; ZH legal renders Chinese company only, while EN/HI render the English company fallback. Browser checks measured footer/nav S marks as `30x30`, radius `8px`, with matching gradient, border color, font size/weight, and box shadow.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; real visual perception / 真实观感 PASS. A normal user now sees the same corrected contact-style footer reused across the target complete pages, and the footer `S` icon visually matches the navigation `S` mark.

**Remaining failures / next agent:** none for the scoped footer reuse and S mark parity task. The read-only review noted unrelated dirty baseline changes outside this scope, including non-footer `solutions.html`/`resources.html` work, tracked QA evidence changes, and deleted old delivery/public package files; those were not reviewed as part of this footer task and were not reverted. Packaging, Lighthouse/axe full delivery, delivery sync, zip rebuild, staging, commit, and push remain deferred unless explicitly requested.

### 2026-05-21 CST - Package B Resources topology sizing parity with Solutions

**Files changed:** `sampora-website-public-v3/resources.html`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, and `ISSUE_LEDGER.md`. Page-source write scope was limited to Resources hero/topology sizing CSS: `.hero-grid` right-column basis, `.resource-console:not(.modal-open) .topo-stage`, `.resource-console:not(.modal-open) .console-window`, and the mobile non-modal `.topo-stage` override. No visible copy/i18n, topology JavaScript, topology SVG semantics, ticker/right-console content, header/nav/footer, CTA links, QA output, delivery folders, zips, staging, commit, push, or packaging file was intentionally changed.

**Audit item addressed:** `[CONFIRMED]` Package B Resources visual/topology sizing regression. Newest user direction superseded Preview A: Resources topology size must reuse/match `solutions.html`, not the old Preview A `820 / 300` aspect metric.

**Red evidence before repair:** source showed Resources `.hero-grid { grid-template-columns: minmax(0, 1fr) minmax(430px, .86fr); }`, desktop `.resource-console:not(.modal-open) .topo-stage { height: auto; aspect-ratio: 820 / 300; }`, and mobile `.topo-stage { height: 240px; }`. Fresh Chrome/Playwright red geometry at `1811x845` showed Solutions stage `542.42x224px` and Resources stage `557.84x204.08px`; Resources right/window width was `603.84px` versus Solutions `588.42px`; Resources visible `#resourceConsoleWindow.bottom` was `50.67px` above invisible `#resourceConsole.bottom`, so the prior card alignment metric was using an invisible wrapper bottom rather than the visible window border.

**Implementation summary:** changed Resources desktop hero grid to the Solutions right-column basis `minmax(0, 1fr) minmax(360px, .82fr)`. Replaced the non-modal Resources topology aspect-ratio behavior with `height: 224px; aspect-ratio: auto;`. Added non-modal `.console-window { min-height: 662px; }` for the visible window sizing floor and changed the mobile non-modal stage override to `height: 220px`. The short write lock was held only during this scoped `resources.html` CSS write and released immediately after the patch; all verification was read-only.

**Verification run:** `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. `git diff --check -- sampora-website-public-v3/resources.html` -> PASS with LF/CRLF warning only. Inline `resources.html` scripts parsed `3/3`. Source checks confirmed the Solutions-matched CSS values.

**Runtime geometry PASS:** Chrome/Playwright using system Chrome compared `solutions.html` and `resources.html` across EN/ZH/HI at `1811x845`, `1440x900`, and `390x900`. Resources `#topoStage` height matched Solutions exactly in all samples: `224px` on desktop and `220px` on mobile. Resources visible window width matched Solutions exactly in all samples: `588.42px` on desktop and `358px` on mobile. Desktop Resources card bottom aligned to visible `#resourceConsoleWindow.bottom` within `0.05px` in EN/ZH and exactly in HI; `#resourceConsole.bottom - #resourceConsoleWindow.bottom` was `0`, so no invisible wrapper-bottom alignment remains. Horizontal overflow was `0` for document and body in all samples. Resources visible window height is intentionally lower than Solutions (`662px` versus roughly `744-753px`) because the content stacks differ; the required exact parity targets were stage size and right-column/window width, and the left-card alignment target is the visible Resources window border.

**Topology motion PASS:** Resources EN/ZH/HI samples at desktop `1440x900` and mobile `390x900` kept `animationName=topoDash`, `animationDuration=2.4s`, changing dash offsets, 3 active flows, 9 packets, and packet movement. Desktop packet movement was `38.42px / 43.84px / 37.58px`; mobile packet movement was `21.17px / 31.57px / 21.20px`.

**Visual acceptance PASS / 真实观感:** a normal user now sees Resources topology at the same desktop and mobile height as Solutions, with the right console width matching Solutions and the left Resources cards distributed down to the visible window border. The previous invisible-wrapper bottom and superseded Preview A aspect-ratio metric are no longer accepted evidence; future reviewers must align against `#resourceConsoleWindow.bottom` and compare stage dimensions directly to Solutions.

**Remaining failures / next agent:** none for this scoped Resources topology sizing parity repair. Existing unrelated dirty workspace changes, screenshot/QA artifacts, delivery folder deletions, and unrelated page files were preserved and not reviewed. Packaging, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, and push remain deferred unless explicitly requested.

### 2026-05-21 CST - Package B Resources full-screen visible console bottom alignment follow-up

**Files changed:** `sampora-website-public-v3/resources.html`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, and `ISSUE_LEDGER.md`. Page-source write scope was limited to the Resources desktop hero/topology CSS in the existing `@media(min-width:1101px)` block and the initial topology state value. No visible copy/i18n strings, topology SVG structure, ticker content, header/nav/footer, CTA links, other pages, QA output artifacts, delivery folders, zips, staging, commit, push, or packaging file was intentionally changed.

**Audit item addressed:** `[CONFIRMED]` Package B Resources first-viewport full-screen visual regression. The user rejected the prior visual PASS because the full-screen browser still showed the left resource cards lower than the visible right console border. The newest browser evidence also showed that simply stretching the right border created an unacceptable empty lower topology area, and the user flagged the yellow Supplier packet visible on initial entry.

**Red evidence before final correction:** Chrome at `1920x1080` measured `.resource-entry-grid.bottom = 840.11px`, `#resourceConsoleWindow.bottom = 828px`, and `#resourceConsole.bottom = 840.11px`. This proved the invisible wrapper aligned with the left column while the visible child window did not. A brief stretch attempt made the visible border align but stretched the child to wrapper height, producing visible empty space in the lower console/topology area; that attempt was reverted before the final fix. Initial Resources state was `mode: "supplier"`, so page entry opened on yellow Supplier packets.

**Implementation summary:** kept the visible Resources console at its normal content density (`min-height:662px`, topology stage `224px`) and bottom-aligned it inside the already stretched wrapper using `.resource-console:not(.modal-open) { display:flex; flex-direction:column; justify-content:flex-end; }`. This aligns the visible child border to the left card border without stretching the right console internals. Changed the initial topology state from `supplier` to `setup`, so entry opens on the Project path rather than a yellow Supplier packet state. After the user reported the same edge dot had become blue, the root cause was narrowed to SVG `animateMotion` circles being visible at their default SVG-edge coordinate before or during path placement. The root fix keeps packet dots visible but replaces `animateMotion` with explicit packet `data-path` / duration / phase metadata plus `startPacketMotion()` / `updatePacketPositions()` using each path's `getPointAtLength()` result. Initial and animated packet coordinates now come from the actual path geometry instead of from the default circle origin.

**Verification run:** `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. `git diff --check -- sampora-website-public-v3/resources.html` -> PASS with LF/CRLF warning only. Chrome file-URL checks: at `1920x1080`, `leftCards.bottom - #resourceConsoleWindow.bottom = 0px`, `#resourceConsole.bottom - #resourceConsoleWindow.bottom = 0px`, stage height `224px`, overflow `0`, initial scope `PROJECT`, packet layer display `inline`, active flow animation `topoDash`; at `1580x768`, left/window delta `-0.05px`, wrapper/window delta `0px`, stage height `224px`, overflow `0`, initial scope `PROJECT`. Packet bug verification sampled entry immediately, `150ms`, and `1050ms`: visible packet count was `0` at immediate entry while opacity was still fading in, then `3` at `150ms` and `1050ms`, and all visible packets had `nearSvgLeft=false` / `nearStageLeft=false`. Mode checks for setup/supplier/finance at `1920x1080` kept active packet dots visible with `nearLeftCount=0`, active flow count `3`, and `flowAnim=topoDash`.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS for the full-screen/tall desktop regression and smaller desktop guard; real visual perception / 真实观感 PASS for the specific user complaint: the left card bottom now aligns to the visible right console bottom without hard-stretching the right console's lower topology area, and packet dots now originate from their actual paths rather than appearing as distracting yellow/blue edge artifacts.

**Remaining failures / next agent:** none for this scoped follow-up. Future reviewers must measure the visible `#resourceConsoleWindow.bottom` at full-screen/tall desktop sizes, not only small desktop windows, and must reject fixes that align by stretching the visible console height and creating dead lower-console space.

- **Pitfall / symptom:** a Resources visual check can pass in smaller desktop windows while full-screen/tall desktop still shows the left cards below the visible right console border; a naive border-stretch fix aligns the bottom but creates empty lower topology space. Separately, SVG packet circles can render as isolated yellow/blue dots at the topology edge on entry.
- **Root cause:** the grid item wrapper `#resourceConsole` stretches to the left-column height, while the visible child `#resourceConsoleWindow` has its own fixed/min content height and can be top-aligned inside that taller wrapper. Packet artifacts come from SVG `animateMotion` circle elements being visible at their default SVG-edge coordinate before the intended path placement is active.
- **Prevention rule:** for Resources hero alignment, measure and align against the visible `#resourceConsoleWindow.bottom` at full-screen/tall desktop sizes. Do not use the wrapper bottom as proof, and do not stretch the child window height unless the internal content distribution is also visually accepted. Treat standalone packet dots as visual failures; if packet dots are used, give them deterministic path-derived coordinates before making them visible.
- **Verification:** browser geometry comparing `.resource-entry-grid.bottom`, `#resourceConsoleWindow.bottom`, and `#resourceConsole.bottom` at full-screen/tall desktop plus a screenshot/real visual check that the right console lower area is not dead space and that no isolated packet dots are visible on entry.
- **Owner package:** Package B Resources implementation plus controller/read-only visual acceptance.

### 2026-05-21 CST - Package B Resources hero console lower ticker compactness

**Files changed:** `sampora-website-public-v3/resources.html`, `SAMPORA_LATEST_REPAIR_HANDOFF.md`, and `ISSUE_LEDGER.md`. Page-source write scope was limited to Resources non-modal right-console lower CSS: `.console-main`, `.console-ticker`, `.console-ticker:before`, `.console-ticker:after`, the reduced-motion `#consoleTicker:after` fallback, and `.resource-console:not(.modal-open) .console-window` minimum height. No visible copy/i18n, topology SVG structure, packet path script, mode cycling behavior, header/nav/footer/CTA, other pages, packaging, zips, staging, commit, or push was intentionally changed.

**Audit item addressed:** `[CONFIRMED]` Package B Resources visual polish follow-up. The user reported that after visible bottom alignment was solved, the lower console/ticker area felt weird: ticker text sat in a loose lower area and the sweep glow read like a random light block. Owner package is Package B Resources; acceptance categories are Visual acceptance, Topology motion guard, and Scope.

**Red evidence before repair:** fresh Chrome geometry at `1920x1080` and `1580x768` showed the visible alignment was already solved (`leftCards.bottom - #resourceConsoleWindow.bottom = 0px`, wrapper/window delta `0px`, overflow `0`, stage `224px`), but the ticker was a `55px` block with `display:block`, no flex centering, sweep width `68px`, opacity `1`, and about `21.7px` of empty console background below the ticker because the non-modal window floor exceeded its children.

**Implementation summary:** tightened the ticker into a compact footer strip: `display:flex`, `align-items:center`, `min-height:44px`, `padding:8px 18px`, dot as a fixed flex item, and a softer/narrower sweep (`34px`, opacity `.36`, lighter gradient). Reduced `.console-main` bottom padding from `16px` to `12px` and reduced the non-modal visible window minimum height from `662px` to `626px` so the footer strip sits at the bottom instead of leaving a loose blank tail. Short write lock was held only during this scoped CSS patch and released before verification.

**Verification run:** `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. `git diff --check -- sampora-website-public-v3/resources.html` -> PASS with LF/CRLF warning only. Chrome/Playwright file-URL checks at Resources EN `1920x1080` and `1580x768`: `leftCards.bottom - #resourceConsoleWindow.bottom = 0px`, `#resourceConsole.bottom - #resourceConsoleWindow.bottom = 0px`, horizontal overflow `0`, stage height `224px`, ticker height `44px`, stage-to-ticker gap `12px`, ticker-to-window-bottom `1px`, ticker `display:flex`, `align-items:center`, and softened sweep width `34px` / opacity `.36`. Mobile ZH/HI spot checks at `390x900` kept stage height `220px`, overflow `0`, ticker height `44px`, and wrapper/window delta `0px`.

**Topology motion and packet guard:** active flow remained `topoDash`; dash offsets changed over time at desktop and mobile. Active path-positioned packets remained visible and moving: desktop packet movement was about `16.98-27.59px` at `1920x1080` and `15.84-25.70px` at `1580x768`; mobile ZH/HI samples also moved. `nearLeftCount=0` in all samples, so the isolated edge-dot bug was not reintroduced.

**Visual acceptance PASS / 真实观感:** a normal user now sees the lower console area as an intentional compact footer strip rather than a loose padded lower band. The ticker text is vertically centered with its status dot, the decorative sweep is subdued instead of reading as a random block of light, the topology stage still has active content above it, and the already-fixed bottom alignment is preserved.

**Remaining failures / next agent:** none for this scoped lower-console ticker compactness repair. Existing unrelated dirty workspace changes were preserved and not reviewed. Packaging, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, and push remain deferred unless explicitly requested.

### 2026-05-21 CST - Package B Solutions Supplier Network hover frame color correction

**Files changed:** `sampora-website-public-v3/solutions.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. Page-source write scope was limited to `#solutionCards` card hover/frame CSS: added a more specific Supplier Network hover override after the general cyan hover rules. Footer, footer i18n, email, page copy, topology, CTA, other pages, QA artifacts, delivery folders, zips, staging, commit, and push were intentionally untouched.

**Audit item addressed:** `[CONFIRMED]` newest Package B visual issue: Supplier Network card baseline frame is amber/yellow, but the newer hover selected effect was forcing the outer frame cyan/blue. Owner package is Package B Solutions/Resources; acceptance categories are Visual acceptance and Scope. Ledger guards checked: `VISUAL-QA-001` and `HANDOFF-006` remain strategy guards; no recurring issue row status changed.

**Red evidence before repair:** current source had `#solutionCards .plan-card:hover` setting cyan `border-color: rgba(56, 210, 255, .52)` and cyan box-shadow after the amber `.plan-card.network` baseline, so the scoped hover rule could override the intended Supplier Network frame color.

**Implementation summary:** added `#solutionCards .plan-card.network:hover` and `#solutionCards .plan-card.network.active:hover` after the general hover/active block, using amber/yellow border and amber glow/shadow values consistent with the baseline Supplier Network card. Panel and Enterprise hover rules were left cyan/blue. Existing behavior that `#solutionCards` cards do not auto-apply `.active` was preserved.

**Verification run:** Chrome/Playwright file-URL checks using repo-local Playwright and system Chrome covered Solutions EN desktop `1440x900` and mobile `390x900`. Supplier Network hover computed `borderColor=rgba(246, 169, 59, 0.62)` and amber-only box-shadow, while Panel/Enterprise hover kept cyan `rgba(56, 210, 255, 0.52)` border/shadow. Both viewports had document/body horizontal overflow `0`, `activeCards=0`, one-line badges/headers, and no title/short overlap. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. `git diff --check -- sampora-website-public-v3/solutions.html` -> PASS with LF/CRLF warning only.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; real visual perception / 真实观感 PASS. A normal user should now see the Supplier Network hover frame remain amber/yellow, while Panel and Enterprise still show the intended cyan hover selected effect.

**Remaining failures / next agent:** none for this scoped hover color correction. Existing unrelated dirty workspace changes were preserved and not reviewed. Footer was intentionally untouched/preserved. Packaging, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, and push remain deferred unless explicitly requested.

### 2026-05-21 CST - Package C Plans card hover selected state

**Files changed:** `sampora-website-public-v3/plans.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. Page-source write scope was limited to the new scoped CSS block `<style id="plans-card-hover-selected-state">` for `#plansGrid .plan-card` hover/focus/selected/active visual states. No visible copy/i18n, CTA hrefs, `renderPlans()` data/JS, header/footer/contact form, `.plan-fit-row`, other pages, QA artifacts, delivery folders, zips, staging, commit, push, or packaging file was intentionally changed.

**Audit item addressed:** `[CONFIRMED]` newest Package C visual request: add a mouse-hover selected effect to the three main `plans.html` plan cards. Owner package is Package C Plans/Contact; acceptance categories are Visual acceptance and Scope. Ledger guards checked: `VISUAL-QA-001`, `HANDOFF-004`, `HANDOFF-006`, and `CTA-001`; no recurring issue row status changed.

**Red evidence before repair:** read-only verification found the three cards are rendered as `#plansGrid .plan-card:nth-child(1)`, `.plan-card.recommended`, and `.plan-card.enterprise-plan`. Current CSS had static baseline/recommended/enterprise styling, but no scoped `#plansGrid .plan-card:hover`, `:focus-within`, `.active`, or `.selected` selected-state feedback for the main cards. The only existing hover pattern was for the separate `.plan-fit-row` selector preview, not the three main cards.

**Implementation summary:** added a scoped hover/focus/selected state for all three main plan cards with a restrained selected effect: slight lift, brighter border, stronger glow/shadow, and deeper background. Recommended keeps its lime-green selected identity, Enterprise keeps its cyan identity, and the normal Panel card gets a cyan selected state. A reduced mobile hover transform guard keeps mobile layout stable while preserving the runtime selected/focus styling. The implementation worker released its short write lock immediately after editing.

**Verification run:** implementation reported `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS, `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS, and `git diff --check -- sampora-website-public-v3/plans.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> PASS with LF/CRLF warning only. Read-only review confirmed the new style block is scoped to `#plansGrid .plan-card`, does not include `.plan-fit-row`, and contains no copy/href/JS edits; existing historical dirty diff in `plans.html` for footer/horizontal-line/footer-i18n was treated as unrelated baseline and not part of this hover task. Browser/CDP runtime review used a temporary `.selected` proxy equivalent to the new selected-state selectors and removed it after sampling. Desktop `1440x900` EN/ZH/HI had no horizontal overflow; all three cards kept width/height stable and moved up by `4px`. ZH/HI selected states measured Panel border `rgba(56,210,255,.54)`, Supplier Network border `rgba(181,240,86,.62)`, and Enterprise border `rgba(123,227,255,.64)`. Mobile `390x900` EN/ZH/HI had document/body width `375px` versus viewport `390px`, all card widths `343px`, unchanged width/height, and only the selected top shift.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; real visual perception / 真实观感 PASS. A normal user should now clearly see the three main plan cards enter a selected state on hover/focus, with visible lift, border emphasis, deeper background, and glow, while the recommended card's lime highlight remains distinct and Enterprise remains visually coherent.

**Remaining failures / next agent:** none for this scoped `plans.html` card hover selected-state task. Packaging, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, and push remain deferred unless explicitly requested.

### 2026-05-21 CST - Package E cross-machine Git push policy

**Files changed:** `SAMPORA_LATEST_REPAIR_HANDOFF.md` and `ISSUE_LEDGER.md`. No page source, QA output artifacts, screenshots, JSON evidence, delivery folders, zips, temp browser folders, old public-package deletions, staging, packaging, or site behavior files were intentionally changed.

**Strategy item addressed:** `[STRATEGY]` `HANDOFF-007` created after the user clarified that future GitHub pushes must carry the Class 1/Class 2 files needed to pull the branch on another computer and keep working. Screenshot evidence should be handled more tightly than QA scripts/JSON.

**Policy summary:** future cross-machine continuation pushes must include changed Class 1 must-push files: page/source files, active handoff/acceptance/ledger/glossary/policy docs, repair scripts, and small code/config needed for the site or QA commands. They should also include changed Class 2 files: QA scripts and compact `.json` evidence such as page-layer, language, Lighthouse, and axe outputs. Screenshot/image/video evidence is Class 3: useful for visual proof and final handoff, but not part of ordinary small repair pushes unless explicitly needed or requested.

**Verification run:** `git diff --check -- SAMPORA_LATEST_REPAIR_HANDOFF.md ISSUE_LEDGER.md` must pass before committing. Next controller should inspect staged files before push and exclude unrelated old package deletions, zip deletions, temp browser folders, untracked historical/reference folders, and screenshot churn unless the user explicitly assigns those files.

**Remaining failures / next agent:** none for this docs/process policy update. Existing unrelated dirty workspace deletions and untracked folders remain intentionally untouched.

### 2026-05-21 CST - Package C Plans P0/P1 hero, matrix, footer, language guard, and status carousel

**Files changed:** `sampora-website-public-v3/plans.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. Page-source write scope was limited to the Plans hero copy/console, hero alignment CSS, Start trial visual parity, capability matrix CSS, footer CSS consolidation, language-switching guard, and top status carousel. No other page, header/nav structure, URL, form intent, plan data model, QA output artifact, delivery folder, zip/package, staging, commit, push, or packaging file was intentionally changed.

**Audit items addressed:** `[CONFIRMED]` `plans_fix_v8.md` P0-1/P0-2/P1-1/P1-4 plus the later `[CONFIRMED]` status-carousel task from `plans_status_carousel_only_codex.md`. P1-2 matrix and P1-3 footer started as rendered-pass guards, then were preserved while scoped CSS was aligned to the latest brief. P1-5 language switching was treated as a `[REGRESSION]` guard and rechecked after the carousel integration. Owner package is Package C Plans/Contact; acceptance categories are Scope, Language/i18n, CTA policy, Plans matrix layout, Legal/footer, Visual acceptance, and Navigation/status runtime guard. `CTA-001`, `I18N-001`, `TERMINOLOGY-001`, `VISUAL-QA-001`, and `HANDOFF-006` remain guard/strategy rows; no ledger status changed.

**Implementation summary:** the controller kept implementation out of the main thread and dispatched subagents. First-pass read-only verification confirmed hero highlight loss, right-card console mismatch, hero alignment deltas, CTA visual mismatch, and legacy/broad footer CSS debt. The first broad implementation agent was closed after long silence and left partial `plans.html` edits; those edits were preserved as the current baseline, not reverted. A scoped worker added the plan-fit status carousel and P1-5 language guard integration through `render(lang)`, preserving `renderChrome`, `renderPlans`, and `renderMatrix`, and keeping `innerHTML` whitelisted to `heroTitle`. A later implementation agent disconnected remotely after writing additional matrix/footer/hero CSS improvements, so the controller required an independent read-only final review before closeout.

**Verification run:** read-only final review reported `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS, `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS, `git diff --check -- sampora-website-public-v3/plans.html` -> PASS with LF/CRLF warning only, and inline script syntax PASS for 3/3 scripts. Browser/CDP review covered Plans EN/ZH/HI at desktop `1440x900`, desktop `1366x900`, mobile `390x900`, plus `900x900` for matrix/footer tablet behavior.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; real visual perception / 真实观感 PASS. Final review found the hero title keeps one safe `.grad` highlight in EN/ZH/HI; only `heroTitle` uses `innerHTML`; the right selector card has the console top bar and three lights; desktop hero top/bottom alignment deltas are `0px`; mobile stacks without horizontal overflow; header/hero/final Start trial buttons share gradient/hover behavior; the matrix remains centered with internal mobile/tablet scroll; the footer remains one large card with no mini cards, numbering, left rail, bullets, overflow, or legal-name regression; language switching updates hero, selector, plan cards, matrix, final CTA, footer, active state, `html lang`, `data-lang`, and `localStorage`; and the status carousel renders immediately, rotates through Panel / Supplier Network / Enterprise, and remaps language immediately. Mobile status remains visually hidden under the pre-existing small-screen rule, which was intentionally preserved because the carousel brief forbids changing mobile status/header behavior.

**Remaining failures / next agent:** none for this scoped Plans P0/P1 + carousel round. Existing unrelated untracked `archives/` and local reference folders were preserved and not reviewed. Packaging, Lighthouse/axe full delivery, delivery sync, zip rebuild, staging, commit, and push remain deferred unless explicitly requested.

### 2026-05-22 JST - Package E homepage front-of-site terminology strategy

**Files changed:** `SAMPORA_TERMINOLOGY_GLOSSARY.md`, `ISSUE_LEDGER.md`, and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. No `sampora-website-public-v3/**` page source, delivery folder, zip/package, QA evidence output, topology/workflow animation logic, footer, other page, staging, commit, push, or packaging file was intentionally changed by this Package E docs/strategy worker.

**Strategy item addressed:** `[STRATEGY]` P0-2 homepage/front-of-site terminology governance. The approved strategy allows neutral supply-side front-of-site display labels such as `RESP_03 / respondent ops`, `PARTNER_04 / delivery partner`, and `Supplier pool active` so later agents do not misclassify those labels as terminology regressions. This is not a backend taxonomy deletion: `Survey Station` and `Subcontractor` may still exist as supply sources, backend business roles, or topology source concepts when a future scoped task and fresh evidence require them.

**Verification run:** `git diff --check -- SAMPORA_TERMINOLOGY_GLOSSARY.md ISSUE_LEDGER.md SAMPORA_LATEST_REPAIR_HANDOFF.md` -> PASS with LF/CRLF warnings only. Text search confirmed `RESP_03`, `PARTNER_04`, `Supplier pool active`, `Survey Station`, `Subcontractor`, and `TERMINOLOGY-001` are present in the expected governance docs.

**Remaining failures / next agent:** none for this docs-only strategy update. Next homepage/source worker may edit `sampora-website-public-v3/index.html` only under its own assigned ownership and fresh verification; it must not treat the allowed neutral labels above as a regression by themselves. Existing unrelated dirty page-source changes were intentionally preserved and not reviewed.

### 2026-05-22 JST - Package A homepage P0/P1 continuation closeout

**Files changed:** `sampora-website-public-v3/index.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. Page-source write scope was limited to homepage non-footer CSS comment cleanup and non-footer inline homepage translation/project data coverage. Existing footer dirty diff, footer markup/CSS behavior, About route/href, topology/workflow animation logic, other pages, QA artifacts, delivery folders, zips, staging, commit, push, and packaging were intentionally untouched.

**Audit items addressed:** Package A homepage P0/P1 continuation from `E:\Downloads\SAMPORA_INDEX_PAGE_P0_P1_CODEX_INSTRUCTIONS_REVISED.md`. Current partial diff already contained the title-only section-head layout, neutral topology labels `RESP_03` and `PARTNER_04`, India-priority hero console projects, Product Surfaces/Pricing rewrites, and old APAC/EU/US example removal. This worker filled the remaining runtime dictionary gaps: localized `statusNetworks`, strengthened final HI overrides for Pricing/Product Surfaces/workflow/outcomes, preserved allowed core topology/system labels, and cleaned historical/mojibake CSS comments without deleting rules.

**Verification run:** inline Node i18n check over `[data-i18n]`, `[data-i18n-html]`, `[data-i18n-aria-label]`, and `[data-i18n-title]` -> PASS with EN/ZH/HI missing `0`; required status strings were present in source (`Supplier pool active`, `供应商池已启用`, `सप्लायर पूल सक्रिय`). Grep/static check -> PASS: no `APAC`, `EU /`, `US /`, `Survey Station`, `survey station`, `Subcontractor`, `STN_03`, or `SUB_04` in `index.html`; expected `RESP_03`, `respondent ops`, `PARTNER_04`, `delivery partner`, supplier-pool strings, and IN project regions were present. Title-only section check -> PASS for `#outcomes`, `#platform`, `#workflow`, `#product-surfaces`, `#customers`, and `#pricing` with scoped `.section-head.title-only` CSS. Inline script syntax extraction -> PASS for 6 scripts. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS.

**Remaining failures / next agent:** none found for this scoped Package A continuation closeout. Core uppercase topology labels such as `SAMPLE SUPPLY POOL` and neutral labels `respondent ops` / `delivery partner` remain intentionally allowed per the glossary/front-of-site strategy and were not treated as untranslated fallback. Existing unrelated dirty workspace changes remain preserved and unreviewed. Footer, About route rename, other pages, delivery folders, zip/package work, staging, commit, and push remain deferred unless explicitly requested.

### 2026-05-22 JST - Package D Resource Manuals footer source parity follow-up

**Files changed:** `sampora-website-public-v3/resource-manuals.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. Page-source write scope was limited to Resource Manuals footer-source parity with current `plans.html`: removed the extra `data-static-legal` footer attribute, removed duplicate local `#footer .sampora-footer-card` width rules, and removed page-local footer border overrides for `.sampora-footer-legal`, `.sampora-footer-col h4`, and `.sampora-footer-contact-item`. No Resource Manuals app/data behavior, manual content, status carousel, other page, QA artifact, delivery folder, zip/package, staging, commit, or push was intentionally changed.

**Root cause:** the earlier footer reuse left Resource Manuals with a page-local static legal attribute and later cleanup CSS that overrode the shared Plans-style footer contract. The attribute was not referenced by `assets/resource-manuals` or `assets/i18n-sweep.js`; however, `final-audit-static-check.mjs` still requires the contiguous English legal string somewhere in each official page. To keep the footer row matching `plans.html` while satisfying the static audit, Resource Manuals now keeps the same non-rendered `legalStaticGuard` source pattern instead of a footer markup attribute.

**Verification run:** `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\resource-manuals-language-smoke.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\resource-manuals-browser-language-check.mjs"` -> PASS. Targeted source grep confirmed `data-static-legal`, `.sampora-footer-col h4`, page-local `.sampora-footer-legal {`, page-local `.sampora-footer-contact-item {`, and duplicate `#footer .sampora-footer-card` width overrides are absent. Node source comparison reported the scoped footer CSS contract equal to current `plans.html`; the footer markup differs only by line-ending shape on the legal row.

**Remaining failures / next agent:** none for this scoped Package D footer-source parity follow-up. Resource Manuals language behavior remains guarded by the smoke/browser checks above. Existing unrelated dirty workspace changes were preserved and not reviewed. Packaging, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, and push remain deferred unless explicitly requested.

### 2026-05-22 JST - Controller-led complete-page footer reuse and Plans hero-card glow closeout

**Files changed this round:** `sampora-website-public-v3/index.html`, `sampora-website-public-v3/solutions.html`, `sampora-website-public-v3/resources.html`, `sampora-website-public-v3/resource-manuals.html`, `sampora-website-public-v3/contact.html`, `sampora-website-public-v3/about_sampora_issues_fixed.html`, `sampora-website-public-v3/plans.html`, and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. Existing related docs `ISSUE_LEDGER.md` and `SAMPORA_TERMINOLOGY_GLOSSARY.md` were already dirty from earlier strategy work in this workspace; this entry covers only the footer/glow round.

**User scope and orchestration:** reuse the current `plans.html` footer across complete pages, and add the About figure-2-style highlight effect only to the `plans.html` hero right-side big card. The controller stayed orchestration-only; implementation and review were delegated to subagents.

**Verification evidence:** Hooke initial review reported static/language PASS but found `resource-manuals.html` strict footer source mismatch. Mendel fixed Package D footer parity by removing `data-static-legal`, duplicate `#footer .sampora-footer-card` width overrides, extra local `.sampora-footer-legal`, `.sampora-footer-col h4`, and `.sampora-footer-contact-item` overrides, while keeping the non-rendered `legalStaticGuard` pattern aligned with the `plans.html` static-audit guard. Pauli post-fix review PASS confirmed normalized footer markup equal to Plans, key CSS contract counts match, `final-audit-static-check.mjs` PASS, and `resource-manuals-language-smoke.mjs` PASS. Boyle visual review PASS confirmed the Plans glow is visible and scoped to `.plan-selector-card`, footer visual reuse across complete pages PASS, and Plans matrix desktop/mobile/tablet PASS.

**Language guard evidence:** `render(lang)` still calls `renderChrome`, `renderPlans`, and `renderMatrix`; only `heroTitle` uses `innerHTML`; EN/ZH/HI `heroTitleHtml` safe versions exist. Language switching updates hero, selector card, plan cards, matrix, final CTA, footer, active language state, `html lang`, `data-lang`, `lang`, and `localStorage`.

**Remaining failures / next agent:** none for this scoped footer/glow round. Out of scope/deferred: redirect-only `products.html`/`pricing.html`, packaging, zip rebuild, Lighthouse/axe full delivery, staging, commit, push, and delivery sync. Informational only, not a repair task: a prior note mentioned non-footer About page header-actions overflow around 1180px; that remains outside this footer/glow round unless the user assigns it separately.

- **Pitfall / symptom:** Footer runtime/visual PASS can hide source-contract drift in one page due to leftover page-local responsive width/CSS overrides or static legal attributes.
- **Root cause:** Parity checks focused on rendered behavior and not normalized footer markup/CSS contract.
- **Prevention rule:** For shared footer reuse, verify both runtime visual/legal behavior and the normalized source contract against the reference page, including page-local overrides.
- **Verification:** Source comparison of footer markup and CSS counts plus runtime EN/ZH/HI footer checks.
- **Owner package:** Shared footer / Package D or touched page owner plus controller review.

- **Pitfall / symptom:** Local highlight/glow can be implemented but effectively invisible if the pseudo-element animation path keeps it mostly outside the card or opacity/position sampling is not checked.
- **Root cause:** Static CSS existence was treated as effect existence.
- **Prevention rule:** Visual review must time-sample the pseudo-element in browser, confirm it overlaps the intended card with visible opacity, and scope-check that only the target card has the effect.
- **Verification:** Browser computed style/time samples for `.plan-selector-card::after` and scope checks against other `.console-card::after` instances.
- **Owner package:** Package C Plans visual review/controller.

### 2026-05-22 JST - Package A homepage HI fallback and mobile mast gap mini-fix

**Files changed:** `sampora-website-public-v3/index.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. Page-source write scope was limited to non-footer mobile header CSS and the final Hindi homepage translation/project ticker overrides for Pricing, Product Surfaces, workflow, outcomes, and final CTA copy. Existing footer dirty diff, footer markup/CSS behavior, About route/P2 rename, topology/workflow animation logic, other pages, delivery folders, zips, staging, commit, push, and packaging were intentionally untouched.

**Review failures addressed:** `[CONFIRMED]` Language/i18n failure where HI had no missing keys but rendered Pricing/Product Surfaces/workflow/outcomes/final override text still contained obvious English fallback phrases; `[CONFIRMED]` mobile real visual perception failure where `.status` was hidden at `390x900` but `.mast` still started at `y=34`, leaving an empty top band.

**Implementation summary:** added a final scoped `Object.assign(translations.hi, ...)` override so the affected HI surfaces read as Devanagari-first Hindi/SaaS hybrid while retaining accepted product terms such as Sampora, API, QC, CPI, IR/LOI, Panel, Supplier Network, Enterprise, governance, review, settlement, invoices, permissions, and export controls. Updated the HI project ticker to avoid the English `Partner supply ... routes warming` phrase. Added a mobile-only `.mast,.mast.compact,body.nav-condensed .mast{top:0!important}` rule under the existing `max-width:760px` header guard so hidden mobile status no longer reserves the desktop status offset.

**Verification run:** targeted HI browser/manual check at `1440x900` found no banned full fallback phrases in Pricing/Product Surfaces/workflow/outcomes/final text, with Devanagari count `1506`; inline Node i18n coverage over `[data-i18n]`, `[data-i18n-html]`, `[data-i18n-aria-label]`, and `[data-i18n-title]` reported EN/ZH/HI missing `0`; inline script syntax extraction PASS for `5/5` scripts; browser/CDP geometry at mobile `390x900` for EN/ZH/HI reported `.status display=none height=0`, `.mast top=0 height=67`, top element `.mast`, and horizontal overflow `0`; desktop `1440x900` reported `.status display=block height=35`, `.mast top=35`, and overflow `0`; `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS; `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS; `git diff --check -- sampora-website-public-v3/index.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> PASS with LF/CRLF warnings only.

**Remaining failures / next agent:** none found for these two scoped review failures. Existing unrelated dirty workspace changes remain preserved and unreviewed. Footer, About route/P2 rename, other pages, delivery, zip/package work, staging, commit, and push remain deferred unless explicitly requested.

### 2026-05-22 JST - Package A homepage HI visible-language blocker follow-up

**Files changed:** `sampora-website-public-v3/index.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. Page-source write scope was limited to homepage non-footer HI visible-copy/i18n and dynamic project ticker text. Footer markup/CSS/copy, About route/P2 rename, topology/workflow animation logic, other pages, delivery folders, zips, staging, commit, and push were intentionally untouched.

**Review blocker addressed:** `[CONFIRMED]` Package A Language/i18n blocker where final read-only review still saw long English fallback in HI at Pricing, Product Surfaces, workflow, outcomes, final CTA, and the hero project ticker. Known bad visible strings included `Project source`, `Client + country`, `respondent assets, member profiles, incentives, wallets`, `sample operations workflow Sampora`, `project intake, supplier allocation, review`, and `export controls`.

**Implementation summary:** added final HI source overrides for the affected homepage surfaces and updated HI project ticker data before render. The fix keeps accepted short product/SaaS/metric terms such as Sampora, API, QC, CPI, IR/LOI, Panel, Supplier Network, Enterprise, Tenant, LIVE/RUN/Quota, workflow, review, settlement, trial, demo, and Partner Network where they function as compact product labels or dashboard terms, while replacing the reviewer-reported English主体 phrases with Devanagari-first Hindi/SaaS hybrid copy. Minimal same-key fallback wording was also adjusted so the exact known bad source strings no longer remain in `index.html`.

**Verification run:** browser/CDP HI visible-text check at desktop `1440x900` and mobile `390x900` reported `bad=[]` for status, hero, ticker, workflow, outcomes, Product Surfaces, Pricing, and final CTA; desktop status remained visible with `.mast top=35`, mobile `390x900` kept `.status display=none`, `.mast top=0`, and horizontal overflow `0`. Targeted source grep reported PASS absent for `Project source`, `Client + country`, `respondent assets, member profiles, incentives, wallets`, `incentives, wallets`, `sample operations workflow`, `project intake, supplier allocation, review`, and `export controls`. Inline Node i18n coverage over `[data-i18n]`, `[data-i18n-html]`, `[data-i18n-aria-label]`, and `[data-i18n-title]` reported EN/ZH/HI missing `0`. Inline script syntax extraction PASS for `6/6` scripts. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS.

**Remaining failures / next agent:** none found for this scoped HI visible-language blocker. Remaining English terms in the checked HI view are accepted product/system/metric labels or short SaaS hybrids, not the long fallback strings reported by the reviewer. Existing unrelated dirty workspace changes were preserved and not reviewed. Footer, About route/P2 rename, other pages, delivery folders, zip/package work, staging, commit, and push remain deferred unless explicitly requested.

### 2026-05-22 JST - Package A homepage Product Surfaces screen alignment

**Files changed:** `sampora-website-public-v3/index.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. Page-source write scope was limited to Product Surfaces non-footer CSS for `.surface-card`, scoped `#product-surfaces .surface-card h3`, and `.surface-screen`. No Product Surfaces copy, EN/ZH/HI translation data, RESP/PARTNER terminology, Pricing, CTA, footer, Topology/Workflow animation logic, other page, delivery folder, zip/package, staging, commit, or push was intentionally changed.

**Issue addressed:** `[CONFIRMED]` Package A visual layout failure where the third Product Surfaces card (`Respondent Portal`) started its `.surface-screen` about one title-line higher than the neighboring cards because card internals used natural block flow and different title/body heights. The repair gives Product Surfaces cards a stable internal grid track and a scoped title reserve so `small`, `h3`, body text, and `.surface-screen` use consistent vertical start logic across languages and desktop/mobile.

**Verification run:** Browser/CDP geometry after repair covered `1440x900` and `390x900` for EN/ZH/HI. `.surface-screen` relative Y values were aligned with max diff `0`: EN `211.91 / 211.91 / 211.91`, ZH `213.91 / 213.91 / 213.91`, HI `212.91 / 212.91 / 212.91` at both viewports. Horizontal overflow was `0` in every checked viewport/language. Additional browser overflow checks reported `rowOverflowCount=0`, `cardTextOverflowCount=0`, and `screenBeyondCardCount=0` for all six viewport/language combinations. Inline script syntax extraction -> PASS for 5 inline scripts. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. Smoke guard PASS confirmed `RESP_03` and `PARTNER_04` remain present, `Survey Station`, `survey station`, `Subcontractor`, `STN_03`, and `SUB_04` were not reintroduced in `index.html`, and the About href remains `about_sampora_issues_fixed.html`.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; real visual perception / true visual acceptance PASS. A normal user now sees the three Product Surfaces small screen panels begin at the same vertical position inside their cards instead of the third card floating upward, with no horizontal overflow or row/card text escaping the card.

**Remaining failures / next agent:** none found for this scoped Product Surfaces screen-alignment fix. Existing unrelated dirty workspace changes were preserved and not reviewed. Footer, copy/i18n, Pricing, CTA, Topology/Workflow animation logic, other pages, delivery folders, zip/package work, staging, commit, and push remain deferred unless explicitly requested.

### 2026-05-22 JST - Package B Solutions P0/P1 topology-fit-status closeout

**Files changed:** `sampora-website-public-v3/solutions.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. Page-source scope was limited to Solutions P0/P1 copy/i18n, section leads, compare row copy, topology foreground terminology, status bar synchronization, topology/modal language attributes, duplicated JS render-function cleanup, manual topology-fit lock behavior, and a mobile top-chrome gap fix. Footer structure/content, navigation structure, other pages, QA artifact outputs, delivery folders, zips, staging, commit, push, and packaging were intentionally untouched.

**Audit items addressed:** `[CONFIRMED]` items from `E:\Downloads\SAMPORA_SOLUTIONS_PAGE_P0_P1_CODEX_INSTRUCTIONS.md`, with the user's latest override that Hero topology and Operating Fit must remain linked during auto rotation. Owner package is Package B Solutions/Resources; acceptance categories are Scope, Language/i18n, Topology motion/interaction, Visual acceptance, and Navigation/status runtime guard. `TOPOLOGY-001`, `I18N-001`, `TERMINOLOGY-001`, `VISUAL-QA-001`, and `HANDOFF-006` remain guard rows; no ledger status changed.

**Implementation summary:** the controller stayed orchestration-only. Faraday was closed after long silence and left partial `solutions.html` edits, which were treated as the current baseline and not reverted. Bernoulli completed the JS structure and behavior path: duplicate `renderTopologyModebar`, `renderHeroTopology`, and `renderRecommendation` definitions were reduced to one stable version; `renderStatus()` now follows the active solution; solution cards, fit buttons, modebar buttons, and topology nodes route through `setTopologyFit(..., { userSelectedFit: true })`; and `setLang()` now updates `data-i18n`, `data-i18n-title`, and `data-i18n-aria-label`. Zeno cleaned non-footer fallback/initial foreground terminology so `owned panel / station`, `STN_04`, and `survey station connected` no longer remain in the Solutions hero/topology/cards/fit/matrix source path, while `PANEL_04 / panel delivery connected` is present. Darwin added a mobile-only `.mast` top guard so hidden mobile status no longer leaves a 34px blank band.

**Verification run:** implementation workers reported inline script syntax PASS, `git diff --check` PASS with LF/CRLF warnings only, duplicate-function counts at `1`, `renderStatus` and manual-lock paths present, old non-footer topology terminology absent, `PANEL_04` and `panel delivery connected` present, mobile top guard present, and live-dot `pulse 1.9s ease-out infinite` preserved. Final read-only review reported `git diff --check -- sampora-website-public-v3/solutions.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> PASS with LF/CRLF warnings only and inline script extraction `node --check` -> PASS for 3 inline scripts.

**Runtime and visual evidence:** final read-only browser review observed auto rotation `Panel -> Supplier Network -> Enterprise`; Hero modebar/topology, Operating Fit active/recommendation title, and status bar stayed synchronized. Manual clicks held for more than `3.5s` without auto switching after solution-card selection, Operating Fit selection, and topology-node selection. EN/ZH/HI switching updated status, modal title, close `title`, and close `aria-label`; close text remains `X`. Desktop `1440x900` reported `overflowX=0`, `.status display=block height=35`, `.mast top=35`; mobile `390x900` reported `overflowX=0`, `.status display=none height=0`, `.mast top=0`. The status bar kept the existing live-dot pulse and no scan/marquee-style status animation was observed.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; real visual perception / true visual acceptance PASS. A normal user sees the intended linked Solution loop, with status, topology, and recommendation changing together during auto rotation and staying fixed after manual choice. Section leads are visible without crowding, mobile has no top blank band, and the current checked foreground surfaces do not push survey station/subcontractor as the Solutions core target. The remaining `survey station` wording is in `footerDesc` only and is out of scope for this Package B Solutions P0/P1 round.

**Remaining failures / next agent:** none for this scoped Solutions P0/P1 topology-fit-status closeout. If footer/shared positioning copy should be changed later, open a separate footer/shared-copy task. Existing unrelated dirty workspace changes remain preserved and unreviewed. Packaging, Lighthouse/axe full delivery, delivery sync, zip rebuild, staging, commit, and push remain deferred unless explicitly requested.

### 2026-05-22 JST - About page mojibake repair

**Files changed:** `sampora-website-public-v3/about_sampora_issues_fixed.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. About source write scope was limited to encoding pollution, damaged visible fallback text, the main About `I18N` dictionary, CTA/menu/close glyphs, title/meta text, and UTF-8 no-BOM normalization. Existing current dirty footer blocks were intentionally preserved: `<style id="sampora-standard-footer-css">` kept SHA-256 `37bffef2dd49004fbe3e5bdc14c59bdc45c6232001013980a29fdf2e3827a669`, and `<script id="sampora-standard-footer-i18n">` kept SHA-256 `f94a54ad8571939f8afa09411dd4191bfeefb90909506d3fe7fd44867c928900`. Other pages, delivery folders, zips, staging, commit, and push were intentionally untouched.

**Issue addressed:** `[CONFIRMED]` Package About Language/i18n and active-source mojibake failure. Red check before repair found a UTF-8 BOM, source mojibake markers, damaged language-button/mobile glyph text, and a failing main inline script caused by corrupted strings. The repair rebuilt About from byte-safe `HEAD` text while reinserting the two current footer dirty blocks by exact id.

**Verification run:** post-repair source marker guard -> PASS: configured bad marker set absent, required good glyph/text set present, and no UTF-8 BOM. Inline script syntax extraction -> PASS for all 3 inline scripts, including JSON-LD, main About script, and footer i18n script. Browser/CDP About check covered EN/ZH/HI at `1440x900` and `390x900`: visible nav, language buttons, hero CTA, footer, mobile nav glyph/title/meta had no mojibake markers; mobile toggle changed from menu glyph to close glyph; horizontal overflow was `0`; About href remained `about_sampora_issues_fixed.html`; Book demo and Start trial hrefs retained required `intent=` URLs. `node "sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs"` -> PASS. `node "sampora-website-public-v3\qa-evidence\active-handoff-mojibake-guard.mjs"` -> PASS.

**Remaining failures / next agent:** none found for this scoped About mojibake repair. Existing unrelated dirty workspace changes remain preserved and unreviewed. Broader browser suites, Lighthouse/axe, delivery sync, zip rebuild, staging, commit, and push remain deferred unless explicitly requested.

### 2026-05-22 JST - About bottom CTA arrow mini-fix

**Files changed:** `sampora-website-public-v3/about_sampora_issues_fixed.html` and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. Scope was limited to the bottom About CTA question-mark arrow residual in the fallback and EN `ctaPrimary` string, both restored to `Start trial →`; footer CSS/i18n blocks, other About content, other pages, delivery, zips, staging, commit, and push were intentionally untouched.

**Verification run:** source grep, About EN/ZH/HI browser/CDP visible marker checks at `1440x900` and `390x900`, inline script syntax, `final-audit-static-check.mjs`, `page-layer-static-check.mjs`, `active-handoff-mojibake-guard.mjs`, and scoped `git diff --check` were rerun in this mini-fix round.

**Remaining failures / next agent:** none found for this scoped CTA arrow residual.

### 2026-05-22 JST - Plans and About hero sweep removal worker

**Files changed:** `sampora-website-public-v3/plans.html`, `sampora-website-public-v3/about_sampora_issues_fixed.html`, and `SAMPORA_LATEST_REPAIR_HANDOFF.md`. Scope was limited to removing the hero right-card sweep/shine pseudo-element animation from Plans `.plan-selector-card` and About `.hero .console-card`. Header/nav active underline, footer title/link lines, `.live::before`, About `.hero::before` static background glow, visible copy, CTA hrefs, i18n dictionaries, other sections/pages, delivery folders, zips, staging, commit, and push were intentionally untouched by this worker.

**Verification run:** source grep found no `plan-selector-scan`, `.plan-selector-card::after`, `console-scan`, or `.console-card::after` in the touched pages. Inline script syntax extraction passed for `plans.html` `4/4` and `about_sampora_issues_fixed.html` `2/2`. Browser/CDP checks at `1440x900` and `390x900` showed `.plan-selector-card::after` and `.hero .console-card::after` both have `animationName=none` and `content=none`, horizontal overflow `0`, and hero right cards still within viewport width. `node sampora-website-public-v3\qa-evidence\page-layer-static-check.mjs` -> PASS. `node sampora-website-public-v3\qa-evidence\final-audit-static-check.mjs` -> PASS. `git diff --check -- sampora-website-public-v3/plans.html sampora-website-public-v3/about_sampora_issues_fixed.html SAMPORA_LATEST_REPAIR_HANDOFF.md` -> PASS with LF/CRLF warnings only.

**Acceptance status separation:** technical/static PASS; runtime geometry PASS; real visual perception / true visual acceptance PASS. A normal user no longer sees a moving sweep/shine line across the Plans hero right selector card or the About hero console card; the hero layouts remain intact on desktop and mobile.

**Remaining failures / next agent:** none found for this scoped sweep-removal worker. Existing unrelated dirty workspace changes remain preserved and unreviewed.

### 2026-05-22 04:39 JST - Package E Resources and Resource Manuals P0/P1 handoff closeout

**Scope and file ownership:** Package E updated only this handoff record. Implementation-worker source changes recorded for this P0/P1 round are `sampora-website-public-v3/resources.html` and `sampora-website-public-v3/assets/resource-manuals/app.js`. `sampora-website-public-v3/resource-manuals.html` was already dirty/pre-existing and was not claimed as a Package D worker repair in this closeout. `ISSUE_LEDGER.md` was intentionally left unchanged; guard rerun notes only apply to `MANUAL-001`, `TOPOLOGY-001`, `I18N-001`, `VISUAL-QA-001`, and `HANDOFF-006` as applicable.

**Implementation summary:** Package B added a mode-aware Resources status bar with EN/ZH/HI `statusModes`, `renderStatusBar()`, status refresh after i18n/topology updates, `surfaceText`/`setupText` section paragraphs, checklist `.check-state` cleanup with LIVE/READY removal and two-column button layout, targeted visible-copy replacements for backend map/backend entries/where available/payment settings, preserved the topology single timer/manual lock, and added the mobile top-chrome CSS mini-fix so hidden status no longer leaves a top offset. Package D added Resource Manuals app aliases for `#configuration-guide`, `#operation-manual`, and `#rollout-notes`; visible Configuration/Operation targets; an independent Rollout Notes module with EN/ZH/HI launch-preparation, roles/permissions, admin-entry, and launch-checklist content; `scroll-margin-top:112px`; runtime removal/replacement of legacy hidden spans; and preserved footer semantics.

**Verification summary:** `final-audit-static-check.mjs` PASS; `page-layer-static-check.mjs` PASS; `node --check` PASS for `assets/resource-manuals/app.js` and `data.js`; `resources.html` inline script parse PASS for 3 scripts; scoped source assertions PASS `54/54`; `resource-manuals-language-smoke.mjs` PASS; `resource-manuals-browser-language-check.mjs` PASS; `git diff --check` PASS with LF/CRLF warnings only. Runtime browser review was PASS/PASS_WITH_CONCERNS: Resources entry clicks/deep links were visible, EN/ZH/HI mode auto-cycle and 10.3s manual lock passed, Resource Manuals rollout content was visible/localized, no horizontal overflow at `1440x900`, `768x1024`, or `390x900`, mobile top chrome gap was fixed (`.mast top=0`, top hit header not BODY), and footer remained visible. Footer preservation: Package D reported the Resource Manuals footer hash preserved as `d0869bc56c89db0939113e43318469610e26415ae37a657b0d238a4a277d712b` with length `2915`; Resources footer semantics were not intentionally changed by Package B workers.

**Remaining failures / next agent:** none blocking for this P0/P1 scope. Non-blocking concerns: raw `resource-manuals.html` still has legacy hidden fallback spans, but `app.js` removes/replaces them at runtime and runtime review passed; `assets/resource-manuals/app.js` has one literal replacement-character marker inside a damaged-text guard regex, not visible output, and language smoke passed; ZH Resources third surface card still says `印度市场上线说明`, which is localized and outside the current forbidden/internal-copy scope, so classify separately only if a future audit asks to remove India-specific positioning; existing unrelated dirty workspace changes remain preserved/unreviewed. No packaging, zip rebuild, delivery sync, staging, commit, or push was performed.
